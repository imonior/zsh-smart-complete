# チェンジログ

このプロジェクトのすべての重要な変更はこのファイルに記録されます。

フォーマットは [Keep a Changelog](https://keepachangelog.com/) に基づき、このプロジェクトは
[セマンティックバージョニング](https://semver.org/lang/ja/) に準拠します。

## [v2.4.2] - 2026-09-26

### 修正
- **履歴のインデックス作成が履歴を実行していた。「グレーの候補が出ない」という報告の正体は、プラグイン全体が止まっていたことだった。**
  二つの履歴バックエンドとも、各行の出現回数を `(( _SMART_BUILD_FREQ[$cmd] > max_freq ))`（atuin 側は `$command`）と同じ形で比較していた。
  `(( ))` —— および `$(( ))` —— の中で添子の位置に置かれた裸のパラメータは、zsh では単純な展開で済まない。
  **展開されたテキストが算術式として再解析される**のだ。ここでのテキストは履歴の 1 行まるごとだった。
  報告者のマシン（zsh 5.9、履歴 912 行）で関数単位トレースをすると、評価は
  `((  _SMART_BUILD_FREQ[functions -T _smart_cmds_rebucket] > max_freq  ))` まで到達していた。
  shell の構文に対して算術が行われていたのである。そこから 3 つの結果が生じ、ユーザーには 1 つの症状にしか見えなかった：

  1. `$(…)` を含む行は、**インデックス構築の最中に実際に実行された**。シードした履歴ファイルでローカルに再現
     （その行が指すマーカーファイルが後で存在した）。新規の atuin シナリオは修正前のコードでこれにより `FAIL` になる。
  2. `]` が閉じていない行は `bad math expression: illegal character: ]` を投げる——ユーザーが見たエラーそのもの。
     呼び出し側は rebuild の `stderr` をすべて捨てるため、残る痕跡は空のインデックスだけだった。
  3. このユーザーの履歴では評価が**戻ってこなかった**：rebuild は Ctrl-C で中断するしかなく、それもコマンドごとに一度。
     `_smart_bootstrap_once` は rebuild **後に**自分自身を `precmd_functions` から外すので、`precmd` に残り続ける。
     おまけに `count == 0` の空インデックスが、キーストローク側の経路に完全な rebuild を再試行させる。

  同じ bootstrap が ZLE ラッパーを取り付けるのも rebuild の **後**なので、ウィジェットは一度も有効にならなかった
  （`_SMART_EVT_CAPTURED=0`）：インラインのグレー候補もメニューも出ない。`smart-status` は
  `history indexed: 0`、`last rebuild: 0`、`last suggestion []` と表示した。v2.4.1 の 2 つの色修正は無関係だった：
  このユーザーのセッションは `memo_ok=1 color=fg=110`・terminfo 256 色、つまり当時から正しかった分岐を通っている。

  カウントはスカラー経由で運ばれる。算術はそれを数としてしか読まない：`_SMART_BUILD_FREQ[$cmd]=$f` に
  `if (( f > max_freq ))`。`_smart_history_backend_atuin_build` も同じ形で修正した。
  `_smart_history_rebuild` の永続化ループも同じで、そこにある
  `$(( base - _SMART_BUILD_REC_RANKS[$c] ))` は、`$c` が履歴 1 行まるごとがゆえに同じ添字評価の問題を持っていた。
  これは増分 upsert 側がすでに使っていた書き方でもあり、これで二つの書き込み経路の形が揃った。

### 追加
- `tests/test-history.zsh` の シナリオ 10（28 → 34 アサーション）と `tests/test-atuin.zsh` の シナリオ 7
  （26 → 31）は、`]` が閉じていない 1 行と `$(touch <marker>)` を含む 1 行を置いた履歴をシードし、
  **本物の** rebuild を実行する——zsh 側は非対話 shell で `fc -R`、atuin 側は使い捨ての SQLite DB——、
  そして次を検証する：マーカーが作られていないこと、rebuild が `stderr` に何も書いていないこと、
  敵対的な行が key としてテキストのまま保存されていること、各 freq の値が正しいこと。

  修正前のツリーでの実測（zsh 5.9）：atuin シナリオは新しい 5 つのうち 3 つが落ちる——
  suite 自身のラベルで言えば `敌意命令行没有被执行 got=[yes] want=[no]`（置換が実行された）、
  `构建过程 stderr 干净 got=[97] want=[0]`、`重复行 freq 累加 got=[1] want=[2]`。
  `$(( _SMART_BUILD_FREQ[$command] + 1 ))` の行自体が例外を投げるので、2 回目のカウントが入らない。
  history シナリオは「1 件赤くなる」より悪い：実行はシナリオの見出しで止まり、180 バイトが残る。中身は
  `_smart_history_backend_zsh_build:19: bad math expression: operator expected at
  '( ; while ...'` と `_smart_history_rebuild:67: …`、そして作られたマーカーファイル。

  これらが**カバーしない**もの：報告ユーザーが実際に踏んだ形、つまり評価がまったく戻ってこないという状態。
  あちらのマシンで観察されたもので、こちらでは再現していない——同じ 2 行の fixture はローカルでは大きな音で失敗するだけで、
  このバグを発見させたのはその失敗である。「戻らない」ことを検証するには、テストをハングさせるか実時間上限を置くしかないが、
  時間上限アサーションは名前のついた flake になる。コスト：ローカルで 0.5 秒と 0.3 秒。
  取り消し方：`场景 10` / `场景 7` という見出しで始まる 2 ブロックを削除する。

## [v2.4.1] - 2026-09-26

### 修正
- **zsh 5.8 / 5.8.1 で候補に色が付かなかった。原因は、それを識別するためのマーカーだった。**
  `region_highlight` の項目は属性ビットから再描画されるので、自分の枠をあとで取り出すにはテキストに何かを残すしかない——
  そこで `… fg=8 memo=zsh-smart-complete:suggestion` と書いていた。だが `memo=` を解析するのは zsh 5.9 以降
  （`Src/Zle/zle_refresh.c`）だけ。5.8 では `Src/prompt.c: match_highlight()` が色を保存する**前**に
  `else if (*teststr) break;` に到達するため、コンマ以外の後続トークンは残りの走査を丸ごと打ち切る。
  項目は属性**ゼロ**で格納されていた——計測値は `rh=1 12 none`、しかも同じセッションの `terminfo[colors]` は 256。
  これにより、最初の 2 回の修正が追っていた terminfo 仮説は否定された。マーカーは、実行中の zsh がそれを運べる場合にだけ
  書くようになった。それ以前のビルドでは、モジュールは「自分が何を書いたか」とその開始位置を覚えておくことで自分の項目を
  識別する——補完リストの再描画は範囲を `BUFFER` の末尾まで切り詰めるので、覚えたテキストはもう一致しないからだ。
  README が約束する性質は両方の分岐で成立している: 高亮は最大 1 枠、削除するのもその 1 枠だけ。
- **バージョンマトリクスの 3 セルは、コードではなくホスト側の理由で赤くなっていた。** 共有スタブ群が `opkg` を
  `PATH` に置いていたため、`install.sh` は Linux では毎回 `install-entware.sh` にハンドオーバーしていた——
  テスト対象のスクリプトが自分のフィクスチャと入れ替わっていたのだ。「`.zshrc` の書きかけ」は Linux 固有の
  インストーラバグに見えたが、実際には**もう一方**のインストーラのファイルで、あちらは設計上 managed マーカーを持たない。
  `tools/check-module-globals.sh` は文書化されたキー一覧を `{n,}` 区間表現で抽出していたが、Debian/Ubuntu イメージの
  `awk` は mawk で、POSIX モード以外では区間を無視する。結果としてキーは 1 つも取れず、dead-key の検証は
  「何もチェックせずに」緑になっていた。2 つのインストーラ群は `python3` と `ps` を必要とするが、最小イメージには無い。
  セルはブロッキングのままにしている——上の色 bug は、まさにそこだけで見つかるものだった。

### 変更
- **赤くなった job は、手元にある失敗をすべて名指しで公開するようになった。** runner の外から job ログを読むには
  認証が必要だが、その check run のアノテーションは不要だ。3 つの job は同じ梱包コードを書写し、それぞれ見つけた
  情報の一部を捨てていた: 広い grep に対する `tail -n 5` は 7 件の失敗アサーションのうち 1 件しか公開しなかった
  （誰も読めなかったため、2 回の執行はバイト単位で同一に返った）。エンドツーエンド手順の `tail -n 20` はスロットを
  画面テキストで埋め尽くし、失敗のほうが落ちた。release gate にはそれが無かった——v2.4.0 の失敗について公開したのは
  `Process completed with exit code 1` の 1 行だけだった。いま唯一の実装は `tools/annotate.sh` で、長いフィールドは
  *項目*の境界で複数のアノテーションに折り畳む（桁で切ると 2 つのアサーション名が入り混じってどちらも読めない）。
  すべて緑のときでも各スイートの `name:passed/failed` を公開するので、README が引用するアサーション数は、それを
  計測した実行から照合できる。常時 exit 0: ビルドを赤くできる診断ツールは第 2 のビルド体系になる。
- v2.3.0 の記録はマトリクスイメージのパッケージを zsh 5.7.1 / 5.8.1 / 5.9 としていたが、各セル自身の
  `zsh --version` は **5.8** / 5.8.1 / 5.9 を報告する。つまり `ubuntu:focal` の数値が誤りで、5 言語とも 5.8 に直した。
  workflow にも数値の出所を明記している。

### 追加
- `tests/test-annotate.sh`: フィクスチャログに対する 39 アサーション。「どのアノテーションも、スクリプト自身が宣言した
  予算より広くない」を含む——これはツールが実際に出力したバイトを測っている。梱包処理を再実装して照合すると、
  スクリプトがどう折り畳もうと合格してしまうからだ。`tests/test-display.zsh` に場面を追加（55 → 74 アサーション）:
  マーカー無し分岐を強制し、項目がキー入力ごとに積み上がらないこと、切り詰められた残骸も引き続き認識・除去されること、
  バージョン判定が `5.10` を `5.9` より新しいものとして読むこと（`5.1` と誤読しない）を確認する。

## [v2.4.0] - 2026-09-26

### 修正
- **インストーラーが最後に空の設定ファイルを書き上げ、その後はその修復を拒み続けていた。** `_install_user_settings` の最終手段の分岐——リポジトリのテンプレートを
  取得できなかったときにだけ通る——は、雛形の内容を `print -r --` で出力していた。
  `print` は zsh の組み込みコマンドであって `PATH` 上のプログラムではないため、
  bash スクリプトの中でこれを呼ぶと、たまたま `print` という名前のプログラムが
  入っているマシンを除いて exit 127 になる。その時点ですでに `.zshrc` の書き換えは終わっており、
  残された 0 バイトのファイルは「存在しないときだけ作る」ガードの条件を満たしてしまうので、
  以降どの実行も直すべきものがあると気づかない。両方のインストーラーを
  `printf '%s\n'` にした。この問題は、コードを読むのではなくインストーラーを実際に
  走らせて見つかっている。

- **インストーラーに渡されたミラー URL が、データではなくコードとして扱われて
  いました。** `SMART_INSTALL_GH_MIRROR` と「自分のミラーを入力する」への回答は、
  `curl` / `wget` の URL を書き換えるダウンロード用シムに入りますが、そのシムは
  **クォートしていない heredoc** から生成され、値がスクリプト本文にそのまま
  挿入されていました。つまり `https://x.example/'; touch ./pwn; '` は URL ではなく
  3 つの文であり、インストール実行時にそのまま実行されます。さらに
  `_run_remote_script` はインストーラーが選んでいないミラーから取得したものも
  実行するため、同じ文字列が実行コードの出どころまで決めてしまいます。シムは今や
  4 行の設定ファイルを `read -r` で読むようになり、文字列補間はしていません。
  両方のインストーラーが使う前に `_mirror_prefix_ok` で検査します: `https://` の
  ホスト、任意のポート、任意のパス — それ以外は拒否します。平文の `http://` を
  拒否するのは**意図的**です。取得したスクリプトは実行されるので、通信方式は
  打ち間違いで下げられるものではないからです。テスト第 27 節では悪意ある値から
  シムを組み立て、ペイロードが設定ファイルの外に出ないこと、実際のダウンローダー
  には元の URL がそのまま渡ること、何も生成されないことを検証しています。
- **インストールが途中で失敗すると、その傷跡が残っていました。** 両方の
  インストーラーは `.zshrc` を*順番に*書きます — まず options ブロック、次に
  loader ブロック。個々の書き込みはすべて rename なので、どれか一つがファイルを
  切り詰めることはありません。しかし 2 つの書き込みのあいだで中断すると、構文は
  通る、シェルは起動する、なのにプラグインは読み込まれないという `.zshrc` が
  残ります。となりにある `.bak.<timestamp>` は何も取り消せません。それを指すものが
  何もなく、作った実行はすでに終わっているからです。いまは `_guard_config_write` が
  最初の書き込み前にスナップショットを取り、`EXIT` trap が終了コード非ゼロでそれを
  戻します — インストーラーが新しく作ったファイルを消すこともあります。
  「もともと `.zshrc` はなかった」同じように戻すべき状態だからです。
  `_release_config_write_guard` は書き込み順の最後が完了した時点で trap を解除します。
  それより後の、無関係な段階の失敗が、ユーザーが頼んだ設定をなかったことにできない
  ようにするためです。第 25 節には guard なしの対照実行が残っており、それがこの節の
  他の検証に意味を与えています。
- **テンプレートが何も出力しなかったとき、動く設定が空に上書きされることが
  ありました。** `apply_template` の各分岐は直接 `$dest` へリダイレクトしていたため、
  認識できない接頭辞 — または中身が空で返ってきたダウンロード — では、ユーザーの
  ファイルを切り詰めたあとで失敗していました。いまは必ず `<dest>.zsc-new` に書き、
  それが空なら現在の言語でエラーを言って拒否し、`mv -f` 1 回（書き込み先と同じ
  ディレクトリ内で）で所定の位置に置きます。
- **使えて、読まれていて、宣言がどこにもない global が 2 つあった。** すべての
  モジュールが `no_warn_create_global` を有効にしているため、未宣言の名前への最初の
  書き込みは静かに global を作ってしまう。結果 `_SMART_PROBE_SUFFIX_RET` は「それで
  値を返す関数」の副作用にすぎず、`_SMART_RH_MARKER` はトップレベルの代入にすぎなか
  った。いまは両方とも定義の場所で `typeset -g` を明示している。どちらも動いている
  シェルでは誤動作しなかった。見えなかったのは「このモジュールはどの状態を持つのか」
  を宣言を読んで答える種類の検査のほうで、それが見つかった不具合だった。
- **状態コンテナ自身の key 一覧が 4 か所で誤っていた。** 将来の移植のために
  `_SMART_STATE` を記録しているコメントブロックは、コードでは `history.frequency` と
  名前がついているサブマップを `history.freq` と書いていた。何も書き込んだことのない
  `history.first_char` を載せていた（`_SMART_CMDS_FIRST` の先頭文字バケットがとっくに
  その計画を置き換えている）。誰も読み書きしない `last_err` を約束していた。そして
  毎回のリビルドで使われている 3 つのサブマップ——`history.cwd`、`history.host`、
  `history.exit`——は載っていなかった。`suggestion.source` の値の列挙にも、ネイティブ
  メニュー経路が実際に格納する `completion` が抜けていた。仕様を名乗る文書は静かに
  古びるので、下のチェッカーは `lib/` に書き手も読み手もいない key を弾くようになった。
- **単一列ポップアップで、入力済みの語がパターンとして使われていた。** コマンド一覧は
  `${(M)_sc_out:#${w}*}` で絞り込んでいたが、これは入力中のテキストをそのまま glob に
  渡すことであり、そこで特殊なのは `[ ] * ?` だけではない。`EXTENDED_GLOB`（複数の人気
  フレームワークが有効にする）の下では `#`、`^`、`(`、`<->` もパターン構文なので、
  エスケープしていない接頭辞は、入力したテキストが接頭辞でもないコマンドにマッチして
  しまう（実測：`zsc#` が `zsc_probe_alpha` にマッチした）。あるいは単に不正なパターンに
  なる。コマンド側の分岐は、パス側の分岐が以前からやっていたように `${(b)…}` で語を
  エスケープするようになった。これで `git#` はコマンド `git#` を意味する：文字どおりの
  マッチは無く、何も生成されず、その語はこのモードがカバーしていない他の文脈と同じように
  ネイティブのグリッドへフォールバックする。`tests/test-menu.zsh` の 14h 節は、エスケープ
  後の接頭辞が何もマッチしないこと、そして同じ入力で stderr が空のままであることを
  断言し、おまけに普通の接頭辞はちゃんとマッチするかも断言している——これがあるから
  前の二つは空振りでは済まない。
- **同じ秒内の 2 回の実行が、`~/.zshrc` のバックアップを上書きし合っていた。** アンインストールは
  管理ブロックを外す前に設定を `.zshrc.bak.<epoch>` へコピーし、インストールも同じ形の
  名前へコピーする——つまりこの名前は、2 つの実行が本当に 1 秒以上離れているときだけ一意で、
  「入れて、その入れを取り消す」はまさに 1 秒を共有し得る組み合わせだ。そうなると `cp -p` は
  そこに既にあったコピーを黙って置き換える。置き換えられる側は、アンインストーラーが守ると
  約束した、手で書いた設定のただ一つの版である。いまは名前が埋まっていればコピーは
  `.bak.<epoch>-1`、`-2` …とずれていく。種明かしは `tests/test-installer-sandbox.sh` の
  コイン投げだった：第 8 節は何もしないアンインストールのあとのバックアップを数えて正確に 1
  個を期待していたが、それはインストールとアンインストールのコピーが同じ秒にぶつかって互いの
  証拠を消したときだけ成り立つ。いまでは直前の実行が残した組と比較する。そして `find | head -1`
  を「アンインストールのコピー」と読んでいた主張は `sort | tail -1` になった——インストール
  側も設定をバックアップしていた実行では、`find` が最初に返すのはインストールが *シード*
  ファイルについて作ったバックアップで、管理ブロックなど入っていない。同じコインの裏表だ。
### 追加
- **`tests/test-installer-sandbox.sh`——インストーラーが初めて「実行」された。**
  `tests/` の既存のものは `lib/*.zsh` を source するか `install.sh` から単一の関数を
  取り出してフィクスチャで駆動するだけで、インストーラー全体を走らせるテストは
  ひとつもなかった。関数を組み上げるコードも、その実行順も、このファイルが **bash** で
  実行されるという事実も、どれも検証対象外だった。新しいスイートは `env -i`
  （環境変数を一切継承しない）の下で両方のインストーラーを走らせ、`HOME`、`ZDOTDIR`、
  `XDG_*` は作成した `mktemp -d` の中に置き、`PATH` の先頭にスタブ用ディレクトリを
  置くので、ネットワークにもパッケージマネージャーにも届かない。`curl` / `wget` /
  `git` / `brew` / `opkg` の呼び出しは実行されずにログへ記録されるだけで、そのログ自体も
  アサートされている（ミラーの速度テストは走らなければならない、
  `SMART_INSTALL_GH_MIRROR` は clone の URL を書き換えたことが見えなければならない、
  Entware 用スクリプトは `opkg` ガードの手前で `.zshrc` に触れず止まる）。12 本の
  インストール経路——既定値、4 種類の combo プリセット、5 種類の UI 言語、2 つのネットワーク
  分岐——それぞれについて、終了コード、`unbound variable` の有無、`command not found` の
  有無、そしてインストールが成功したなら残すべきもの（`.zshrc` の管理ブロック 2 つ、
  空でない `settings.zsh`）を検証する。ソースを読む grep は heredoc の本体を先に空白行へ
  置き換える。これらのスクリプトは zsh 設定を *内部に埋め込んで* おり、あの行たちは
  zsh で正しいからだ。対応する 2 つのアサートは、このフィルターがテンプレート内の zsh を
  隠しつつ実行される zsh は隠さないこと、そして行数を変えないこと——報告される行番号が
  引き続きファイルそのものを指すように——を確認する。55 アサート。
- **`tests/test-installer-shared.sh`——2 つのインストーラー間の約束事を、前提ではなく
  検査に置き換えた。** あるファイルが `msg()` に要求する message key が、そのファイル
  自身の `_msg()` で定義されているかを検証する（未知の key は key 自体を出力するのが
  答えなので、この種類の不具合を捕まえられるアサートはこれしかない）。プローブ用のファイル
  1 つでこの lint が空振りしていないことも確認する。そのうえで heredoc を理解する
  パーサーで 2 ファイルを関数ごとに比べる：一致するものは共有 core へ、一致しないものは
  理由を添えた一覧に載っていなければならない。一覧にない新しいズレはスイートを失敗させ、
  載っている項目が静かに一致へ戻っても同じく失敗する。13 アサート。

- **`--uninstall`、および argv が届かない `curl | bash` 用の `SMART_UNINSTALL=1`
  を追加しました。** 削除するのはインストーラーが書いたものだけです — `.zshrc` 内の
  管理された 2 つのブロック、プラグインのチェックアウト、`settings.zsh`、そして
  私たちが作った `zsc-settings` シンボリックリンク。fzf、starship、atuin、zinit
  などのパッケージはインストールしたまま、`starship.toml` もすべての `.bak.*`
  もそのまま残します。これらはシェル全体と共有されており、私たちのほうが後から
  来た可能性が高いからです。`.zshrc` を編集する*前に*タイムスタンプ付きのバックアップを
  取り、それが作れない場合は編集を拒否します。ヘッドレス実行では `SMART_UNINSTALL=1`
  自体が確認の意味を持ち、ターミナルではたずねます。除去処理は
  `_upsert_options_block` の逆で、マーカーより前の古い形 — `zinit ice` と
  `zinit light imonior/zsh-smart-complete` — は**対で**取り除きます。孤立した
  `zinit ice` は次に読み込まれるプラグインの指定を静かに書き換えてしまうからです。
  私たちのブロックの前にしか存在意義のなかった空行は一緒に消え、ユーザー自身の
  2 つの設定の間の空行はそのまま残ります。
  `tests/test-installer-sandbox.sh` は現在、インストーラーが決して書かない行を
  シードしたホームへインストールし、そこからアンインストールして、設定が**1 バイト
  違わず**元の状態に戻ることを検証します。
- **未使用コードを検出す lint を 2 つ。** これまで見つかっていた腐敗の類は
  ソースからは見えません。一方はインストーラーが定義しているのに一度も呼ばない
  関数を報告し、もう一方はカタログに定義されているのにどちらのインストーラーも
  呼んでいないメッセージキーを報告します — 両カタログは設計上、共通の上位集合
  なので、この検査はファイルをまたぐ必要があります。それぞれの lint には検出器
  自体のプローブ（生存関数 vs 死んだ関数、heredoc 内の `read` vs 外、キーの
  接頭辞 vs 語全体）と、カタログ走査が実際に数百キーを見つけたことを検証する
  アサーションが付いています。「何も見つからなかった」ことで通過する lint は
  無意味だからです。見つかったもの: `_cleanup_old_baks`（91 行、見つけた
  `~/.zshrc.bak.*` をたずねずにすべて削除する上、未設定の配列を参照していたので
  `set -u` では途中で終了し、そもそも到達不能でした）、`_backup_if_normal`、
  `install_or_upgrade_pkg`、そしてどちらのスクリプトにも呼び出し側のない
  カタログエントリ 34 件。
- **`tools/check-module-globals.sh` — 新しい global は理由を述べなければならない。**
  `lib/state.zsh` は「可変のクロスモジュール実行データはすべてここにあり、コードベースの
  よそに単独の `_smart_thing=foo` は存在しない」と主張していた。その外側にある 66 個の
  global がそれに反対している。欠陥だったのは数ではなくそのコメントだった：それを
  信じた読者には実際にある状態の探しようがなく、global を 1 つ増やした作者にも抵抗は
  なかった。ルールはいま、本当に保っている境界を書いている——実行 *状態* はコンテナへ、
  そのほかは同ファイル末尾の登録表へ理由を添えて——そしてチェッカーは両方向を見る：
  未登録の global は落ち、何もマッチしない登録行も落ちる（リネームが、もう存在しない
  コードベースを語ったまま残る登録表を、リネームで置き去りにする——それを捕まえる
  のがこの方向の検査だ）、
  理由のない項目は落ち、記録されているのに誰も書かない読まない key も落ちる。宣言その
  ものを移す件は、拒否する前に測った：コンテナの下標は 1 回の読みが 1 µs をはるかに
  下回り、素の global は約 0.2 µs。つまり速度はもともと論点ではなく、寿命が論点だった。
  keymap ごとバインディングスナップショットは、ユーザー自身の keymap から 1 シェル 1 回
  取ってくるもので、`lib/state.zsh` は `lib/event/zle.zsh` の *前に* source される。
  `_smart_state_reset` で空になるコンテナへ移せば、ユーザー自身の設定からしか復元
  できないデータを消してしまうことになる。出パラメータ用の枠（`_SMART_BUILD_*` と、
  フォークしない `_scan` の返り値）はもともと 1 回の呼び出し分の寿命しかない。`tests/test-module-globals.sh` は
  84 件のアサーションを追加したが、その大半はコードベースではなくチェッカーを狙っている。
  正しく動くことを誰も確認できない lint は、ただ緑と報じるだけの lint だからだ。これは
  `tests/run-all.sh` が自動発見するので CI でも走る。
### 変更
- **両方のインストーラーが `set -u` 付きで走るようになった**（前は `set -eo pipefail`）。
  このような分岐を実際にたどる何かがあって初めてこのフラグは誠実になるので、
  上のスイートはそのために存在する。そこに到達するのに 3 件の修正がいった。`MIRROR_TIMES[…]` の
  11 か所を `:-999` で既定値にし、空の配列になり得る 2 か所の展開を
  `${arr[@]+"${arr[@]}"}` にした——macOS に出回っている bash 3.2 は `-u` の下で
  `"${empty_array[@]}"` を即座に中止させるが、`"$@"` は安全だ。
- **キーバインドを尋ねるプローブが 1 つになった。** `lib/event/zle.zsh` は
  `lib/engine/native.zsh` の `_smart_current_binding` を 1 バイト違わず複製しており、
  過去の退行のせいで存在している 2 つのガード（`bindkey` の表示の*最後*のフィールドを読む、
  キー側の文字列でマッチしない、`undefined-key` は未定義扱い、自前の widget を「元のバインド」
  として受け取らない）まで一緒にコピーされていた。そのため片方を直すともう片方は静かに
  壊れたままだ。複製は削除し、残った側の関数に「これが唯一のバインドプローブである」と明記した。
  `_smart_event_capture_originals` はシェルの起動あたり 20 回前後これを呼ぶ。
- **メニューの計測がタイプ中に fork しなくなった。** `_smart_menu_now_ms` は値を
  コマンド置換で返していたので、一覧の更新 1 回に 2 回の読み取りが**各 349 µs**かかる。
  これまで lister が採ったのと同じ worker/wrapper の分離にし、ホットパスの
  `_smart_menu_now_ms_scan` は `_SMART_MENU_NOW_MS` へ値を公開し、印刷する側のラッパーは
  `smart-doctor`・ステータス出力・テストのために残す。実測**各 9.9 µs**。
- **両方のインストーラーが共有する部分は 1 回だけ書く。** `install.sh` と
  `install-entware.sh` は約 80 の関数を重複させていた。どちらも
  `curl -fsSL … | bash` で単体実行できるスクリプトとして案内しているため、実行時に
  となりのファイルを source できない。その制約は本物だが、これまで満たし方が
  コピペで、しかも複写はずれていなかった——同じ文字列が両方で別々の message key に
  入っており、`msg()` は未知の key には key 自身を返すので、打ち間違いはエラーに
  ならずにユーザーの端末へ `mirror.unavailable_line` と表示する。いまは環境に依存しない
  30 の関数（ミラー、curl ラッパー、ダウンロード shim、言語メニュー、オプション
  ブロック）を `lib/install/core.sh` が持ち、`tools/build-installers.sh` がそれを
  マーカー行の間にそのまま両ファイルへ差し込む。ユーザーにはビルド手順がないので、
  生成物はコミットしたままにする。CI は `bash tools/build-installers.sh --check` を
  走らせるので、ブロックがソースと食い違えば出荷済みの不具合ではなくビルド失敗になる。
  重複のまま残る 21 の関数は意図的に環境依存（opkg のパス、`$HOME` と `$ZDOTDIR`、
  メッセージカタログ）で、理由とともに `tests/test-installer-shared.sh` に載っている。
  新しいズレはスイートを失敗させ、載っている項目が静かに一致へ戻っても失敗させる。
  このリファクタは宣言ではなく実測で検証した：body ハッシュ付きの関数集合も、両ファイルの
  最上位文の順序も変わっていない。対象に含まれていたテキスト変更は 1 件だけで、
  実際にその 1 件をおこなった：entware の `_tty_read` のコメントを install.sh と
  同じ書き方にした。

- **4 つの BEGIN/END マーカー定数は共有コア側で定義するようになりました**、各
  スクリプトが最初にブロックを書き込む地点ではなく。アンインストールは何もインストール
  されるより前にこれらのブロックを見分けなければならず、それらの代入が 4000 行
  スクリプトの後半に置かれているうちは、それができませんでした。
- **`lib/state.zsh` は読み方と、誰が空にしてよいのかを書くようになった。** accessor は
  `print` するので、呼ぶことはコマンド置換と fork（約 0.4 ms）を意味する——ところが
  ファイルはモジュールに「これらのヘルパー経由で読み書きせよ」と言い、実際のホットパスは
  すべて配列を直接下標していた。これは正反対の、そして本当に従われてしまう助言だった。
  文書はいま実規則（書き込みはヘルパー、キー入力パスは直接下標）を書き、ここでの 1 読み
  あたりの実測も載せている：素の global で約 0.2 µs、リテラル key で約 0.6 µs、key を
  変数に入れると約 0.8 µs、`$()` 経由だと約 400 µs。`_smart_state_reset` の見出しも、
  実際の呼び出し手を挙げるようになった（`smart-reindex` ではない：あのコマンドは
  `_smart_history_rebuild` でインデックスを組み直すもので、こちらは refill する履歴の
  枠だけを意図的に消す。コンテナ全体を空にすると `enabled`、`buffer`、ラップ済み
  widget が変化検出に使うカーソル記録まで失われる）。
- **単一列ポップアップは、入力を続けても形を変えなくなった。** `SMART_MENU_SINGLE_COLUMN`
  はコマンド名を*行の最初の語*にだけ生成し、最近ディレクトリを `cd ` 直後の*空語*にだけ
  一覧していた——だから `ls -la | gr` はファイルシステムから候補を描くのに対して、まだ何も打っていない
  プロンプトでの `gr` はコマンドを描く。`cd Dow` は現在のディレクトリのファイルを答える
  のに、となりのネイティブ・グリッドはとっくに `~/Documents` を出している。機能のこの二つは
  ネイティブの補完器と一致していて、はみ出していたのは縦一覧のほうで、ユーザーに見える症状は
  「入力している途中で、候補がどこから来ているのかが変わる」ことだった。いまでは、シェルがまだコマンドを
  選んでいる位置はすべてコマンド位置とみなす——`|`、`&&`、`;` の後、そして代入や
  コマンドをくるむ語（`sudo`、`command`、`env`、`time`、`nice` など）が並んだ後の語。
  最近ディレクトリも入力済みの接頭辞に答える（完全なパスと最終成分、どちらも見る）。ただし
  従来どおり `cd`/`pushd` だけ、`SMART_RECENT_PATHS` がオンのときだけ、そしてその語がまだ
  明示的なパスではないときだけ——ネイティブの `cd` 補完器と同じ 4 つの条件であり、それゆえに
  二つの一覧は同じ形を保てる。いまだにグリッドへフォールバックするものは、推測でなく
  書き出してある：git サブコマンド、ssh ホスト、オプション文字列、`~user`、そして
  コマンドをくるむ語がすでにコマンドを取ったあと（`sudo git`）。一つの限界は隠さず断言した：`git log|gr` は
  コマンド位置では*ない*。ポップアップの語は空白で切られるので、現在の語は `log|gr` だからだ
  ——一つの機能のために「語」の定義を作り直すとポップアップ全体が動くので、この判定は
  控えめに保ってある。これは打鍵のたびに走るため、`tests/test-perf.zsh` に節が増えた（語が
  2 つの行で 1 回約 19 µs、長い行に対するスケーリング上限も併せて）——挙動のゲートと
  並べて。この主張には画面側の証も加わった：`tests/e2e-tmux.sh` のシナリオ 10b+ は 6 つの
  コマンド名を行頭と `|` の直後でそれぞれ入力し、どちらでも 6 行であることと 1 行に 2 つの
  候補が無いことを要求する。これは本当に失敗し得る断言だ——以前の判定に戻すと後の入力では
  候補が一つも生成されず、画面には一覧が出ないかグリッドになる。既定はいまだに `false`
  である。
## [v2.3.0] - 2026-09-24

### 修正
- **ヒストリが大きくなると、インデックス再構築のあいだシェルが止まった。** 2 か所で結果を 1 件ずつ積み上げており、積むたびにその時点で溜まった内容を毎回丸ごとコピーしていた：保存用のリスト文字列（コマンドごとに `joined+=$'\n'$v`）と、先頭文字別のバケット表——後者は要素ごとに組み立てると、追加するにも添字で読み戻すにも zsh 配列の連結リストをたどることになる。8000 コマンドのヒストリでインデックスを再構築すると **693 ms** かかり、規模を 4 倍にすれば時間は **14 倍** だった。両方とも 1 回の C レベル走査で連結する形に変えた（文字列は `${(F)…}`、バケットごとに `${(M@)array:#${key}*}` の前方一致射影）ので、同じ 8000 コマンドの再構築は **30 ms**、4 倍の規模なら 4 倍の時間。上限側の 20000 コマンドでは、「気づかない再構築」と「プロンプトが数秒固まる再構築」の違いになる。この射影はキーをリテラルとして扱わなければならない：`*` で始まるコマンドだけのバケットにはそれ以外を入れてはならず、glob の特殊文字すべてについてアサートしてある。
- **Enter を 1 回押すごとに、インデックス全体を 2 回スキャンしていた。** これまで「最近使った」順は*順位*として保存されていた——新旧順に並んだ配列中の各コマンドの位置——したがって 1 件を最新に繰り上げると他の全コマンドのスタンプを打ち直しになり、メンバー判定もまず配列から探す必要があった。どちらも Enter のたびに O(インデックス規模) で、8000 コマンドのインデックスで同じコマンドを 50 回実行すると **9.8 秒**、周期再構築を切った状態では*新しい*コマンドごとに上限適用のため配列全体をコピーしていた（100 回で **19.3 秒**）。いま `history.recency` は単調増加の**最終使用 tick** を持ち、エンジンは年齢で比較する（`history.tick` から保存済み tick を引く）ので、Enter はスロット 1 個だけを更新して他には触らない。`_SMART_CMDS` は追記専用のメンバー集合となり、最近使用順はバケット側に置き、淘汰は `shift` 1 回。同じ 50 回の Enter は **32 ms**、上限到達時に新しい 100 コマンドは **12 ms**。
- **キー入力ごとに、メモリにある値を読むためだけにサブシェルを fork していた。** 表示層とイベント層が `$(_smart_state_get …)` や `$(_smart_menu_lister)` で状態を取っていた——ポップアップと再描画の経路上にそういう呼び出しが 9 か所、1 回はこのマシンで実測約 0.41 ms の `fork()` であり、そのうち 1 つ（`_smart_menu_word`）は `$()` の中で ZLE を動かせない原因にもなっていた。ホットパスは `_SMART_STATE` と `_SMART_MENU_LISTER_RET` を直接添字で読む。出力用のラッパーはステータス表示、`smart-doctor`、テストのために残してあり、どちらがどちらなのか両側にコメントを書いてある。
- **Entware インストールでは設定ブロックが任意の位置に——あるいは全く入らなかった。** `install-entware.sh` は `ZSC_BLOCK_BEGIN` / `ZSC_BLOCK_END` を定義しておらず、`_upsert_options_block` はこの名前で比較する：変数が空だと `grep -qF "" "$file"` は**すべての**ファイルにマッチするため、「管理されたローダーブロックの上に挿入する」分岐が常に当たり、実際には「最初の空行の前に挿入」として動いた。生成された `.zshrc` に空行はないので設定は黙って捨てられ、PC から共有した dotfile では、プラグイン読み込み前（バインドキーの初期化中に読まれる項目がある）ではなく任意の位置に入った。2 つのマーカーは `install.sh` が書くのと同じ値に定義し、両インストーラーのマーカー定数 4 つすべてを検査し、`_upsert_options_block` の全分岐をフィクスチャで駆動した（マーカーも空行もないファイルを含む）。

### 追加
- **`tests/test-state.zsh`、`tests/test-display.zsh`、`tests/test-native.zsh`。** 3 つのモジュールに直接テストがなかった：状態コンテナ（他の全モジュールがここ経由で書き込み、そのバケット鏡像はまさに今書き換えた）、表示層（`region_highlight` の 1 エントリが誤ればゴーストは色を失い、1 エントリ漏れれば構文ハイライターは自分のエントリを失う——いずれもサジェスト粒度のテストでは見えない）、そしてネイティブ Tab ブリッジ（契約の全体は「Tab はプラグイン読み込み前と同じことをする」ことで、`compinit` を一度も実行していないユーザーも含む）。新しいスイートは通常の `zsh` スクリプトで走る：`$bindkey`、キーマップ、`$widgets` はいずれもそこで機能し、ターミナルを要する `zle` だけは、渡されたウィジェット名を記録する mock 関数で代替する。
- **`tests/test-perf.zsh` —— 計算量のトリップワイヤー。** 上で直したバグはすべて挙動テストには見えなかった：出力は完全に正しく、症状は秒数だけ。入力中に走る 3 つの操作を実測する（実際のヒストリに近い形の合成インデックス、動詞 13 種なのでバケット処理と全配列処理を区別できる）、それぞれに壁時計の上限と 4 倍規模でのスケーリング比をアサートし、測定値を表示するので失敗する前にもうドリフトが見える。上限は実測の 5〜8 倍に置いてあるので CI のノイズでは触れない。この修正前のツリーでは失敗する：`FAIL l_set history.cmds at 8000 commands 692.6 ms (cap 500 ms)`、`FAIL 50 upserts over a 8000 command index 9772.7 ms (cap 500 ms)`。
- **CI を 3 ジョブに分けた（それぞれ何を証明できるかで分けている）。** `test` がゲート（macOS + Linux）。`zsh-versions` は同じスイートを `ubuntu:focal` / `ubuntu:jammy` / `debian:bookworm`、つまりパッケージ済み zsh 5.8 / 5.8.1 / 5.9 で実行する——このコードベースには「zsh 5.9 で実測」と書いた主張が多いのに、OS あたり 1 ビルドでしか検証されていなかったため。`e2e-advisory` は tmux のエンドツーエンドスイートを**非ブロッキング**で実行する：レンダリング後の画面を見られる唯一のスイートであり、同時に実ターミナルを実時間待ちで駆動する唯一のスイートなので、良い変更を赤くする資格を得る前にフリッカ率を測る必要がある。release ワークフローには `release` が依存する `gate` ジョブが増えた——スイートが緑、`VERSION` がタグと一致、CHANGELOG にその節があることが条件。人々が実際にインストールするタグへの発布は、これまで無条件だった。

### 変更
- **`./tests/run-all.sh` が覚えておけばよい唯一のコマンドになった。** `tests/test-*.zsh`（zsh）と `tests/test-*.sh`（bash）を自動発見し、部分一致フィルタと `-v` を受け取り、各スイートのアサーション数を合計する。CI ジョブは 12 個のファイルを直に列挙していて、「誰も追加しなかった新しいファイルは黙って一度も走らない」というコメントがまさにそばにあった——上の 2 モジュールが無テストだったのはそのため。
- **README のテストサマリから手カウントの数字をなくした。** 「12 テストファイル、804 アサーション」はスイートを 1 つ増やした最初のコミットで既に間違っていた。行は、実測の総数を出力するランナーを指す。5 か国語すべてで更新。
- **重複したコードとコメントを削除**した。同じウィジェットを 2 回登録していた 2 つ目の `zle -N _smart_native_complete`、reverse-Tab ウィジェットに逐語コピーされていた「列挙後の `region_highlight`」の理由書き、そして 3 か所にコピーされながら自己説明が 2 通りになっていた lister の表記メモを含む。受け付ける綴りは 1 度だけ宣言し、すべてのコピーが一致することは `smart-lister` を駆動するテストが保証する。
- **`smart-doctor` はシェルに関数を残さない。** プレフィックス探索用の関数をそのコマンドのトップレベルで定義していたため、普通の `smart-doctor` を 1 回走るとセッションに関数が残った。今はプラグイン自身のプレフィックスで命名し、コマンド終了時に `unfunction` する。

## [v2.2.11] - 2026-09-23

### 追加
- **インストーラーがプラグインの隣にローカル設定スクリプト（`zsc-settings`）を置くようになり、`~/.zshrc` を変更せずにすべての設定を調整できる。** インストーラーは `${XDG_CONFIG_HOME:-$HOME/.config}/zsh-smart-complete/settings.zsh` を作成し、`bin/zsc-settings` を `~/.local/bin/zsc-settings` にシンボリックリンクする。プラグインは内蔵の既定値より**前に**このファイルを source するため、そこに書いた `KEY='VALUE'` 行は既定値を上書きする。`zsc-settings` は `wizard` / `list` / `get KEY` / `set KEY VALUE` / `edit` / `reset [KEY]` / `path` / `init` を提供；`set` は設定の型（bool / int / enum / path）に基づいて値を検証し不正な入力を拒否する。変更は zsh 再起動後に反映される。別のファイルを使うには `SMART_USER_CONFIG` でそのファイルを指す。

### 変更
- **ライブ補完ポップアップを autocomplete 相当に：パスと単一マッチでも表示。** パス（例 `/u`、`~/l`、`cd /usr/`）は**最後の `/` 以降の最初の 1 文字**から候補を表示し、裸の `/`（末尾が `/` の場合も）はそのディレクトリを即座に一覧します——v2.2.9 のルールは 2 文字のセグメントを待っていたため、パス補完が「死んでいる」ように見えていました。単一マッチも 1 行ポップアップを描画（インライン灰字は引き続き表示）し、灰字だけにはなりません。パス以外の単語は引き続き 2 文字の閾値（`git s` は `git st` まで表示しません）。旧挙動に戻すには設定ファイルに `SMART_MENU_MIN_MATCHES=2` を書きます。


## [v2.2.10] - 2026-09-22

### 修正
- **1 文字入力しただけで行が確定したように見えていました。毎打鍵で端末が 1 行スクロールしていたためです。** 表示層の最後は `zle -R "" ""`（「新しい region_highlight を強制再描画する」）で、この形式は zsh にプロンプト領域をゼロから作り直させます。pty に実際に書き出されたバイト数で測定すると、2 行プロンプトでの 1 打鍵は **96 バイトで、うち `\r\r\n` を含みます** — 本物の改行のあとにカーソルアップと行全体の消去が続きます — これに対し**外すと 33 バイト**。端末ではカーソルはほとんどの時間、最終行にあります（コマンド出力があれば必ずそこに押し出されます）。最終行での改行は画面全体を 1 行スクロールさせ、続くカーソルアップはすでにずれた内容の上に着地するため、入力中の行の文字が混ざります（`lls-la /etc/`）。結果、どのキーも確定したように見え、その下に新しいプロンプトが印字されたように見えていました。ゴースト**と**ポップアップを両方切った状態でも同じ打鍵が 32 バイトかかり、素の zsh は 1 バイトです。この呼び出しは存在した 2 か所 — `_smart_display_update` と `→` の受け入れ経路 — から削除しました。`region_highlight` 配列の書き込みだけで十分です。widget からの戻りで ZLE が 1 回再描画し、その再描画がゴーストとその色を運びます（検証済み: 今も `ESC[38;5;110m`）。
- **本当に必要な 1 回の再描画は、毎打鍵ではなく必要なときだけ発行されるようになりました。** この一律再描画を外したことで、それが副次的に行っていた処理が露呈しました。より長いプレフィックスのために描いた候補行を撤回する処理です。一覧を描き続けている間、その領域は zsh 自身が維持します — `git st` から `git sta` へ絞ると 3 行が実際に 2 行になります — しかし**最後**の遷移、つまりプレフィックスが 1 候補まで絞られて一覧が抑止される tick は zsh の一覧処理に到達しないため、古い行は新しいゴーストの下に残り続けていました。プラグインは「自分が描いた行がまだ画面にあるか」を追跡し、**1 回の絞り込みにつきちょうど 1 回**だけ消去用の再描画を要求するようになりました（新規 `_smart_menu_forget_rows`）。意図的に何も描かない tick（ゲートが閉じた場合とスロットルでスキップした場合）も含みます。実測: この遷移ではきれいに消え（`git stat` の下に残留行 0 行、画面は修正前と同一）、通常の打鍵は依然 33 バイト、両機能オフなら 1 バイト — 素の zsh と完全に一致します。

### 追加
- **`tests/test-repaint.zsh` — 1 打鍵あたりに zsh が書き出すバイト数の回帰テスト。** tmux のエンドツーエンドではこの種のバグを**構造的にまったく検出できません**。tmux は「改行 + カーソルアップ」の組を打ち消すため、毎打鍵で再描画していてもいなくても、画面**も**スクロールバック**も**同一になります。このテストは `zsh/zpty` で実際の `zsh -i` を起動し、`zsh/mapfile` で生のバイト列を読み（コマンド置換では検証対象の末尾改行そのものが取り除かれます）、2 行プロンプトで 1 つの不変条件を検証します: **1 打鍵は 1 行に留まる** — 改行なし、垂直方向のカーソル移動なし、画面消去なし — さらに「両チャネルを切ると、1 打鍵は自分のエコーだけしか費やさない」。修正前のコードでは失敗しここでは成功するため、この再描画を復活させる将来の変更はユーザーではなく CI が先に検出します。`smart-menu status` は、プラグインが描いた候補行がまだ画面にあるかどうかも報告するようになりました。プラグインが保持する唯一の画面状態です。

## [v2.2.9] - 2026-09-22

### 修正
- **17 個の制御キーが死んでいました（Ctrl-A、Ctrl-E、Ctrl-K、Ctrl-L、Ctrl-R、Ctrl-U、Ctrl-W など）。** 印刷可能文字は 2 つの範囲バインド（`bindkey -R "^@"-"^_"`、`-R " "-"~"`）で覆っていますが、前者の範囲は**制御キーもまとめて**奪っていました。zsh 5.9 で実測すると、実行直後に `bindkey -M emacs '^A'` は `self-insert` を返し、Ctrl-A は行頭へ移動する代わりに生の制御文字を挿入していました。無事だったのは、プラグインが意図的にラップしているキー（`^M`、`^Y`、`^_`、`^G`、`^I`）だけで、これらは後から再バインドされるためです。現在は、プラグインのどのバインドよりも**前に** keymap ごとに全制御キーの元のバインドをスナップショットし、範囲バインドの直後に復元するため、Ctrl-A/E/F/K/L/N/P/R/T/U/W/V/X（および `^A`–`^_` の残り）はユーザーの設定どおりに動作します。ラップ対象のキーは、その後に続くラッパーバインドが改めて引き受けます。
- **`SMART_SUGGEST_STRATEGY=history,completion` の completion 側は一度も候補を出していませんでした——そしてそれが既定値になりました。** その背後にあるプローブは `$( _smart_menu_completion_suffix )` として呼ばれていました。つまりコマンド置換の内部で、`$()` はサブシェルを fork し、そこでは `zle` ビルトインを実行できないため、プローブは静かに空を返し、毎打鍵で `after == before` となり、出荷された日からすべてのユーザーで completion 戦略は死んでいました。既定値を `history,completion` にした理由は、history だけでは**ヒントが最も期待される場所に「ヒントの空白」ができる**からです。`cd /u` はファイルシステム候補が 1 つだけなので一覧は `SMART_MENU_MIN_MATCHES=2` で抑止され、`cd /usr/...` を一度も実行していなければ履歴にも一致せず、画面には何も出ません。そこで widget コンテキスト用の `_smart_menu_probe_suffix` が stdout ではなくグローバル変数で結果を返すようになり、直接呼び出されます。表示用ラッパーはテストと手動デバッグ用に残し、`$()` の中で決して使ってはならない理由をコメントに記しています。`SMART_SUGGEST_STRATEGY=history` で従来の履歴のみの動作に戻せます。
- **インラインの灰色候補が実際に入力した文字と見分けにくく、1 文字の前置詞だけでコマンド全体が入力済みのように見えていました。** 候補は `region_highlight` で `SMART_SUGGEST_COLOR` により着色されますが、既定値は `fg=8`（明るい黒）でした。多くのテーマ、そして Ubuntu / WSL の既定パレットではこれは通常の前景色とほぼ同じ濃さなので、`l` の後ろに薄い `s -la /usr/` が続くと、行全体がすでに `ls -la /usr/` になったように読めます。バッファには何も挿入されていません。`l` の状態で Enter を押すと実行されるのは `l` です。既定値は `auto` になりました。256 色端末では `fg=110`（明らかにくすんだ青灰色）に解決されます。`terminfo[colors]` で検出し、保険として `TERM` も見ます（`*256color*`、`*truecolor*`、および kitty / Alacritty / WezTerm / iTerm2 / GNOME / Konsole / foot などの既知の 256 色端末）。真の 8/16 色端末には色番号 110 が無いため `fg=8` のままです。明示的な指定（`SMART_SUGGEST_COLOR="fg=245,bold"`）はこれまでどおりそのまま尊重されます。
- **候補一覧が最初の 1 打鍵で開き、しかも最小文字数が「入力中の部分」ではなく「パス全体」を数えていました。** `SMART_MENU_MIN_PREFIX`（とコマンド語用の `SMART_MENU_MIN_PREFIX_CMD`）の既定値は `1` だったため、`l` を押しただけで一致するコマンドと履歴の全一覧が描画されました。まだ意味のある入力をしていないのに、です。`/etc/l` ではシェルの単語全体を測っていたため文字数が 6 と判定され、`/` を打った直後にも一覧が出ました。両キーは既定値 `2` になり、**最後の `/` より後ろの部分だけ**を数えます。`/etc/l` は 1 文字、`/etc/lo` で一覧が開きます。上限の `SMART_MENU_MAX_PREFIX` は引き続き単語全体を測定します。注記: 一度描画された一覧は通常の端末出力であり、zsh はそれを消去しません（素の zsh も同じ挙動です）。したがって「一覧を描く回数を減らす」ことだけが有効な手段です。描画後に消去する実装も作って測定しましたが効果が無く、削除しました。
- **Starship が推奨の 2 行レイアウトではなく、自身の既定プロンプトを描画し続けていました。** `curl ... | bash` で実行した場合 `templates/` ディレクトリが存在しないため、設定は 2 番目のインラインコピーから書き出されますが、そのコピーには `format =` 行がありませんでした。`format` が無いと starship はファイルの残りを暗黙のうちに無視し、自身の既定（`hostname in ~ via 🐍 ... ❯`）を出力します。これは v2.2.8 を新規インストールしたときに実際に見えた症状そのものです。フォールバックのコピーは `templates/starship.toml.example` とバイト単位で同一になりました（単一の正規書き込み関数。インストーラーテスト第 21 節で固定）。既存の `starship.toml` は分類されます: **推奨**（`success_symbol = "[:> ](bold green)"` の印と `format` キーの両方を持つ）はそのまま、**旧版**（以前のバージョンが書いたもので `format` キーが無い）は `~/.config/starship.toml.bak.<タイムスタンプ>` にバックアップして確認なしで自動修復、**カスタム**は従来どおり先に確認します。
- **インストーラーの出力に英語のハードコードが残っていました。** 残りのブロック——パッケージマネージャーのメッセージ、zsh / Oh-My-Zsh / atuin / fzf / starship / Entware の各セクション、クリーンアップ、バックアップ、powerlevel10k、`.zshrc`、組み合わせスニペット——が 5 言語すべてで i18n テーブルを通るようになりました。`install.sh` は 234 キー、`install-entware.sh` は 150 キー、ハードコードされた出力文字列は 0、空文字に解決されるキーも 0、参照されるキーはすべて定義済みです。これまで 5 つのキーが未定義のままで、生のキー名をそのまま表示していました（メインインストーラーの `i.fzf_not_in_feed`、Entware インストーラーの `w.omz_failed`、`w.zsh_theme_write_failed`、`s.zsh_theme_set`、`s.zsh_theme_appended`）。インストーラーテスト第 22 節。

## [v2.2.8] - 2026-09-21

### 修正
- **インストーラーの言語選択メニューがすべての選択肢を英語で表示していたため、英語を読めないユーザーは自分の言語を選べませんでした。** `select_language` はこれまで `msg lang.option_*` を通じて 5 つの選択肢を描画していましたが、この表は `LANG_CODE` に従い既定言語 (`en`) で英語にフォールバックするため、メニュー全体が英語になっていました。メニューは現在、各言語をその言語の表記 (endonym) で常に表示します: `English / 简体中文 / 繁體中文 / 日本語 / 한국어`。これで英語を知らなくても各ユーザーが自分の言語を認識できます。不要な `lang.option_*` キーを削除したため `install.sh` と `install-entware.sh` は同一の i18n 表とメニューを持ち、この一致性は新しいインストーラーテスト (第 18 節) で釘付けされています。

## [v2.2.7] - 2026-09-19

### 修正
- **インストーラーをすべて既定値で使っても、画面に動的な提案が 2 つ出る: プラグインのポップアップに加え、atuin 自身のフローティング検索 TUI。** 推奨設定はこれまで、`atuin` バイナリがあれば無条件に `eval "$(atuin init zsh --disable-up-arrow)"` を実行していました。このフラグは上矢印を解除するだけです: atuin は依然として **Ctrl-R**、さらに最近のバージョンでは **`?`**（Atuin AI）もバインドし、その TUI は 2 つ目の全画面 UI です（一致する履歴を重複込みですべて浮動リストに表示し、Enter で実行）。グレー提案のためにプラグインは atuin の SQLite 履歴 DB を直接読んでいるので、キー バインドに利益はありません。生成される設定は `ATUIN_NOBIND="true" eval "$(atuin init zsh)"` に変更されました: 履歴記録とプラグインの atuin バックエンドはそのままで、↑ / Ctrl-R / ? は標準動作のまま、画面の UI は 1 つだけです。atuin の TUI をバインドするかはインストーラーの明示的な質問になりました（既定はいいえ、5 言語対応）。また atuin init 行をコンボスニペットから統合ブロックへ移動したため、atuin 記録はすべてのコンボで機能します（以前は Oh-My-Zsh / p10k コンボで静かに無効でした）。同梱の `templates/zshrc.example` も同じ規則に従います。プラグイン管理ブロックの**外**にある `atuin init` 行（以前の手動設定の残りなど）を報告する読み取り専用の `_scan_foreign_atuin` を追加しました——放置すると 2 つ目の UI がいつの間にか復活します。

## [v2.2.6] - 2026-09-19

### 修正
- **インラインのグレー提案が色を失い、キー入力ごとにゾンビのハイライト項目が残っていました。** プラグインの `region_highlight` 処理にある 2 つの欠陥で、どちらも色を認識できる tmux プローブで実証しました。1) 項目を識別するマーカーが `#コメント` トークンでしたが、zsh は再描画のたびに `region_highlight` からコメント文字を捨てるため、前の項目を取り除くフィルタが一致しなくなり、入力ごとに不要項目が蓄積しました（実測：5 キー -> 8 項目、fast-syntax-highlighting 読み込み時 -> 171 項目）。現在は zsh がそのまま保持する `memo=` トークンを使います。2) 候補リストを描画すると zsh が行を再描画し、POSTDISPLAY に及ぶハイライト項目が BUFFER 末尾に切り詰められます（長さゼロ = 無色）。プラグインはリスト描画の直後にその項目を書き戻します——追加の再描画はなく、リストはそのまま残ります。zsh-syntax-highlighting と fast-syntax-highlighting の両方で検証済み：正しく共存でき、互換フックは不要です——以前の「F-Sy-H は他プラグインの項目を消す」という結論は誤りでした。
- **`bash -c "$(curl -fsSL .../install.sh)"` が `argument list too long: bash` で失敗。** install.sh が Linux の 1 引数 128 KiB 上限（`MAX_ARG_STRLEN`）を超えました。すべてのドキュメントはパイプ形式 `curl -fsSL .../install.sh | bash`（ミラー形式：`curl -fsSL .../install.sh | SMART_INSTALL_GH_MIRROR=... bash`）に統一され、スクリプトを argv で渡しません。
- **パイプで渡したときにインストーラーのプロンプトが入力を待たない**（`curl ... | bash`）：stdin はスクリプトそのものなので、普通の `read` は即座に空行を返し、言語 / プロキシ / ミラーのメニューが無言で既定値を採っていました。対話読み取りはすべて `/dev/tty` を開き直す `_tty_read` に統一しました。`BASH_SOURCE[0]` もガード済み——stdin で渡されると未設定になります（`set -u` では即中止、ガードがなければ `SCRIPT_DIR` が黙って呼び出し元のカレントディレクトリになります）。
- **Starship の解析エラー `Error parsing "format": --> 1:7`（`[$user] › $directory`）。** 2 つの誤りの重複です：starship のトップレベル `[テキスト]` グループには `(スタイル)` 接尾辞が必須、そしてトップレベルの変数名は `$username`（`$user` は `[username]` モジュール内でのみ有効）。テンプレートは `$username › $directory` に変更し、両方のコピー（サンプルテンプレートと Entware インストーラーのインライン版）は実際の `starship` バイナリでレンダリングする回帰テストで固定しました。

## [v2.2.5] - 2026-09-19

### 追加
- **其它の起動ファイルにある競合ローダーの探査（読み取り専用の提醒）。** インストーラーの競合クリーンアップは `~/.zshrc` しか編集しません——これは意図的な設計で、使用者が他で管理している設定を変えないためです。しかし `zsh-autocomplete` / `zsh-autosuggestions` のローダー行が `.zprofile`、`.zshenv`、`conf.d/*.zsh`、`.zshrc.d/*`、`/etc/zsh/zshrc` に書かれていることがあります。そうした行が残ると、毎回 `exec zsh` で該当プラグインが読み込まれ、重複提案 / Tab 競合が再発します。クリーンアップ後、インストーラーは今後これらのファイルを**走査**し、正確な `ファイル:行番号` で**警告**して手動清理を促します——これらのファイルは決して編集しません。

- **IP 帰属の検出は「表示する候補」を決めます — 中国大陸以外ではプリセットを隠しますが、direct は従来どおり実測します。** 外部 IP 帰属の検出結果は **中国大陸** / **中国大陸以外** / **検出失敗** の 3 種類です。中国大陸 / 検出失敗: 全候補を表示し、全候補（**direct を含む**）を速度測定し、常に 2 つの手動入力を提供します。帰属が不明な場合も何も隠さず、選択は利用者に委ねます。**中国大陸以外: プリセットミラーをすべて隠します**（ghproxy 系と gitclone.com は大陸専用の経路であり、この地域では誤誘導になるか遅くなるだけです）— direct だけが残りますが、direct は**従来どおり速度測定**され、2 つの手動入力（ミラー源・フルプロキシ）もそのまま残ります。メニュー番号は表示中の候補に合わせて詰められるため、選んでも何も起きない空き番は生じません。併せて **`kgithub.com` を削除**しました（ドメイン置換型ミラーは長期的に安定運用できません）。
- **手動入力は 2 種類: ミラー源とフルプロキシ。** 仕組みが本質的に異なるため、両方を用意しました。**ミラー源**は GitHub の URL を書き換えます（プレフィックスまたはドメイン置換）。**フルプロキシ**（システムプロキシに設定できる種類。例: `http://127.0.0.1:7890`、`socks5://127.0.0.1:1080`）は `HTTP_PROXY`/`HTTPS_PROXY` としてエクスポートされ、curl/git/wget の**すべて**のリクエストがそれを経由します（URL は書き換えません）。フルプロキシは採用前に到達性を検証し、非対話では `SMART_INSTALL_PROXY` で指定できます。
- **プリセットミラーに「中国大陸向け」を明記。** ghproxy.net / ghproxy.com / mirror.ghproxy.com / gitclone.com は中国大陸のネットワーク向けの存在なので、その旨をラベルに記載しました——中国大陸以外の利用者が、自分にとっては直連より遅い経路を選んでしまうのを防ぐためです。

### 修正
- **競合検出が古いバックアップを残骸として再報告しなくなりました。** zinit ディレクトリの走査は部分文字列一致のため、前回実行が残した `zsh-autocomplete.bak.<タイムスタンプ>` が、実際のプラグインが既に無いにもかかわらず「競合プラグインのディレクトリ」として再度報告され、削除を承諾するとそのバックアップまで削除されていました。バックアップディレクトリは（動作中のプラグインではないため）スキップされ、実際のプラグインディレクトリのみが報告されます。
- **読み取り専用スキャンが `fzf-tab` を競合として報告しなくなりました。** `fzf-tab` はサポート対象の代替リスト描画器（`SMART_MENU_LISTER=fzf-tab`）であり、競合として警告するのは公開済みの統合と矛盾していました。今後は zsh-smart-complete と機能が重複する `zsh-autocomplete` / `zsh-autosuggestions` のみを報告します。

## [v2.2.4] - 2026-09-16

### 追加
- **`SMART_MENU_LISTER=builtin|fzf-tab` — 明示的な 2 択です。** 2 つの補完一覧表示器は
  どちらも描画する権利があるため、「画面にポップアップが 2 つ出る」はどちらにも直せない
  問題です。どちらかが止まる必要があります。その選択が推測ではなく設定になりました:

  ```zsh
  smart-lister                   # 現在どちらが持っているか
  smart-lister fzf-tab           # こちらは描画せず、外部ピッカーに委ねる
  smart-lister builtin           # 取り戻す
  ```

  委ねるのは**一覧だけ**です。インラインのグレー提案は動き続け、`SMART_MENU` も
  変更されないため、`smart-lister builtin` は完全な取り消しになります。`builtin`、
  `smart`、`internal`、`native`、`built-in`、`on`、`yes`、`true`、`1` はこのプラグイン、
  `fzf-tab`、`fzf_tab`、`fzf`、`ftb`、`external`、`none`、`off`、`no`、`false`、`0` は
  委ねることを意味します（`off` は「こちらの一覧をオフ」であり「一覧なし」ではありません
  — それは `SMART_MENU=false` です）。未知の引数は**明示的に報告され非ゼロを返します**。
  以前はタイプミスがステータス表示に化けて「切り替えに成功した」ように見えていました。
  この設定は fzf-tab を**導入しません**。導入するのはインストーラの fzf-tab の質問で、
  対応する `SMART_MENU_LISTER` を管理ブロックに書き込むため、再実行しても回答が残ります。
- `smart-doctor` は一覧の持ち主を報告し、**読み込まれていない**ピッカーに委ねている場合に
  警告します。しかもこの警告は他のすべての判定より優先されます。「何も描画されない」は
  「一覧が 2 つ出る」より悪い状態だからです。最初の判定は「外来の補完フックがいくつ
  読み込まれているか」だけを見ており、まさにこの状況でそれが 0 になるため、一覧が
  出ようがないのに健全な shell だと報じていました。

### 変更

### 変更
- **単一列はオプトインになりました（既定はオフ）。** v2.2.3 では既定で有効にしていましたが、
  それは誤った既定でした。縦並びの一覧は候補を*生成*することでのみ描画でき、生成には実際の
  機能コストが伴います。
  - `_main_complete` を**迂回**するため、対象となる文脈では候補の**説明**、
    `list-colors` の色付け、グループ化、`matcher-list` が失われます。文書化されている
    あいまい一致は、生成された候補には**適用されません**。
  - 生成されるのはコマンド / 関数 / エイリアス / ビルトイン、ファイルパス、`cd` の最近
    ディレクトリのみです。それ以外（git サブコマンド、ssh ホスト、`--オプション`、
    `sudo …`）はネイティブのグリッドへフォールバックするため、**入力中にポップアップの形が
    変わります** — これは「2 つ目の一覧が出た」と誤解されがちです。
  - 端末幅を超える候補は切り詰められます（省略記号なし）。
  削除したものは何もありません。`SMART_MENU_SINGLE_COLUMN=true` は v2.2.3 が出荷した縦並び
  そのままです。インストーラは trade-off を質問文に明記して尋ねるようにしました（既定は**いいえ**）。
- `tests/e2e-tmux.sh` は **41** アサーションに増えました。シナリオ 10 は単一列の契約を
  順に検証します — 既定はグリッド (10a)、オプトインで 1 行 1 候補 (10b)、戻すとグリッド
  (10c)。10a と 10c があるからこそ 10b は空振りせず、失敗し得る検査になります。シナリオ 11
  は一覧表示器の切り替えを BEFORE → OFF → BACK ON で歩きます。この構造でしか中間の段階は
  意味を持ちません（戻す段階がなければ「0 行」は「単に補完しなくなった shell」でも満たされ
  ます）。インライン提案が引き渡し後も生きていることも検証します。
- v2.1.6 の A/B ベースラインは**再測定**したもので、**比例計算ではありません**:
  **20/41**。v2.1.6 にはポップアップもこの切り替えも存在しないため、10a/10b/10c は失敗し、
  シナリオ 11 も大半が失敗します。その領域でも 3 件は通りますが、**本物は 1 件だけ**です —
  引き渡し後もインライン提案が残ること (v2.1.6 にも `POSTDISPLAY` があります)。残る 2 件 —
  10b の「1 行に 2 つ入らない」と 11b の「引き渡し後に一覧を描かない」— は**空振り**です。
  そのビルドは一覧をまったく描かないからです。この 3 つを区別できることが、ベースラインを
  過去の分数から按分できない理由そのものです。
- `tests/test-menu.zsh`: 144 → 183。シナリオ 5 は**`smart-lister` CLI 自体を駆動**し、
  受け付けるすべての綴りについて、この一覧を符号化している 3 か所が一致することを検証します。
  それらは別々に書かれており（正規化関数、「認識されるか」の検査、CLI）、**すでにドリフト
  していました**: CLI は `on` を受け付けるのに認識検査は拒否するため `smart-lister on` が
  成功し、直後の `smart-lister` がそれと矛盾しました。また `no`/`false`/`0` は 2 つの補助
  関数には fzf-tab なのに CLI ではステータス出力に落ちていました。手書きの綴り一覧では
  これを捕まえられません — 前回まさにそうして漏れました。`smart-doctor` の新しい引き渡し
  判定はシナリオ 13b で固定され、**誤検知の側**（実際に読み込まれているピッカーは警報を
  消さなければならない）も含みます。
- `tests/test-config.zsh`: 39 → 43。新しいシナリオ 5 は、すでに 2 度ドリフトした値を固定します:
  5 つの README に書かれた単一列の既定値は `lib/config.zsh` の実際の既定値と一致しなければ
  なりません。
- 合計: **590 アサーション**（zsh 9 スイートで 532 + インストーラスイート 58）、e2e **41/41**。

## [v2.2.3] - 2026-09-16

### 修正
- **`Delete` キーがインライン候補の残像を残していた。** Backspace はラップ済みでしたが
  `Delete`（`ESC [ 3 ~`）は未対応で、履歴候補を呼び出した後に 1 文字削除すると直前の候補が
  画面に**凍り付いたまま**になっていました。`Delete` も Backspace と同様にラップし
  （emacs / viins 両キーマップ）、解除時に元へ戻します。
- **単一列ポップアップは実際にはグリッドで描画されていた。** パディング幅を
  `local cols=... pad="$cols"` と 1 行で書いていましたが、`local` コマンドの展開は
  どちらの代入よりも**前**に起きるため `pad` は空文字列になり、`${(r...)…}` は幅 0 へ
  パディングしていました。**コードは正しく見えるのに**一覧は多列のままでした。幅は
  変数名で渡すようにしています（`${(r.cols.. .)...}`）。

### 追加
- **単一列（縦並び）ライブポップアップ** — `SMART_MENU_SINGLE_COLUMN`、既定 `true`。
  入力中ポップアップを zsh 標準の多列グリッドではなく **1 行 1 候補**で描画します。候補は
  直接生成し（コマンド / 関数 / エイリアス、ファイルパス、`cd ` の最近ディレクトリ）、
  **表示文字列**をちょうど `COLUMNS` 幅に**パディングまたは切り詰め**ます。これにより
  数学的に 1 列しか入りません。`false` で標準グリッドに戻せます。
  - 「取得」ではなく「生成」な理由: `compadd` を関数で上書きすると、一部の zsh では
    候補が**まったく追加されなくなる**ため（実測）。ポップアップが黙って空になります。
  - 入力語は glob エンジンへ渡す前にエスケープします: `[` を入力するとパターン `[*` が
    でき、「不正なパターン」は nomatch ではなく生成器を**中断**させ、**キーストロークごとに**
    `bad pattern:` を出力していました。ただし先頭の `~/` は意図的にエスケープしません
    （そうしないと `~/…` の候補がすべて消えるため）。
  - フォールバックは維持: 生成器が扱えない文脈（git サブコマンド、ssh ホスト、オプション
    文字列）では通常の補完がそのまま動きます。
- **`smart-doctor`** — 「画面に 2 つ目の候補一覧を描けるのは誰か」の指紋をすべて出力します。
  `_main_complete` / `compadd` / `_complete` が zsh 標準の入口のままか、
  zsh-autocomplete / zsh-autosuggestions / fzf-tab / シンタックスハイライトが読み込まれて
  いるか、キーマップごとに `Tab` を誰が持つか、一覧を有効にし得る zstyle、そしてこの
  プラグイン自身の状態 — 最後に判定を 1 行。**読み取り専用**なので壊れかけの shell でも
  安全に実行できます。
- **インストーラのオプションが対話式に。** fzf-tab、Tab メニュー、単一列レイアウト、最近
  ディレクトリ、↑/↓ 履歴検索、zsh-vi-mode、候補の取得元をインストール時に 1 つずつ質問し、
  回答は生成される `~/.zshrc` の管理ブロックへ書き込まれます。`install.sh` と
  `install-entware.sh` の両方に対応。
  - このブロックは意図的に**プラグイン読み込みより前**に置きます。一部のオプション
    （特に `SMART_MENU_HISTORY_KEYS`）はプラグインがキーバインドを設定する**時点**で
    読まれるため、後から書いても黙って無視されます。
  - fzf-tab は既定で **オフ**（明示的なオプトイン）。有効にすると組み込みの選択メニューを
    強制的にオフにします — 両方を同時に有効にすることが、2 つの一覧表示器が同じ画面領域を
    奪い合う原因そのものです。
  - `NONINTERACTIVE=1` では文書化された既定値になります。

### 変更
- **`starship.toml` テンプレートを修正。** トップレベルの `format` が
  `[$user]($style)` になっていましたが、`($style)` は各 `[section]` の内部でのみ有効なため
  **ユーザー名が飲み込まれていました**。1 行目を `[$user] › $directory`、2 行目を
  `$character` にしています。
- インストーラの `.zshrc` テンプレートに管理されたオプション枠を追加し、末尾の
  「Optional:」コメント群を、質問対象**ではない**残りの調整項目に置き換えました。

### テスト
- `tests/test-menu.zsh`: 109 → 144。新しいシナリオ 13b（`smart-doctor`）と 14（単一列の候補
  生成、および単一列の不変条件: すべての表示文字列がちょうど `COLUMNS` 幅で、パディングが
  実際に挿入されるテキストへ影響しないこと）。
- `tests/test-zle.zsh`: 93 → 102。両キーマップの `Delete` バインド、解除時の返却、そして
  毎キーストロークで stdout へ何も出さないこと。
- `tests/test-config.zsh`: 37 → 39。`SMART_MENU_SINGLE_COLUMN` の既定値と上書き。
- **`tests/test-installer-options.sh`（新規、54 アサーション、bash）** — インストーラの
  オプション機構を抽出し、使い捨てファイルに対して駆動します。ソースからは見えない性質を
  固定します: 管理ブロックは**プラグイン読み込みより前**に置かれること、再実行が冪等で
  あること。さらに各インストーラ既定値を `lib/config.zsh` と突き合わせます — この照合が
  Tab メニューの既定値と実際の出荷値の不一致を検出しました。
- `tests/e2e-tmux.sh`: 29 → 33。**10** は 6 候補が 6 行を占め、すべて表示され、1 行に 2 つ
  入らないことを検証します。**10b** は `SMART_MENU_SINGLE_COLUMN=false` を有効にして
  グリッドが戻ることを検証し、10 が実際に失敗し得ることを示します。
- 合計: **538 アサーション**（zsh 9 スイートで 484 + インストーラスイート 54）、
  e2e は **33/33**。

## [v2.2.2] - 2026-09-16

### 修正
- **`Tab` の直後の `Enter` が行を実際に実行**するようになりました（単なる再描画では
  なく）。これまでは補完後、最初の `Enter` が accept-line ウィジェットに飲み込まれ
  （補完をまだ「進行中」と見なして表示を更新するだけ）、補完した `cd …` は**もう一度**
  `Enter` を押さないと実行されませんでした。ウィジェットは自身の状態をクリアしてから
  `zle .accept-line` を直接呼ぶようになりました。これは**以前から存在した不具合**で、
  v2.2.1 でも再現します。

### 追加
- **最近のディレクトリ候補**（`lib/engine/recent.zsh`、`SMART_RECENT_PATHS`、既定 `true`）。
  `cd` / `pushd` / `chdir` の引数を補完するとき、**実際に移動した**ディレクトリが候補に
  なります。さらに `cd ` の後の**空の単語**では即座に一覧表示されます——空の単語を一覧
  表示する価値がある唯一の場面です。補完器を `zstyle ':completion:*' completer` の先頭に
  差し込む実装なので、ユーザー自身の補完チェーンと共存し、`smart-recent off` /
  `smart-disable` でクリーンに除去されます。
- **読み取り専用。** データは zsh 自身の最近ディレクトリ・データベース（`cdr` と `~[1]`
  が使うものと同じ）で、プラグインは**決して書き込みません**。`SMART_RECENT_PATHS_MAX`
  （既定 `20`）が候補数の上限です。`smart-recent status` は現在利用可能な件数を報告します。
- **`smart-recent on|off|toggle|status`** 実行時コマンド。

### 変更
- **あいまい一致は文書化のみで、自前実装はしません。** ライブポップアップはユーザー自身の
  補完システムを走らせるので、`zstyle ':completion:*' matcher-list` はすでに自動で効きます
  ——本プラグインに**あいまい一致のコードは意図的にありません**。自前で足すと compsys と
  衝突するだけです。README の「オプション機能」節に設定すべき 1 行を示しています。

### テスト
- `tests/test-recent.zsh`（38 アサーション）：`cd` 引数位置の判定、データベース解析
  （空白 / 引用符 / XDG 位置 / 古いエントリ）、そして**回帰として固定**した点——補完器は
  `zstyle ':completion:*' completer` 経由で接続され、`$completer` 配列では**ない**
  （その変数は zsh に存在せず、以前のコードは「接続済みに見えて」実際は何もしていません
  でした）。
- `tests/e2e-tmux.sh`：**8b**（Tab の後の Enter が実行する）と **9**（`cd ` で最近の
  ディレクトリを一覧、Tab で完全なパスを補完）を追加。29 アサーション、**v2.1.6 では
  17/29**。

## [v2.2.1] - 2026-09-16

### 修正
- **ライブポップアップがキー入力を喰わなくなりました。** 以前は `LISTMAX=-1` をリスト
  取得呼び出しの前後でスコープ代入して zsh の「N 個すべて表示しますか（M 行）?」を抑止
  していましたが、これは ZLE の次回入力読み取りを壊します。`git status` と入力しても
  バッファには `gitstatus` しか入らず、shell は**誤ったコマンド**を実行していました。
  `LISTMAX` には一切触れないようにし、代わりに大きすぎる候補リストを描画しない
  （`SMART_MENU_MAX_MATCHES`、既定を「無制限」から `100` へ）ことで抑止します。短い候補
  リストと 1200 エントリのディレクトリの両方でクリーンだった唯一の設定です。
- `smart-menu status` と tick デバッグログが「下限未満」と「上限超過」を区別できるように
  なりました。以前は両方とも `below min` と記録され、調査方向を誤らせていました。

### 追加
- **`SMART_SUGGEST_STRATEGY`**（`history` | `history,completion`、
  zsh-autosuggestions と同名）。`completion` を加えると、履歴に無いパス・オプション・
  サブコマンドも灰色候補として提示できます（カーソル位置の単語の「曖昧でない前綴」を
  補完システムに問い合わせます）。
- **名前付きで再割り当て可能なウィジェット**：`smart-accept-suggestion`、
  `smart-accept-word`、`smart-execute-suggestion`、`smart-suggestion-toggle`。
- **`SMART_MENU_HISTORY_KEYS`**（既定 `false`）：`true` のとき、行が空でなければ ↑/↓ が
  履歴の前方一致検索（zsh-autocomplete の看板機能）になり、空行では通常の履歴移動に
  戻ります。

### テスト
- `tests/e2e-tmux.sh` に実端末での**バッファ完全性**検証を追加：1 文字ずつ入力して文字が
  欠落しないこと、そして**実際に実行されたコマンド**が入力どおりであることを確認します。
  以前のスイートは「リストが描画されたか」しか見ておらず、ポップアップがコマンドライン
  を壊している間も緑のままでした。

## [v2.2.0] - 2026-09-15

このプラグインが存在する**全理由**をついに実現したリリース：`zsh-autocomplete` +
`zsh-autosuggestions` の二つの半身を、単一プラグインの中でネイティブに提供します。

### Added
- **入力でポップアップする候補メニュー**（`lib/engine/menu.zsh`）：バッファを編集するたびに
  候補リストを計算・描画するため、補完は入力中に表示されます——`Tab` は不要。ユーザー自身の
  compsys 上に、プライベートな補完ウィジェット（`zle -C ... list-choices`）を使って実装。その
  completer は `compstate[nmatches]` を読み、リストを描画するか決定します。`compinit` には
  一切触れません。マスタースイッチは `SMART_MENU`、実行時は `smart-menu on|off|status`。
- **行内灰色サジェストと候補リストが共存。** リスト再描画と `POSTDISPLAY` は同じ画面領域を争う
  ため、順序を固定しました：先に灰色文字を描画し、その後にリストを実行、その後は再描画しません。
  単一の候補（`SMART_MENU_MIN_MATCHES`）はリストを破棄し、灰色文字に譲ります。
- **適応型スロットル**（`SMART_MENU_SLOW_MS` / `SMART_MENU_COOLDOWN_KEYS`、既定オフ）：閾値以上に
  時間がかかるリスト取得はクールダウンを招き、**継続的に**高負荷な補完のためのもの。既定オフの
  理由は、実測で唯一の尖峰が補完サブシステム読み込み時の一回限り ~180ms であり、それを絞ると
  セッション最初の `git <TAB>` でポップアップを失うだけで、節約はその一度の 180ms だからです。
- **`SMART_MENU_DEBUG=/path/to/log`**：各 tick の決定（ゲート拒否 / クールダウンによるスキップ /
  候補数 / 処理ミリ秒）を1行追記します。「ポップアップしなかった」は「候補が一つで灰色文字に譲った」
  と見分けがつかないため、このログが役立ちます。
- **`Alt+→` で単語一つを受け入れ**：行内サジェストの単語一つを受け入れ（その後残りを再サジェスト）。
  受け入れるものがないときは標準の `forward-word` にフォールバックします。

### Fixed
- **重大 — 新規セッションで右矢印が効かない**：`ESC [ C`（CSI）しかバインドしておらず、
  `TERM=xterm-256color` の `kcuf1` は `ESC O C`（*アプリケーションカーソルキー* 形式）であり、
  ZLE は端末をそのモードに切り替えるため。そのキーは zsh 標準の `forward-char` に渡り、サジェストは
  決して受け入れられず、灰色文字だけが表示される——典型的な「灰色文字は出るのに矢印が死んでいる」
  報告。すべての矢印エンコーディングをバインドし、シーケンス一覧は `terminfo` に CSI/SS3 の
  フォールバックを加えて構築します。
- **重大 — 同じ端末で `Alt+→` も死**：`Alt` は文字通り「`ESC` を送り、その後に矢印」なので、多重
  エンコーディング問題を継承します。旧版は `ESC [ 1 ; 3 C`（xterm）と `ESC ESC [ C` しかバインド
  しておらず、アプリケーションカーソルキーモードの端末が送る `ESC ESC O C` は未バインドで、リテラル
  な `^[` をバッファに落としていました。`Alt` 形式は今後「各矢印エンコーディングに `ESC` を前置」して
  **派生**させるため、二つが再び乖離することはありません。
- **バインド捕獲が生バイト列を誤解析**：`_smart_evt_binding`（`lib/event/zle.zsh`）と
  `_smart_current_binding`（`lib/engine/native.zsh`）は照会したシーケンスをテキスト一致で剥ごうと
  していましたが、`bindkey` はキーを常に `^X` 記法でエコーするため、生バイト列（例：terminfo の
  `kcuf1`）では一致に失敗し、*キー文字列*がウィジェット名として格納されていました。保存された原本が
  汚染され、アンバインド時にキーが二度と復元されませんでした。両パーサは今後 `bindkey` 出力の末尾
  フィールドを取ります。
- **ループ内の `local` が stdout に漏れる**（`lib/event/zle.zsh`）：zsh 5.9 は `local` 宣言を2回目
  以降に実行すると `var='<旧値>'` を印字し、これは2回以上反復するループ内に書かれた `local` で
  発生します。その出力は ZLE ウィジェット経路でそのままコマンドラインに届きます。すべてのループ変数を
  関数先頭で一度だけ宣言するようにし、`tests/test-zle.zsh` で bind/unbind サイクルの無音を
  アサートします。
- **`zmodload -F` の機能プレフィックス**：`EPOCHREALTIME` と `terminfo` は*パラメータ*なので
  `p:EPOCHREALTIME` / `p:terminfo` として要求する必要があります。拒否された `b:` 要求により両方が
  未定義のままとなり、スロットルと terminfo 由来の矢印シーケンスが静かに無効化されていました。回帰
  テストを追加。
- **連想配列の引用下付き**：`assoc["km|seq"]=x` は引用符をキー名の一部として格納するため、
  `assoc[km|seq]` では到達できません。キーは今後変数で組み立て、引用なし下付きで索引します
  （`lib/state.zsh` が既に守っていた規則と同じ）。
- **インストーラ**：*なぜ* `zsh-autocomplete` / `zsh-autosuggestions` を削除するか（両方の挙動は
  今やネイティブ）を説明するようになり、黙って削除しなくなりました。

## [v2.1.6] - 2026-09-15

### Fixed
- **重大 — 印字可能 ASCII 入力が飲み込まれる**：`_smart_evt_binding` が `bindkey -R "^@-^_"`
  範囲クエリから疑似ウィジェット `undefined-key` を捕獲し、印字可能キーをそこにディスパッチしたため、
  `zle undefined-key`（無操作）がすべての ASCII キー入力を食っていました。CJK/UTF-8（バイト >= 0x80、
  再バインド範囲外）は本来の `self-insert` から挿入され続けたため、「中国語は入るが英語が入らない」。
  捕獲は今後 `undefined-key` を未バインドに正規化して `self-insert` を使い、`_smart_evt_dispatch` も
  それを守り、`_smart_current_binding`（native.zsh）も同様に強化。`tests/test-zle.zsh` に回帰
  テストを追加。
- **キー捕獲の強化**：self-insert の原本ウィジェットは今後範囲プローブではなくハードコードします
  （範囲クエリは当社のバインド前は `undefined-key`、バインド後は当社の wrapper を返し、どちらも使える
  原本ではない）。捕獲は専用フラグ `_SMART_EVT_CAPTURED` で守り、単一の `ORIG_*` 変数の内容ではなく
  したため、古い・手動設定の `_SMART_EVT_ORIG_SELF_*` が全体捕獲をスキップ（それは静かにネイティブな
  Tab バインドや他の原本も落とす）することはなくなりました。捕獲プローブはさらに、いかなる `_smart_*` /
  `smart-*` ウィジェットの記録も拒否し、再捕獲が自らの wrapper にディスパッチし直すことはなくなりました。
- **インストーラ — 管理ブロックのマーカが書かれなかった**：`build_zsc_integration` が bash スクリプト
  内で `print -r --`（zsh 組込み）を使っていたため呼び出しは静かに失敗し、`# >>> zsh-smart-complete
  integration (managed) >>>` / `# <<< ... <<<` マーカ行が落とされていました。BEGIN マーカがないと
  `_upsert_zsc_block` は一致できず、再インストールのたびに重複ブロックを追加していました。今後は
  `printf '%s\n'` を使用。

### Added
- **任意の `zsh-vi-mode`（opt-in、既定 NO）**：vi キーバインドは確かに有用ですが、本プラグインは
  キーマップ全体を所有し、各行初期化で ZLE を再初期化するため、他プラグインのバインドを壊す典型的原因
  です——ゆえに暗黙にインストールすることはありません。選択時、インストーラはそれをクローンし、当社の
  ウィジェットを *前* に読み込み、`zvm_after_init` / `zvm_after_lazy_keybindings` 経由で再適用する
  ブロックを書きます。
- **インストーラがフロー内で fast-syntax-highlighting をインストール**（`_ensure_zinit_plugin
  zdharma-continuum/fast-syntax-highlighting`）、フルコンボとプラグイン両経路で、Zinit の初回起動
  自動クローンに依存しなくなりました。

### Changed
- **インストーラの .zshrc 戦略**：完全推奨 `.zshrc` テンプレートは、今回フルスタックを（再）インストール
  した場合（Phase 0/5 コンボ）のみ推奨。プラグインのみのインストールは今後、マーカ区切りの
  `zsh-smart-complete` ブロックのみを管理（べき等 upsert、ファイル全体を上書きしない）。

## [v2.1.5] - 2026-09-15

### Fixed
- **インストーラ — p10k/OMZ 除去器**：`_remove_p10k` / `_remove_omz` は今後 Zinit クローン済み
  プラグイン dir（`$ZINIT_PLUGINS_DIR` 下の `romkatzen---powerlevel10k`、`OMZ::ohmyzsh---ohmyzsh`）
  も削除するため、非 p10k/OMZ コンボを選ぶと次回起動で再読み込みされる古い残骸を完全に消去します。
  `.bak.*` 成果物はカスケードバックアップを避けるため直接削除。
- **インストーラ — `.zwc` バイトコード**：プラグイン更新経路（`git reset --hard`）は今後 Zinit コンパイル
  済み `*.zwc` キャッシュも削除するため、エンジン修正が更新後に実際に効く（以前は古いコンパイル済み
  コードが読み込まれていた）。
- **エンジン — グローバル漏洩**：`lib/engine/suggest.zsh` の `cmd_cwd` / `cmd_host` / `cmd_exit` は
  今後 `local` 宣言（以前は毎キー入力でグローバルに漏れていた）。
- **エンジン — 履歴上限**：`_SMART_CMDS` は今後 `SMART_SUGGEST_HISTORY_LIMIT`（既定 20000）に制限。
  `SMART_HISTORY_REBUILD_EVERY=0` で周期再構築を無効にした場合、最古エントリを破棄し bucket/assoc
  スロットを同期したままに。

## [v2.1.4] - 2026-09-12

### Fixed
- fzf インストールが静かにスキップされていた（非対話）。インストール進捗が2回表示（Phase 0/5 のち
  Phase 1-4）。`RAN_COMBO` ガードを追加し fzf プロンプトを対話式に。

## [v2.1.3] - 2026-09-11

### Fixed
- すべての y/N プロンプトで `read: -: invalid option` クラッシュ——`IFS=$'\n\t'` が `read $_args` を
  壊していた。 `read "$@"` に変更。

## [v2.1.2] - 2026-09-10

### Fixed
- インストーラプロンプトは今後、ユーザーが各ステップを確認するまでブロック。競合プラグインの `.bak.*`
  カスケード修正（主 dir は一度だけバックアップ）。古いプラグインは今後 `git fetch --depth 1` +
  `git reset --hard` で実際に更新。

## [v2.1.1] - 2026-09-09

### Added
- **zsh 再インストールプロンプト**：zsh が既にインストール済みなら、brew（macOS）または apt
  （Debian/Ubuntu）で再インストール/アップグレードするよう促す。
- **fast-syntax-highlighting**：`.zshrc` テンプレート内の `zinit light
  zdharma-continuum/fast-syntax-highlighting` で読み込み（Zinit は起動時自動クローン）。install.sh が
  直接管理しない。
- **i18n メッセージ**：zh-CN、zh-TW、ja、ko、en に `prompt.zsh_reinstall` を追加。

### Changed
- **Phase 0**：フルコンボインストールに zsh 再インストールロジックを含める。fast-syntax-highlighting は
  Zinit 経由で `.zshrc` テンプレートから読み込み。
- **Phase 1-3**：starship/atuin/zinit プロンプトの `SKIP_DEPS` ガードを復元。

### Fixed
- zsh 再インストールプロンプトは正しい brew/apt フォールバック論理を使用。

## [v2.1.0] - 2026-09-08

### Added
- **Phase 0/5**：完全推奨コンボインストール（zsh + fzf + starship + atuin + zinit + zsh-smart-complete）。
- **対話式バックアップ清掃**：競合プラグイン残骸（`.cache/p10k-*`、`.cache/zsh*`、
  `.local/state/zsh-autocomplete` 等）の清掃を促す。
- **fzf 自動インストール**：パッケージマネージャで不可なら GitHub からクローン。

### Changed
- `SKIP_DEPS!=1` かつ `NONINTERACTIVE!=1` のとき、インストーラは今後まず Phase 0 を実行。Phase 0 が
  スキップされた場合のみ Phase 1-3 がフォールバック。

## [v2.0.6] - 2026-08-26

### Fixed
- リリースワークフロー：tar/zip 前にファイルを stage し 'file changed' 競合を回避。

## [v2.0.5] - 2026-08-26

### Fixed
- `mirror.chosen` メッセージの不正な変数置換。
- 古い `.bak.*` 残骸を清掃。

## [v2.0.3] - 2026-08-26

### Fixed
- i18n：残るすべての中國語ステータスメッセージを翻訳。
- SSH 入力問題を修正。

## [v2.0.2] - 2026-08-26

### Fixed
- i18n：ミラー選択メニューが完全に国際化。

## [v2.0.1] - 2026-08-26

### Fixed
- 3 件のインストーラ問題を解決：i18n コンボメニュー、OMZ/p10k 既定は yes、starship.toml エスケープ。

## [v2.0.0] - 2026-08-25

### Added
- エンジンとインストーラの刷新。
- O(bucket) 接頭辞索引。
- de-subShell スコアリング。
- リアルタイム増分索引。
- Zsh 検出。
- OMZ/p10k コンボセレクタ。
- Entware インストーラ。
- ZLE 行エディタを乱していた毎キー入力の stdout 漏洩を停止。

## 以前のリリース

```
v0.1.0  ZLE フロントエンド、履歴索引、サジェストエンジン
   │
v0.1.3  決定論的ランク付け（減衰 + 頻度 + CWD ブースト）
   │
v0.2.0  Atuin SQLite バックエンド（host / exit / CWD 対応ランク付け）
   │
v1.0.0  GA — 安定公開 API、CI/CD、自動リリース
   │
v2.0.0  エンジンとインストーラの刷新 — O(bucket) 接頭辞索引、de-subShell
        スコアリング、リアルタイム増分索引、Zsh 検出、OMZ/p10k コンボ
        セレクタ、Entware インストーラ
   │
v2.1.0  Phase 0 フルコンボインストール（zsh + fzf + starship + atuin + zinit +
        zsh-smart-complete）、対話式バックアップ清掃
   │
v2.1.6  印字可能 ASCII 入力修正（undefined-key）、捕獲強化、
        fast-syntax-highlighting、コンボ対応 .zshrc、opt-in zsh-vi-mode
   │
v0.5.x  smart-shell-engine（Rust / Go、IPC 経由）（未来、opt-in）
   │
v2.0    Smart Shell — 完全な独立シェル（未来）
```
