# チェンジログ

このプロジェクトのすべての重要な変更はこのファイルに記録されます。

フォーマットは [Keep a Changelog](https://keepachangelog.com/) に基づき、このプロジェクトは
[セマンティックバージョニング](https://semver.org/lang/ja/) に準拠します。

## [v2.2.5] - 2026-09-19

### 追加
- **其它の起動ファイルにある競合ローダーの探査（読み取り専用の提醒）。** インストーラーの競合クリーンアップは `~/.zshrc` しか編集しません——これは意図的な設計で、使用者が他で管理している設定を変えないためです。しかし `zsh-autocomplete` / `zsh-autosuggestions` のローダー行が `.zprofile`、`.zshenv`、`conf.d/*.zsh`、`.zshrc.d/*`、`/etc/zsh/zshrc` に書かれていることがあります。そうした行が残ると、毎回 `exec zsh` で該当プラグインが読み込まれ、重複提案 / Tab 競合が再発します。クリーンアップ後、インストーラーは今後これらのファイルを**走査**し、正確な `ファイル:行番号` で**警告**して手動清理を促します——これらのファイルは決して編集しません。

- **IP 帰属の検出は「表示する候補」を決めます — 中国大陸以外ではプリセットを隠しますが、direct は従来どおり実測します。** 外部 IP 帰属の検出結果は **中国大陸** / **中国大陸以外** / **検出失敗** の 3 種類です。中国大陸 / 検出失敗: 全候補を表示し、全候補（**direct を含む**）を速度測定し、常に 2 つの手動入力を提供します。帰属が不明な場合も何も隠さず、選択は利用者に委ねます。**中国大陸以外: プリセットミラーをすべて隠します**（ghproxy 系と gitclone.com は大陸専用の経路であり、この地域では誤誘導になるか遅くなるだけです）— direct だけが残りますが、direct は**従来どおり速度測定**され、2 つの手動入力（ミラー源・フルプロキシ）もそのまま残ります。メニュー番号は表示中の候補に合わせて詰められるため、選んでも何も起きない空き番は生じません。併せて **`kgithub.com` を削除**しました（ドメイン置換型ミラーは長期的に安定運用できません）。
- **手動入力は 2 種類: ミラー源とフルプロキシ。** 仕組みが本質的に異なるため、両方を用意しました。**ミラー源**は GitHub の URL を書き換えます（プレフィックスまたはドメイン置換）。**フルプロキシ**（システムプロキシに設定できる種類。例: `http://127.0.0.1:7890`、`socks5://127.0.0.1:1080`）は `HTTP_PROXY`/`HTTPS_PROXY` としてエクスポートされ、curl/git/wget の**すべて**のリクエストがそれを経由します（URL は書き換えません）。フルプロキシは採用前に到達性を検証し、非対話では `SMART_INSTALL_PROXY` で指定できます。
- **プリセットミラーに「中国大陸向け」を明記。** ghproxy.net / ghproxy.com / mirror.ghproxy.com / gitclone.com は中国大陸のネットワーク向けの存在なので、その旨をラベルに記載しました——中国大陸以外の利用者が、自分にとっては直連より遅い経路を選んでしまうのを防ぐためです。

### 修正
- **競合検出が古いバックアップを残骸として再報告しなくなりました。** zinit ディレクトリの走査は部分文字列一致のため、前回実行が残した `zsh-autocomplete.bak.<タイムスタンプ>` が、実際のプラグインが既に無いにもかかわらず「競合プラグインのディレクトリ」として再度報告され、削除を承諾するとそのバックアップまで削除されていました。バックアップディレクトリは（動作中のプラグインではないため）スキップされ、実際のプラグインディレクトリのみが報告されます。
- **読み取り専用スキャンが `fzf-tab` を競合として報告しなくなりました。** `fzf-tab` はサポート対象の代替リスト描画器（`SMART_MENU_LISTER=fzf-tab`）であり、競合として警告するのは公開済みの統合と矛盾していました。今後は zsh-smart-complete と機能が重複する `zsh-autocomplete` / `zsh-autosuggestions` のみを報告します。

## [v2.2.4] - 2026-09-16

### 追加
- **`SMART_MENU_LISTER=builtin|fzf-tab` — 明示的な 2 択です。** 2 つの補完一覧表示器は
  どちらも描画する権利があるため、「画面にポップアップが 2 つ出る」はどちらにも直せない
  問題です。どちらかが止まる必要があります。その選択が推測ではなく設定になりました:

  ```zsh
  smart-lister                   # 現在どちらが持っているか
  smart-lister fzf-tab           # こちらは描画せず、外部ピッカーに委ねる
  smart-lister builtin           # 取り戻す
  ```

  委ねるのは**一覧だけ**です。インラインのグレー提案は動き続け、`SMART_MENU` も
  変更されないため、`smart-lister builtin` は完全な取り消しになります。`builtin`、
  `smart`、`internal`、`native`、`built-in`、`on`、`yes`、`true`、`1` はこのプラグイン、
  `fzf-tab`、`fzf_tab`、`fzf`、`ftb`、`external`、`none`、`off`、`no`、`false`、`0` は
  委ねることを意味します（`off` は「こちらの一覧をオフ」であり「一覧なし」ではありません
  — それは `SMART_MENU=false` です）。未知の引数は**明示的に報告され非ゼロを返します**。
  以前はタイプミスがステータス表示に化けて「切り替えに成功した」ように見えていました。
  この設定は fzf-tab を**導入しません**。導入するのはインストーラの fzf-tab の質問で、
  対応する `SMART_MENU_LISTER` を管理ブロックに書き込むため、再実行しても回答が残ります。
- `smart-doctor` は一覧の持ち主を報告し、**読み込まれていない**ピッカーに委ねている場合に
  警告します。しかもこの警告は他のすべての判定より優先されます。「何も描画されない」は
  「一覧が 2 つ出る」より悪い状態だからです。最初の判定は「外来の補完フックがいくつ
  読み込まれているか」だけを見ており、まさにこの状況でそれが 0 になるため、一覧が
  出ようがないのに健全な shell だと報じていました。

### 変更

### 変更
- **単一列はオプトインになりました（既定はオフ）。** v2.2.3 では既定で有効にしていましたが、
  それは誤った既定でした。縦並びの一覧は候補を*生成*することでのみ描画でき、生成には実際の
  機能コストが伴います。
  - `_main_complete` を**迂回**するため、対象となる文脈では候補の**説明**、
    `list-colors` の色付け、グループ化、`matcher-list` が失われます。文書化されている
    あいまい一致は、生成された候補には**適用されません**。
  - 生成されるのはコマンド / 関数 / エイリアス / ビルトイン、ファイルパス、`cd` の最近
    ディレクトリのみです。それ以外（git サブコマンド、ssh ホスト、`--オプション`、
    `sudo …`）はネイティブのグリッドへフォールバックするため、**入力中にポップアップの形が
    変わります** — これは「2 つ目の一覧が出た」と誤解されがちです。
  - 端末幅を超える候補は切り詰められます（省略記号なし）。
  削除したものは何もありません。`SMART_MENU_SINGLE_COLUMN=true` は v2.2.3 が出荷した縦並び
  そのままです。インストーラは trade-off を質問文に明記して尋ねるようにしました（既定は**いいえ**）。
- `tests/e2e-tmux.sh` は **41** アサーションに増えました。シナリオ 10 は単一列の契約を
  順に検証します — 既定はグリッド (10a)、オプトインで 1 行 1 候補 (10b)、戻すとグリッド
  (10c)。10a と 10c があるからこそ 10b は空振りせず、失敗し得る検査になります。シナリオ 11
  は一覧表示器の切り替えを BEFORE → OFF → BACK ON で歩きます。この構造でしか中間の段階は
  意味を持ちません（戻す段階がなければ「0 行」は「単に補完しなくなった shell」でも満たされ
  ます）。インライン提案が引き渡し後も生きていることも検証します。
- v2.1.6 の A/B ベースラインは**再測定**したもので、**比例計算ではありません**:
  **20/41**。v2.1.6 にはポップアップもこの切り替えも存在しないため、10a/10b/10c は失敗し、
  シナリオ 11 も大半が失敗します。その領域でも 3 件は通りますが、**本物は 1 件だけ**です —
  引き渡し後もインライン提案が残ること (v2.1.6 にも `POSTDISPLAY` があります)。残る 2 件 —
  10b の「1 行に 2 つ入らない」と 11b の「引き渡し後に一覧を描かない」— は**空振り**です。
  そのビルドは一覧をまったく描かないからです。この 3 つを区別できることが、ベースラインを
  過去の分数から按分できない理由そのものです。
- `tests/test-menu.zsh`: 144 → 183。シナリオ 5 は**`smart-lister` CLI 自体を駆動**し、
  受け付けるすべての綴りについて、この一覧を符号化している 3 か所が一致することを検証します。
  それらは別々に書かれており（正規化関数、「認識されるか」の検査、CLI）、**すでにドリフト
  していました**: CLI は `on` を受け付けるのに認識検査は拒否するため `smart-lister on` が
  成功し、直後の `smart-lister` がそれと矛盾しました。また `no`/`false`/`0` は 2 つの補助
  関数には fzf-tab なのに CLI ではステータス出力に落ちていました。手書きの綴り一覧では
  これを捕まえられません — 前回まさにそうして漏れました。`smart-doctor` の新しい引き渡し
  判定はシナリオ 13b で固定され、**誤検知の側**（実際に読み込まれているピッカーは警報を
  消さなければならない）も含みます。
- `tests/test-config.zsh`: 39 → 43。新しいシナリオ 5 は、すでに 2 度ドリフトした値を固定します:
  5 つの README に書かれた単一列の既定値は `lib/config.zsh` の実際の既定値と一致しなければ
  なりません。
- 合計: **590 アサーション**（zsh 9 スイートで 532 + インストーラスイート 58）、e2e **41/41**。

## [v2.2.3] - 2026-09-16

### 修正
- **`Delete` キーがインライン候補の残像を残していた。** Backspace はラップ済みでしたが
  `Delete`（`ESC [ 3 ~`）は未対応で、履歴候補を呼び出した後に 1 文字削除すると直前の候補が
  画面に**凍り付いたまま**になっていました。`Delete` も Backspace と同様にラップし
  （emacs / viins 両キーマップ）、解除時に元へ戻します。
- **単一列ポップアップは実際にはグリッドで描画されていた。** パディング幅を
  `local cols=... pad="$cols"` と 1 行で書いていましたが、`local` コマンドの展開は
  どちらの代入よりも**前**に起きるため `pad` は空文字列になり、`${(r...)…}` は幅 0 へ
  パディングしていました。**コードは正しく見えるのに**一覧は多列のままでした。幅は
  変数名で渡すようにしています（`${(r.cols.. .)...}`）。

### 追加
- **単一列（縦並び）ライブポップアップ** — `SMART_MENU_SINGLE_COLUMN`、既定 `true`。
  入力中ポップアップを zsh 標準の多列グリッドではなく **1 行 1 候補**で描画します。候補は
  直接生成し（コマンド / 関数 / エイリアス、ファイルパス、`cd ` の最近ディレクトリ）、
  **表示文字列**をちょうど `COLUMNS` 幅に**パディングまたは切り詰め**ます。これにより
  数学的に 1 列しか入りません。`false` で標準グリッドに戻せます。
  - 「取得」ではなく「生成」な理由: `compadd` を関数で上書きすると、一部の zsh では
    候補が**まったく追加されなくなる**ため（実測）。ポップアップが黙って空になります。
  - 入力語は glob エンジンへ渡す前にエスケープします: `[` を入力するとパターン `[*` が
    でき、「不正なパターン」は nomatch ではなく生成器を**中断**させ、**キーストロークごとに**
    `bad pattern:` を出力していました。ただし先頭の `~/` は意図的にエスケープしません
    （そうしないと `~/…` の候補がすべて消えるため）。
  - フォールバックは維持: 生成器が扱えない文脈（git サブコマンド、ssh ホスト、オプション
    文字列）では通常の補完がそのまま動きます。
- **`smart-doctor`** — 「画面に 2 つ目の候補一覧を描けるのは誰か」の指紋をすべて出力します。
  `_main_complete` / `compadd` / `_complete` が zsh 標準の入口のままか、
  zsh-autocomplete / zsh-autosuggestions / fzf-tab / シンタックスハイライトが読み込まれて
  いるか、キーマップごとに `Tab` を誰が持つか、一覧を有効にし得る zstyle、そしてこの
  プラグイン自身の状態 — 最後に判定を 1 行。**読み取り専用**なので壊れかけの shell でも
  安全に実行できます。
- **インストーラのオプションが対話式に。** fzf-tab、Tab メニュー、単一列レイアウト、最近
  ディレクトリ、↑/↓ 履歴検索、zsh-vi-mode、候補の取得元をインストール時に 1 つずつ質問し、
  回答は生成される `~/.zshrc` の管理ブロックへ書き込まれます。`install.sh` と
  `install-entware.sh` の両方に対応。
  - このブロックは意図的に**プラグイン読み込みより前**に置きます。一部のオプション
    （特に `SMART_MENU_HISTORY_KEYS`）はプラグインがキーバインドを設定する**時点**で
    読まれるため、後から書いても黙って無視されます。
  - fzf-tab は既定で **オフ**（明示的なオプトイン）。有効にすると組み込みの選択メニューを
    強制的にオフにします — 両方を同時に有効にすることが、2 つの一覧表示器が同じ画面領域を
    奪い合う原因そのものです。
  - `NONINTERACTIVE=1` では文書化された既定値になります。

### 変更
- **`starship.toml` テンプレートを修正。** トップレベルの `format` が
  `[$user]($style)` になっていましたが、`($style)` は各 `[section]` の内部でのみ有効なため
  **ユーザー名が飲み込まれていました**。1 行目を `[$user] › $directory`、2 行目を
  `$character` にしています。
- インストーラの `.zshrc` テンプレートに管理されたオプション枠を追加し、末尾の
  「Optional:」コメント群を、質問対象**ではない**残りの調整項目に置き換えました。

### テスト
- `tests/test-menu.zsh`: 109 → 144。新しいシナリオ 13b（`smart-doctor`）と 14（単一列の候補
  生成、および単一列の不変条件: すべての表示文字列がちょうど `COLUMNS` 幅で、パディングが
  実際に挿入されるテキストへ影響しないこと）。
- `tests/test-zle.zsh`: 93 → 102。両キーマップの `Delete` バインド、解除時の返却、そして
  毎キーストロークで stdout へ何も出さないこと。
- `tests/test-config.zsh`: 37 → 39。`SMART_MENU_SINGLE_COLUMN` の既定値と上書き。
- **`tests/test-installer-options.sh`（新規、54 アサーション、bash）** — インストーラの
  オプション機構を抽出し、使い捨てファイルに対して駆動します。ソースからは見えない性質を
  固定します: 管理ブロックは**プラグイン読み込みより前**に置かれること、再実行が冪等で
  あること。さらに各インストーラ既定値を `lib/config.zsh` と突き合わせます — この照合が
  Tab メニューの既定値と実際の出荷値の不一致を検出しました。
- `tests/e2e-tmux.sh`: 29 → 33。**10** は 6 候補が 6 行を占め、すべて表示され、1 行に 2 つ
  入らないことを検証します。**10b** は `SMART_MENU_SINGLE_COLUMN=false` を有効にして
  グリッドが戻ることを検証し、10 が実際に失敗し得ることを示します。
- 合計: **538 アサーション**（zsh 9 スイートで 484 + インストーラスイート 54）、
  e2e は **33/33**。

## [v2.2.2] - 2026-09-16

### 修正
- **`Tab` の直後の `Enter` が行を実際に実行**するようになりました（単なる再描画では
  なく）。これまでは補完後、最初の `Enter` が accept-line ウィジェットに飲み込まれ
  （補完をまだ「進行中」と見なして表示を更新するだけ）、補完した `cd …` は**もう一度**
  `Enter` を押さないと実行されませんでした。ウィジェットは自身の状態をクリアしてから
  `zle .accept-line` を直接呼ぶようになりました。これは**以前から存在した不具合**で、
  v2.2.1 でも再現します。

### 追加
- **最近のディレクトリ候補**（`lib/engine/recent.zsh`、`SMART_RECENT_PATHS`、既定 `true`）。
  `cd` / `pushd` / `chdir` の引数を補完するとき、**実際に移動した**ディレクトリが候補に
  なります。さらに `cd ` の後の**空の単語**では即座に一覧表示されます——空の単語を一覧
  表示する価値がある唯一の場面です。補完器を `zstyle ':completion:*' completer` の先頭に
  差し込む実装なので、ユーザー自身の補完チェーンと共存し、`smart-recent off` /
  `smart-disable` でクリーンに除去されます。
- **読み取り専用。** データは zsh 自身の最近ディレクトリ・データベース（`cdr` と `~[1]`
  が使うものと同じ）で、プラグインは**決して書き込みません**。`SMART_RECENT_PATHS_MAX`
  （既定 `20`）が候補数の上限です。`smart-recent status` は現在利用可能な件数を報告します。
- **`smart-recent on|off|toggle|status`** 実行時コマンド。

### 変更
- **あいまい一致は文書化のみで、自前実装はしません。** ライブポップアップはユーザー自身の
  補完システムを走らせるので、`zstyle ':completion:*' matcher-list` はすでに自動で効きます
  ——本プラグインに**あいまい一致のコードは意図的にありません**。自前で足すと compsys と
  衝突するだけです。README の「オプション機能」節に設定すべき 1 行を示しています。

### テスト
- `tests/test-recent.zsh`（38 アサーション）：`cd` 引数位置の判定、データベース解析
  （空白 / 引用符 / XDG 位置 / 古いエントリ）、そして**回帰として固定**した点——補完器は
  `zstyle ':completion:*' completer` 経由で接続され、`$completer` 配列では**ない**
  （その変数は zsh に存在せず、以前のコードは「接続済みに見えて」実際は何もしていません
  でした）。
- `tests/e2e-tmux.sh`：**8b**（Tab の後の Enter が実行する）と **9**（`cd ` で最近の
  ディレクトリを一覧、Tab で完全なパスを補完）を追加。29 アサーション、**v2.1.6 では
  17/29**。

## [v2.2.1] - 2026-09-16

### 修正
- **ライブポップアップがキー入力を喰わなくなりました。** 以前は `LISTMAX=-1` をリスト
  取得呼び出しの前後でスコープ代入して zsh の「N 個すべて表示しますか（M 行）?」を抑止
  していましたが、これは ZLE の次回入力読み取りを壊します。`git status` と入力しても
  バッファには `gitstatus` しか入らず、shell は**誤ったコマンド**を実行していました。
  `LISTMAX` には一切触れないようにし、代わりに大きすぎる候補リストを描画しない
  （`SMART_MENU_MAX_MATCHES`、既定を「無制限」から `100` へ）ことで抑止します。短い候補
  リストと 1200 エントリのディレクトリの両方でクリーンだった唯一の設定です。
- `smart-menu status` と tick デバッグログが「下限未満」と「上限超過」を区別できるように
  なりました。以前は両方とも `below min` と記録され、調査方向を誤らせていました。

### 追加
- **`SMART_SUGGEST_STRATEGY`**（`history` | `history,completion`、
  zsh-autosuggestions と同名）。`completion` を加えると、履歴に無いパス・オプション・
  サブコマンドも灰色候補として提示できます（カーソル位置の単語の「曖昧でない前綴」を
  補完システムに問い合わせます）。
- **名前付きで再割り当て可能なウィジェット**：`smart-accept-suggestion`、
  `smart-accept-word`、`smart-execute-suggestion`、`smart-suggestion-toggle`。
- **`SMART_MENU_HISTORY_KEYS`**（既定 `false`）：`true` のとき、行が空でなければ ↑/↓ が
  履歴の前方一致検索（zsh-autocomplete の看板機能）になり、空行では通常の履歴移動に
  戻ります。

### テスト
- `tests/e2e-tmux.sh` に実端末での**バッファ完全性**検証を追加：1 文字ずつ入力して文字が
  欠落しないこと、そして**実際に実行されたコマンド**が入力どおりであることを確認します。
  以前のスイートは「リストが描画されたか」しか見ておらず、ポップアップがコマンドライン
  を壊している間も緑のままでした。

## [v2.2.0] - 2026-09-15

このプラグインが存在する**全理由**をついに実現したリリース：`zsh-autocomplete` +
`zsh-autosuggestions` の二つの半身を、単一プラグインの中でネイティブに提供します。

### Added
- **入力でポップアップする候補メニュー**（`lib/engine/menu.zsh`）：バッファを編集するたびに
  候補リストを計算・描画するため、補完は入力中に表示されます——`Tab` は不要。ユーザー自身の
  compsys 上に、プライベートな補完ウィジェット（`zle -C ... list-choices`）を使って実装。その
  completer は `compstate[nmatches]` を読み、リストを描画するか決定します。`compinit` には
  一切触れません。マスタースイッチは `SMART_MENU`、実行時は `smart-menu on|off|status`。
- **行内灰色サジェストと候補リストが共存。** リスト再描画と `POSTDISPLAY` は同じ画面領域を争う
  ため、順序を固定しました：先に灰色文字を描画し、その後にリストを実行、その後は再描画しません。
  単一の候補（`SMART_MENU_MIN_MATCHES`）はリストを破棄し、灰色文字に譲ります。
- **適応型スロットル**（`SMART_MENU_SLOW_MS` / `SMART_MENU_COOLDOWN_KEYS`、既定オフ）：閾値以上に
  時間がかかるリスト取得はクールダウンを招き、**継続的に**高負荷な補完のためのもの。既定オフの
  理由は、実測で唯一の尖峰が補完サブシステム読み込み時の一回限り ~180ms であり、それを絞ると
  セッション最初の `git <TAB>` でポップアップを失うだけで、節約はその一度の 180ms だからです。
- **`SMART_MENU_DEBUG=/path/to/log`**：各 tick の決定（ゲート拒否 / クールダウンによるスキップ /
  候補数 / 処理ミリ秒）を1行追記します。「ポップアップしなかった」は「候補が一つで灰色文字に譲った」
  と見分けがつかないため、このログが役立ちます。
- **`Alt+→` で単語一つを受け入れ**：行内サジェストの単語一つを受け入れ（その後残りを再サジェスト）。
  受け入れるものがないときは標準の `forward-word` にフォールバックします。

### Fixed
- **重大 — 新規セッションで右矢印が効かない**：`ESC [ C`（CSI）しかバインドしておらず、
  `TERM=xterm-256color` の `kcuf1` は `ESC O C`（*アプリケーションカーソルキー* 形式）であり、
  ZLE は端末をそのモードに切り替えるため。そのキーは zsh 標準の `forward-char` に渡り、サジェストは
  決して受け入れられず、灰色文字だけが表示される——典型的な「灰色文字は出るのに矢印が死んでいる」
  報告。すべての矢印エンコーディングをバインドし、シーケンス一覧は `terminfo` に CSI/SS3 の
  フォールバックを加えて構築します。
- **重大 — 同じ端末で `Alt+→` も死**：`Alt` は文字通り「`ESC` を送り、その後に矢印」なので、多重
  エンコーディング問題を継承します。旧版は `ESC [ 1 ; 3 C`（xterm）と `ESC ESC [ C` しかバインド
  しておらず、アプリケーションカーソルキーモードの端末が送る `ESC ESC O C` は未バインドで、リテラル
  な `^[` をバッファに落としていました。`Alt` 形式は今後「各矢印エンコーディングに `ESC` を前置」して
  **派生**させるため、二つが再び乖離することはありません。
- **バインド捕獲が生バイト列を誤解析**：`_smart_evt_binding`（`lib/event/zle.zsh`）と
  `_smart_current_binding`（`lib/engine/native.zsh`）は照会したシーケンスをテキスト一致で剥ごうと
  していましたが、`bindkey` はキーを常に `^X` 記法でエコーするため、生バイト列（例：terminfo の
  `kcuf1`）では一致に失敗し、*キー文字列*がウィジェット名として格納されていました。保存された原本が
  汚染され、アンバインド時にキーが二度と復元されませんでした。両パーサは今後 `bindkey` 出力の末尾
  フィールドを取ります。
- **ループ内の `local` が stdout に漏れる**（`lib/event/zle.zsh`）：zsh 5.9 は `local` 宣言を2回目
  以降に実行すると `var='<旧値>'` を印字し、これは2回以上反復するループ内に書かれた `local` で
  発生します。その出力は ZLE ウィジェット経路でそのままコマンドラインに届きます。すべてのループ変数を
  関数先頭で一度だけ宣言するようにし、`tests/test-zle.zsh` で bind/unbind サイクルの無音を
  アサートします。
- **`zmodload -F` の機能プレフィックス**：`EPOCHREALTIME` と `terminfo` は*パラメータ*なので
  `p:EPOCHREALTIME` / `p:terminfo` として要求する必要があります。拒否された `b:` 要求により両方が
  未定義のままとなり、スロットルと terminfo 由来の矢印シーケンスが静かに無効化されていました。回帰
  テストを追加。
- **連想配列の引用下付き**：`assoc["km|seq"]=x` は引用符をキー名の一部として格納するため、
  `assoc[km|seq]` では到達できません。キーは今後変数で組み立て、引用なし下付きで索引します
  （`lib/state.zsh` が既に守っていた規則と同じ）。
- **インストーラ**：*なぜ* `zsh-autocomplete` / `zsh-autosuggestions` を削除するか（両方の挙動は
  今やネイティブ）を説明するようになり、黙って削除しなくなりました。

## [v2.1.6] - 2026-09-15

### Fixed
- **重大 — 印字可能 ASCII 入力が飲み込まれる**：`_smart_evt_binding` が `bindkey -R "^@-^_"`
  範囲クエリから疑似ウィジェット `undefined-key` を捕獲し、印字可能キーをそこにディスパッチしたため、
  `zle undefined-key`（無操作）がすべての ASCII キー入力を食っていました。CJK/UTF-8（バイト >= 0x80、
  再バインド範囲外）は本来の `self-insert` から挿入され続けたため、「中国語は入るが英語が入らない」。
  捕獲は今後 `undefined-key` を未バインドに正規化して `self-insert` を使い、`_smart_evt_dispatch` も
  それを守り、`_smart_current_binding`（native.zsh）も同様に強化。`tests/test-zle.zsh` に回帰
  テストを追加。
- **キー捕獲の強化**：self-insert の原本ウィジェットは今後範囲プローブではなくハードコードします
  （範囲クエリは当社のバインド前は `undefined-key`、バインド後は当社の wrapper を返し、どちらも使える
  原本ではない）。捕獲は専用フラグ `_SMART_EVT_CAPTURED` で守り、単一の `ORIG_*` 変数の内容ではなく
  したため、古い・手動設定の `_SMART_EVT_ORIG_SELF_*` が全体捕獲をスキップ（それは静かにネイティブな
  Tab バインドや他の原本も落とす）することはなくなりました。捕獲プローブはさらに、いかなる `_smart_*` /
  `smart-*` ウィジェットの記録も拒否し、再捕獲が自らの wrapper にディスパッチし直すことはなくなりました。
- **インストーラ — 管理ブロックのマーカが書かれなかった**：`build_zsc_integration` が bash スクリプト
  内で `print -r --`（zsh 組込み）を使っていたため呼び出しは静かに失敗し、`# >>> zsh-smart-complete
  integration (managed) >>>` / `# <<< ... <<<` マーカ行が落とされていました。BEGIN マーカがないと
  `_upsert_zsc_block` は一致できず、再インストールのたびに重複ブロックを追加していました。今後は
  `printf '%s\n'` を使用。

### Added
- **任意の `zsh-vi-mode`（opt-in、既定 NO）**：vi キーバインドは確かに有用ですが、本プラグインは
  キーマップ全体を所有し、各行初期化で ZLE を再初期化するため、他プラグインのバインドを壊す典型的原因
  です——ゆえに暗黙にインストールすることはありません。選択時、インストーラはそれをクローンし、当社の
  ウィジェットを *前* に読み込み、`zvm_after_init` / `zvm_after_lazy_keybindings` 経由で再適用する
  ブロックを書きます。
- **インストーラがフロー内で fast-syntax-highlighting をインストール**（`_ensure_zinit_plugin
  zdharma-continuum/fast-syntax-highlighting`）、フルコンボとプラグイン両経路で、Zinit の初回起動
  自動クローンに依存しなくなりました。

### Changed
- **インストーラの .zshrc 戦略**：完全推奨 `.zshrc` テンプレートは、今回フルスタックを（再）インストール
  した場合（Phase 0/5 コンボ）のみ推奨。プラグインのみのインストールは今後、マーカ区切りの
  `zsh-smart-complete` ブロックのみを管理（べき等 upsert、ファイル全体を上書きしない）。

## [v2.1.5] - 2026-09-15

### Fixed
- **インストーラ — p10k/OMZ 除去器**：`_remove_p10k` / `_remove_omz` は今後 Zinit クローン済み
  プラグイン dir（`$ZINIT_PLUGINS_DIR` 下の `romkatzen---powerlevel10k`、`OMZ::ohmyzsh---ohmyzsh`）
  も削除するため、非 p10k/OMZ コンボを選ぶと次回起動で再読み込みされる古い残骸を完全に消去します。
  `.bak.*` 成果物はカスケードバックアップを避けるため直接削除。
- **インストーラ — `.zwc` バイトコード**：プラグイン更新経路（`git reset --hard`）は今後 Zinit コンパイル
  済み `*.zwc` キャッシュも削除するため、エンジン修正が更新後に実際に効く（以前は古いコンパイル済み
  コードが読み込まれていた）。
- **エンジン — グローバル漏洩**：`lib/engine/suggest.zsh` の `cmd_cwd` / `cmd_host` / `cmd_exit` は
  今後 `local` 宣言（以前は毎キー入力でグローバルに漏れていた）。
- **エンジン — 履歴上限**：`_SMART_CMDS` は今後 `SMART_SUGGEST_HISTORY_LIMIT`（既定 20000）に制限。
  `SMART_HISTORY_REBUILD_EVERY=0` で周期再構築を無効にした場合、最古エントリを破棄し bucket/assoc
  スロットを同期したままに。

## [v2.1.4] - 2026-09-12

### Fixed
- fzf インストールが静かにスキップされていた（非対話）。インストール進捗が2回表示（Phase 0/5 のち
  Phase 1-4）。`RAN_COMBO` ガードを追加し fzf プロンプトを対話式に。

## [v2.1.3] - 2026-09-11

### Fixed
- すべての y/N プロンプトで `read: -: invalid option` クラッシュ——`IFS=$'\n\t'` が `read $_args` を
  壊していた。 `read "$@"` に変更。

## [v2.1.2] - 2026-09-10

### Fixed
- インストーラプロンプトは今後、ユーザーが各ステップを確認するまでブロック。競合プラグインの `.bak.*`
  カスケード修正（主 dir は一度だけバックアップ）。古いプラグインは今後 `git fetch --depth 1` +
  `git reset --hard` で実際に更新。

## [v2.1.1] - 2026-09-09

### Added
- **zsh 再インストールプロンプト**：zsh が既にインストール済みなら、brew（macOS）または apt
  （Debian/Ubuntu）で再インストール/アップグレードするよう促す。
- **fast-syntax-highlighting**：`.zshrc` テンプレート内の `zinit light
  zdharma-continuum/fast-syntax-highlighting` で読み込み（Zinit は起動時自動クローン）。install.sh が
  直接管理しない。
- **i18n メッセージ**：zh-CN、zh-TW、ja、ko、en に `prompt.zsh_reinstall` を追加。

### Changed
- **Phase 0**：フルコンボインストールに zsh 再インストールロジックを含める。fast-syntax-highlighting は
  Zinit 経由で `.zshrc` テンプレートから読み込み。
- **Phase 1-3**：starship/atuin/zinit プロンプトの `SKIP_DEPS` ガードを復元。

### Fixed
- zsh 再インストールプロンプトは正しい brew/apt フォールバック論理を使用。

## [v2.1.0] - 2026-09-08

### Added
- **Phase 0/5**：完全推奨コンボインストール（zsh + fzf + starship + atuin + zinit + zsh-smart-complete）。
- **対話式バックアップ清掃**：競合プラグイン残骸（`.cache/p10k-*`、`.cache/zsh*`、
  `.local/state/zsh-autocomplete` 等）の清掃を促す。
- **fzf 自動インストール**：パッケージマネージャで不可なら GitHub からクローン。

### Changed
- `SKIP_DEPS!=1` かつ `NONINTERACTIVE!=1` のとき、インストーラは今後まず Phase 0 を実行。Phase 0 が
  スキップされた場合のみ Phase 1-3 がフォールバック。

## [v2.0.6] - 2026-08-26

### Fixed
- リリースワークフロー：tar/zip 前にファイルを stage し 'file changed' 競合を回避。

## [v2.0.5] - 2026-08-26

### Fixed
- `mirror.chosen` メッセージの不正な変数置換。
- 古い `.bak.*` 残骸を清掃。

## [v2.0.3] - 2026-08-26

### Fixed
- i18n：残るすべての中國語ステータスメッセージを翻訳。
- SSH 入力問題を修正。

## [v2.0.2] - 2026-08-26

### Fixed
- i18n：ミラー選択メニューが完全に国際化。

## [v2.0.1] - 2026-08-26

### Fixed
- 3 件のインストーラ問題を解決：i18n コンボメニュー、OMZ/p10k 既定は yes、starship.toml エスケープ。

## [v2.0.0] - 2026-08-25

### Added
- エンジンとインストーラの刷新。
- O(bucket) 接頭辞索引。
- de-subShell スコアリング。
- リアルタイム増分索引。
- Zsh 検出。
- OMZ/p10k コンボセレクタ。
- Entware インストーラ。
- ZLE 行エディタを乱していた毎キー入力の stdout 漏洩を停止。

## 以前のリリース

```
v0.1.0  ZLE フロントエンド、履歴索引、サジェストエンジン
   │
v0.1.3  決定論的ランク付け（減衰 + 頻度 + CWD ブースト）
   │
v0.2.0  Atuin SQLite バックエンド（host / exit / CWD 対応ランク付け）
   │
v1.0.0  GA — 安定公開 API、CI/CD、自動リリース
   │
v2.0.0  エンジンとインストーラの刷新 — O(bucket) 接頭辞索引、de-subShell
        スコアリング、リアルタイム増分索引、Zsh 検出、OMZ/p10k コンボ
        セレクタ、Entware インストーラ
   │
v2.1.0  Phase 0 フルコンボインストール（zsh + fzf + starship + atuin + zinit +
        zsh-smart-complete）、対話式バックアップ清掃
   │
v2.1.6  印字可能 ASCII 入力修正（undefined-key）、捕獲強化、
        fast-syntax-highlighting、コンボ対応 .zshrc、opt-in zsh-vi-mode
   │
v0.5.x  smart-shell-engine（Rust / Go、IPC 経由）（未来、opt-in）
   │
v2.0    Smart Shell — 完全な独立シェル（未来）
```
