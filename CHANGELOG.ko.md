# 변경 내역

이 프로젝트의 모든 중요한 변경 사항은 이 파일에 기록됩니다.

형식은 [Keep a Changelog](https://keepachangelog.com/)을 따르며, 이 프로젝트는
[의미론적 버전](https://semver.org/lang/ko/)을 준수합니다.

## [v2.4.0] - 2026-09-26

### 수정
- **설치 스크립트가 마지막에 빈 설정 파일을 만들어 놓고, 이후에는 그 수리를 거부할 수
  있었다.** `_install_user_settings` 의 최후 수단 분기——저장소 템플릿을 가져오지 못할
  때만 거친다——는 시작용 내용을 `print -r --` 로 출력하고 있었다. `print` 는 zsh 내장
  명령이지 `PATH` 위의 프로그램이 아니므로, bash 스크립트에서 이걸 부르면 우연히
  `print` 라는 프로그램을 가진 기계를 빼면 exit 127 이 된다. 그 시점에는 이미 `.zshrc`
  재작성이 끝난 뒤이고, 남아버린 0 바이트 파일은 「없는 때에만 만든다」는 가드 조건까지
  만족시키므로, 그 다음 실행부터는 고칠 게 있다는 사실 자체를 눈치채지 못한다. 두 설치
  스크립트 모두 `printf '%s\n'` 로 바꿨다. 이 문제는 코드를 읽어서가 아니라 설치
  스크립트를 실제로 돌려서 발견됐다.

- **설치 프로그램에 준 미러 URL 이 데이터가 아니라 코드로 취급되었습니다.**
  `SMART_INSTALL_GH_MIRROR` 와 "직접 미러 입력" 에 대한 답은 둘 다 `curl` / `wget`
  URL 을 재작성하는 다운로드 shim 에 들어갑니다. 그런데 그 shim 은 **따옴표 없는
  heredoc** 으로 생성되어 값이 스크립트 본문에 그대로 끼워졌습니다. 그래서
  `https://x.example/'; touch ./pwn; '` 는 URL 이 아니라 세 개의 명령이 되었고,
  설치 실행 시간에 그대로 실행되었습니다. 게다가 `_run_remote_script` 는 설치
  프로그램이 고르지 않은 미러에서 받아온 것까지 실행하므로, 같은 문자열이 실행될
  코드의 출처까지 정했습니다. 이제 shim 은 네 줄짜리 설정 파일을 `read -r` 로 읽기만
  하고 문자열 삽입은 하지 않습니다. 두 설치 프로그램 모두 값을 쓰기 전에
  `_mirror_prefix_ok` 로 검사합니다: `https://` 호스트, 선택적 포트, 선택적 경로 —
  나머지는 모두 거부합니다. 평문 `http://` 를 거부하는 것은 **의도된** 것입니다.
  받아온 스크립트는 실행되므로, 통신 방식은 오타 하나로 격하될 성질의 것이 아닙니다.
  테스트 27 장은 악의적인 값으로 shim 을 만들어, 페이로드가 설정 파일 밖에 남지
  않는지, 실제 다운로더에는 원래 URL 이 그대로 전달되는지, 아무것도 생성되지
  않는지를 검증합니다.
- **설치가 중간에 실패하면 손상된 상태가 그대로 남았습니다.** 두 설치 프로그램 모두
  `.zshrc` 를 *순서대로* 씁니다 — 먼저 options 블록, 다음 loader 블록. 개별 쓰기는
  모두 rename 이라 파일이 잘리지는 않지만, 두 쓰기 사이에서 중단되면 구문은 맞고
  셸은 뜨지만 플러그인은 불러오지 않는 `.zshrc` 가 남습니다. 옆에 있는
  `.bak.<timestamp>` 는 아무것도 되돌리지 못합니다. 그것을 가리키는 게 아무것도
  없고, 만든 실행은 이미 끝났기 때문입니다. 이제 `_guard_config_write` 가 첫 쓰기
  전에 스냅샷을 뜨고, `EXIT` trap 이 종료 코드가 0 이 아니면 그것을 되돌립니다 —
  설치 프로그램이 새로 만든 파일을 지우는 경우도 있는데, "원래 `.zshrc` 가 없었다"
  역시 되돌릴 가치가 있는 상태이기 때문입니다. 순서의 마지막 쓰기가 끝난 뒤에는
  `_release_config_write_guard` 가 trap 을 해제합니다. 그보다 나중의, 상관없는
  단계가 실패했다고 사용자가 요청한 설정을 취소하지 못하게 하기 위해서입니다.
  25 장에는 guard 없이 돌린 대조 실행이 남아 있고, 그것 때문에 이 장의 나머지
  단언들이 의미를 가집니다.
- **템플릿이 내용을 내놓지 않으면 작동하던 설정을 빈 파일로 덮어썼습니다.**
  `apply_template` 의 모든 분기가 `$dest` 에 곧바로 리다이렉트했기 때문에, 알아볼
  수 없는 접두사 — 또는 본문이 비어 돌아온 다운로드 — 는 사용자의 파일을 먼저
  잘라낸 뒤에 실패했습니다. 이제는 반드시 `<dest>.zsc-new` 에 쓰고, 그 파일이
  비어 있으면 현재 언어로 오류를 내며 쓰기를 거부한 뒤, `mv -f` 한 번(쓰려는 파일과
  같은 디렉터리에서)으로 제자리에 놓습니다.
- **쓸 수 있고 읽히는데 어디서도 선언되지 않은 global 이 둘 있었다.** 모든 모듈이
  `no_warn_create_global`을 켜 두기 때문에, 선언되지 않은 이름에 대한 첫 대입은 조용히
  global을 만든다. 그래서 `_SMART_PROBE_SUFFIX_RET`는 "그 값으로 되돌아오는 함수"의
  부작용으로만, `_SMART_RH_MARKER`는 최상위 대입으로만 존재했다. 이제 둘 모두 정의된
  장소에서 `typeset -g`로 밝힌다. 둘 다 동작 중인 셸에서 문제를 일으키지 않았고,
  "이 모듈이 어떤 상태를 소유하는가"를 선언을 읽어 답하는 종류의 검사에만 보이지 않았
  다 — 그 검사가 찾아낸 결함이었다.
- **상태 컨테이너 자신의 key 목록이 네 곳에서 틀렸다.** 미래의 포트를 위해
  `_SMART_STATE`를 기록해 둔 그 주석 블록은, 코드에서 `history.frequency`인 하위
  맵을 `history.freq`라 적어 두었다. 아무도 쓴 적 없는 `history.first_char`를
  올려 두었다(`_SMART_CMDS_FIRST`의 첫 글자 버킷이 그 계획을 이미 오래전에 대체했다).
  아무도 읽거나 쓰지 않는 `last_err`를 약속했다. 그리고 모든 재구성에 쓰이는 세 하위
  맵——`history.cwd`, `history.host`, `history.exit`——은 빠졌다.
  `suggestion.source`의 값 목록에도 네이티브 메뉴 경로가 실제로 저장하는
  `completion`이 빠져 있었다. 규격이라고 밝힌 문서는 조용히 썩으므로, 아래 검사기는
  `lib/`에 쓰는 쪽도 읽는 쪽도 없는 key를 이제 거부한다.
- **단일 열 팝업에서 입력한 단어가 패턴으로 쓰이고 있었다.** 명령 목록은
  `${(M)_sc_out:#${w}*}`로 걸러 냈는데, 이는 입력 텍스트를 그대로 glob 에 넘기는 일이다 —
  여기서 특별한 문자는 `[ ] * ?` 만이 아니다. `EXTENDED_GLOB`(인기 프레임워크 여럿이 켜는
  것) 아래에서는 `#`, `^`, `(`, `<->`도 패턴 문법이므로, 이스케이프하지 않은 접두어는 입력한
  텍스트가 접두어가 아닌 명령까지 매칭할 수 있었다(실측: `zsc#`가 `zsc_probe_alpha`에
  매칭됐다). 아니면 그냥 잘못된 패턴이 되거나. 명령 쪽 분기는 경로 쪽 분기가 이미 하고 있던
  대로 `${(b)…}`로 단어를 이스케이프하게 됐다. 이제 `git#`는 명령 `git#`을 뜻한다: 리터럴
  매칭은 없고 아무것도 생성되지 않으며, 그 단어는 이 모드가 다루지 않는 다른 모든 맥락처럼
  네이티브 그리드로 폴백한다. `tests/test-menu.zsh`의 14h 절은 이스케이프된 접두어가
  아무것도 매칭하지 않는 것과 같은 입력에서 stderr 가 비어 있음을 단언하고, 평범한 접두어는
  여전히 매칭된다는 것도 단언한다 — 마지막이 있어서 앞의 두 개는 헛돌지 않는다.
- **같은 초의 두 실행이 `~/.zshrc` 백업을 서로 덮어쓸 수 있었다.** 제거는 관리 블록을 걷어내기
  전에 설정을 `.zshrc.bak.<epoch>` 에 복사하고, 설치도 같은 꼴의 이름으로 복사한다 — 그래서 그
  이름은 두 실행이 실제로 한 초 이상 떨어져 있을 때만 유일하다. 그런데 「설치하고 방금 그 설치를
  되돌리기」는 바로 한 초를 공유할 수 있는 조합이다. 그러면 `cp -p` 는 이미 그곳에 있던 사본을
  조용히 대체하고, 대체되는 쪽은 제거가 지키겠다고 약속한, 손으로 쓴 설정의 유일한 사본이다.
  이제 이름이 차 있으면 사본은 `.bak.<epoch>-1`, `-2` … 으로 비켜 간다. 이건
  `tests/test-installer-sandbox.sh` 에서 동전 뒤집기로 드러났다: 8 장은 아무것도 하지 않는 제거
  뒤의 백업을 세어 정확히 하나를 기대했는데, 그건 설치와 제거의 사본이 같은 초에 부딪혀 서로의
  증거를 지운 경우에만 성립했다. 이제는 직전 실행이 남긴 묶음을 기준으로 비교한다. `find | head -1`
  을 「제거의 백업」으로 읽던 단언은 `sort | tail -1` 로 바뀌었다 — 설치 쪽도 설정을 백업한
  실행에서는 `find` 가 먼저 내미는 파일이 설치본이 *시드* 파일에 대해 만든 백업이고, 그 안에는
  관리 블록이 아예 없다. 같은 동전, 반대 면.
### 추가
- **`tests/test-installer-sandbox.sh` — 설치 스크립트가 처음으로 “실행”됐다.**
  `tests/` 에 있던 것들은 `lib/*.zsh` 모듈을 source 하거나 `install.sh` 에서 함수 하나를
  뽑아 픽스처로 구동하는 식이라, 설치 스크립트 전체를 돌려본 테스트는 하나도 없었다.
  함수들을 이어 붙이는 코드도, 그 실행 순서도, 이 파일이 **bash** 로 실행된다는 사실도
  검증 바깥이었다. 새 스위트는 `env -i` (환경변수를 하나도 이어받지 않음) 아래에서 두
  설치 스크립트를 돌리고, `HOME`·`ZDOTDIR`·`XDG_*` 를 방금 만든 `mktemp -d` 안으로
  잡으며, 스텁 디렉터리를 `PATH` 맨 앞에 두므로 네트워크도 패키지 관리자도 닿을 수 없다.
  `curl` / `wget` / `git` / `brew` / `opkg` 호출은 실행되지 않고 기록만 남고, 그 기록
  자체도 단언 대상이다(미러 속도 테스트는 반드시 돌아야 하고, `SMART_INSTALL_GH_MIRROR`는
  clone URL 을 바꾼 게 보여야 하며, Entware 스크립트는 `opkg` 가드에서 `.zshrc` 에 손대지
  않고 멈춰야 한다). 12 개 설치 경로——기본값, combo 프리셋 4 종, UI 언어 5 종, 네트워크
  분기 2 종——각각에 대해 종료 코드, `unbound variable` 유무, `command not found` 유무,
  그리고 설치가 성공했다면 남아야 할 것(`.zshrc` 의 관리 블록 두 개, 비어 있지 않은
  `settings.zsh`)을 확인한다. 소스를 읽는 grep 은 heredoc 본문 먼저 빈 줄로 바꾼다. 이
  스크립트들은 zsh 설정을 *안에 품고* 있고 그 줄들은 애초에 zsh 가 맞기 때문이다.
  딸려 있는 두 단언은 이 필터가 템플릿 안의 zsh 는 숨기면서 실행되는 zsh 는 숨기지
  않는다는 것, 그리고 줄 수를 그대로 둔다는 것(보고되는 줄 번호가 여전히 파일을
  가리켜야 한다)을 확인한다. 단언 55 개.
- **`tests/test-installer-shared.sh` — 두 설치 스크립트 사이의 약속을 전제 대신
  검사로 바꿨다.** 한 파일이 `msg()` 에 요구하는 message key 마다 그 파일의 `_msg()` 가
  실제로 정의하고 있는지 확인한다(모르는 key 는 key 자체를 출력해 주므로, 이 종류의
  문제를 잡아내는 단언은 이것뿐이다). probe 파일 하나로 이 lint 가 헛돌지 않는다는 것도
  확인한다. 그리고 heredoc 을 아는 파서로 두 파일을 함수 단위로 비교한다: 맞아 떨어지는
  것들은 공유 core 로 보내고, 안 맞아 떨어지는 것은 이유를 적어 놓은 목록에 반드시
  있어야 한다. 목록에 없는 새 차이가 나면 스위트가 실패하고, 목록에 있는 항목이 조용히
  다시 동일해져도 실패한다. 단언 13 개.

- **`--uninstall`, 그리고 argv 가 전달되지 않는 `curl | bash` 를 위한
  `SMART_UNINSTALL=1` 을 추가했습니다.** 지우는 것은 설치 프로그램이 쓴 것뿐입니다 —
  `.zshrc` 의 관리 블록 두 개, 플러그인 체크아웃, `settings.zsh`, 그리고 우리가 만든
  `zsc-settings` 심볼릭 링크. fzf, starship, atuin, zinit 같은 패키지는 설치된
  채로, `starship.toml` 과 모든 `.bak.*` 파일은 그대로 둡니다. 이것들은 셸 나머지
  부분과 공유되며 우리보다 먼저 있었을 가능성이 크기 때문입니다. `.zshrc` 를 편집하기
  *전에* 타임스탬프가 붙은 백업을 먼저 만들고, 그것이 불가능하면 편집을 거부합니다.
  헤드리스 실행에서는 `SMART_UNINSTALL=1` 자체가 확인의 뜻을 가지고, 터미널에서는
  묻습니다. 제거 과정은 `_upsert_options_block` 의 역이며, 마커 이전의 오래된
  형태 — `zinit ice` 와 `zinit light imonior/zsh-smart-complete` — 는 **쌍으로**
  제거합니다. 혼자 남게 된 `zinit ice` 는 다음에 로드되는 플러그인의 설정을 조용히
  바꿔버리기 때문입니다. 우리 블록 앞에만 존재 이유가 있던 빈 줄은 함께 사라지고,
  사용자 설정 두 덩어리 사이의 빈 줄은 제자리에 남습니다.
  `tests/test-installer-sandbox.sh` 는 이제 설치 프로그램이 절대 쓰지 않을 내용을
  심어 놓은 홈으로 설치한 다음 제거하고, 설정이 **바이트 단위로** 원래대로
  돌아오는지를 검증합니다.
- **쓰이지 않는 코드를 잡아내는 lint 두 개를 추가했습니다.**이런 부패는 소스만
  보고는 보이지 않기 때문입니다. 하나는 설치 프로그램이 정의해 놓고 한 번도 호출하지
  않는 함수를, 다른 하나는 카탈로그에 정의돼 있지만 어느 설치 프로그램도 호출하지
  않는 메시지 키를 보고합니다 — 두 카탈로그는 설계상 공통 상위 집합이므로 이 검사는
  파일을 넘어야 합니다. 각 lint 에는 검사기 자체를 겨눈 프로브(살아 있는 함수 vs
  죽은 함수, heredoc 안의 `read` 밖의 `read`, 키 접두어 vs 전체 단어)와, 카탈로그
  순회가 실제로 수백 개 키를 찾았다는 단언이 붙어 있습니다. "아무것도 못 찾아서"
  통과하는 lint 는 무의미하니까요. 드러난 것: `_cleanup_old_baks` (91 줄, 찾아낸
  모든 `~/.zshrc.bak.*` 를 묻지 않고 지우려 했고, 설정되지 않은 배열을 참조해
  `set -u` 에서 도중에 종료했으므로 애초에 도달할 수도 없었다), `_backup_if_normal`,
  `install_or_upgrade_pkg`, 그리고 두 스크립트에 호출자가 없는 카탈로그 항목 34 개.
- **`tools/check-module-globals.sh`: 새로운 global은 이유를 직접 밝혀야 한다.**
  `lib/state.zsh`는 변하는 모듈 간 실행 데이터가 전부 여기 있고 코드베이스 다른
  곳에 독립된 `_smart_thing=foo`는 없다고 주장했다. 그 밖에 있는 66개의 global이 그
  문장에 반대했다. 결함은 숫자가 아니라 그 주석이었다. 그것을 믿은 독자에게는 실제로
  있는 상태를 찾을 방법이 없었고, global을 하나 더 추가한 저자에게는 아무 저항이
  없었다. 이제 규칙은 실제로 지키는 경계를 적는다 — 실행 *상태*는 컨테이너로, 나머지는
  그 파일 끝의 등록부로 이유와 함께 — 그리고 검사기는 두 방향을 모두 본다: 등록되지
  않은 global은 실패, 아무것도 매칭하지 못하는 등록 줄도 실패(이것이 바로 이름 바꾸기가
  더 이상 존재하지 않는 코드베이스를 등록부가 계속 설명하게 두는 통로다), 이유가 없는
  항목은 실패, 기록돼 있는데 쓰는 쪽도 읽는 쪽도 없는 key도 실패다. 이 선언들을 컨테이너로
  옮기는 일은 거부하기 전에 측정했다 — 컨테이너 첨자는 읽기당 1 µs보다 한참 아래, 단순한
  global은 약 0.2 µs. 따라서 성능은 처음부터 논거가 아니었고 수명이었다. keymap별 바인딩
  스냅샷은 셸마다 사용자 자신의 keymap에서 한 번 떠 오는 것이고, `lib/state.zsh`는
  `lib/event/zle.zsh` *이전에* source 된다. `_smart_state_reset`이 비우는 컨테이너로
  옮기면 사용자 설정만이 되돌려 줄 수 있는 데이터를 지우게 된다. 결과 매개변수 자리
  (`_SMART_BUILD_*`와 fork 없이 되돌리는 `_scan` 반환값)는 애초에 호출 한 번의 수명이다.
  `tests/test-module-globals.sh`는 84개 단정을 추가했고, 대부분 트리보다 검사기를
  겨눈다 — 아무도 동작을 증명할 수 없는 lint는 초록만 보고하는 lint이기 때문이다.
  `tests/run-all.sh`가 자동 발견하므로 CI에서도 실행된다.
### 변경
- **두 설치 스크립트 모두 `set -u` 로 돈다**(전에는 `set -eo pipefail`). 이런 분기들을
  실제로 훑는 것이 있어야 이 플래그가 정직하므로, 위 스위트가 그용으로 존재한다. 거기에
  도달하는 데 수정 세개가 필요했다: `MIRROR_TIMES[…]` 읽기 11 곳을 `:-999` 로 기본값을
  주고, 빈 배열이 정당하게 나올 수 있는 전개 2 곳을 `${arr[@]+"${arr[@]}"}` 로 바꿨다——
  macOS 에 여전히 들어있는 bash 3.2 는 `-u` 아래에서 `"${empty_array[@]}"` 를 바로
  중단시키지만 `"$@"` 는 안전하다.
- **키 바인딩 probe 가 하나가 됐다.** `lib/event/zle.zsh` 는
  `lib/engine/native.zsh` 의 `_smart_current_binding` 을 바이트 단위로 똑같은 사본을
  안고 있었고, 과거 회귀 때문에 존재하는 가드 두개(`bindkey` 출력의 *마지막* 필드를 읽는다,
  키 쪽 문자열로 일치시키지 않는다, `undefined-key` 는 미바인딩으로 본다, 우리 자신의 widget 을
  「원본 바인딩」으로 받아주지 않는다)까지 함께 복제돼 있었다. 그래서 한쪽을 고치면 다른 쪽은
  조용히 깨진 채로 남았다. 중복은 삭제했고 남은 쪽 함수에 이것이 유일한 바인딩 probe 라는
  것을 적어 두었다. `_smart_event_capture_originals` 은 셸 하나당 이걸 20 번 안팎 부른다.
- **메뉴 시계가 타이핑 중에 fork 하지 않는다.** `_smart_menu_now_ms` 는 값을 명령
  치환으로 돌려줬기 때문에 목록 갱신 한 번에 두 번의 읽기가 **각 349 µs** 였다. 이제 lister 가
  쓴 것과 같은 worker/wrapper 분리를 쓴다: 뜨거운 경로의 `_smart_menu_now_ms_scan` 은
  `_SMART_MENU_NOW_MS` 에 값을 올리고, 출력하는 래퍼는 `smart-doctor`·상태 출력·테스트를
  위해 남긴다. 실측 **각 9.9 µs**.
- **두 설치 스크립트가 공유하는 부분은 한 번만 쓴다.** `install.sh` 와
  `install-entware.sh` 는 함수 약 80 개를 중복하고 있었다. 둘 다
  `curl -fsSL … | bash` 로 단독 실행되는 스크립트로 안내하므로 실행 시간에 옆 파일을
  source 할 수 없다. 그 제약은 진짜지만, 그동안은 복사로 충족됐고 사본들은 이미
  어긋나 있었다——같은 문자열이 두 파일에 서로 다른 message key 로 들어 있었고,
  `msg()` 는 모르는 key 에 대해 key 자체를 돌려주므로 오타는 실패하지 않고 사용자의
  터미널에 `mirror.unavailable_line` 이라고 표시된다. 이제 환경과 무관한 함수 30 개
  (미러, curl 래퍼, 다운로드 shim, 언어 메뉴, 옵션 블록)는 `lib/install/core.sh` 가
  갖고 있고, `tools/build-installers.sh` 가 그것을 마커 줄 사이에 그대로 집어넣는다. 사용자에게는
  빌드 단계가 없으므로 산출물은 계속 커밋해 둔다. CI 는
  `bash tools/build-installers.sh --check` 를 돌리므로, 블록이 출처와 다르면 출하된 결함이
  아니라 빌드 실패가 된다. 여전히 중복으로 남은 함수 21 개는 의도적으로 환경 의존
  (opkg 경로, `$HOME` 대 `$ZDOTDIR`, message 사전)이며 이유와 함께
  `tests/test-installer-shared.sh` 에 목록으로 적혀 있다——목록에 없는 새 차이가 나면
  스위트가 실패하고, 목록의 항목이 조용히 다시 동일해져도 실패한다. 이 재구성은 선언이
  아니라 실측으로 검증했다: body 해시와 함께한 함수 집합, 그리고 두 파일의 최상위
  문장 순서 모두 그대로다. 범위에 포함돼 실제로 바뀐 텍스트는 한 건이다: entware 의
  `_tty_read` 주석을 install.sh 의 표현에 맞췄다.

- **네 개의 BEGIN/END 마커 상수는 이제 공유 코어에 정의됩니다**, 각 스크립트가
  처음으로 블록을 쓰는 지점이 아니라. 제거(언설치)는 아무것도 설치되기 전에 이
  블록들을 알아봐야 하는데, 이 대입문이 4000 줄 스크립트 뒷부분에 남아 있는 한
  그럴 수 없었습니다.
- **`lib/state.zsh`는 읽는 방법과 누가 비워도 되는지를 적는다.** 접근자는 `print`를
  하므로 호출은 명령어 치환과 fork 하나(약 0.4 ms)를 의미한다 — 그런데 파일은 모듈들에게
  "이 도우미를 통해 읽고 써라"라고 말했고, 모든 뜨거운 경로는 이미 배열을 직접 첨자하고
  있었다. 정반대이고, 실제로 따라질 조언이었다. 문서에는 이제 실제 규칙(쓰기는 헬퍼로,
  키 입력 경로로는 직접 첨자)과 여기서 측정해 낸 읽기 값이 오른다: 단순 global 약 0.2 µs,
  리터럴 key 약 0.6 µs, key를 변수에 담으면 약 0.8 µs, `$()` 경로는 약 400 µs.
  `_smart_state_reset`의 제목도 실제 호출자를 적는다(`smart-reindex`가 아니다: 그 명령은
  `_smart_history_rebuild`로 인덱스를 재구성하고, 그쪽은 곧 다시 채울 history 슬롯만
  의도적으로 지운다. 컨테이너 전체를 비우면 `enabled`, `buffer`, 래핑된 widget이 변경
  감지에 쓰는 커서 기록까지 사라진다).
- **단일 열 팝업은 입력을 이어가도 모양이 바뀌지 않는다.** `SMART_MENU_SINGLE_COLUMN`은
  명령을 *행의 첫 단어*에만 생성하고 최근 디렉터리를 `cd ` 직후의 *빈 단어*에만 목록했다 —
  그래서 `ls -la | gr`은 파일 시스템에서 후보를 그리는 반면, 아무것도 치지 않은 프롬프트의
  `gr`은 명령을 그렸다. `cd Dow`는 현재 디렉터리의 파일을 답하는데 옆의 네이티브 그리드는
  이미 `~/Documents`를 내밀고 있었다. 기능의 이 두 쪽은 네이티브 완성기와 일치했고, 벗어나
  있던 것은 세로 목록 쪽이었으며 사용자가 보게 되는 증상은 커서 아래에서 팝업의 출처가
  바뀌는 일이었다. 이제는 셸이 아직 명령을 고르는 자리면 어디든 명령 위치로 본다 — `|`,
  `&&`, `;` 뒤, 그리고 대입문이거나 명령을 감싸는 명령어(`sudo`, `command`, `env`,
  `time`, `nice` 등)가 늘어선 뒤의 단어. 최근 디렉터리도 입력한 접두어에 답한다(전체 경로와
  마지막 성분 둘 다). 다만 전과 같이 `cd`/`pushd`에서만, `SMART_RECENT_PATHS`가 켜져 있을
  때에만, 그 단어가 아직 명시적 경로가 아닐 때에만 — 네이티브 `cd` 완성기와 같은 네 가지
  조건이고, 그러기에 두 목록이 같은 모양을 유지할 수 있다. 여전히 그리드로 폴백하는 것들은
  추측이 아니라 적혀 있다: git 하위 명령, ssh 호스트, 옵션 문자열, `~user`, 그리고 명령을
  이미 받아 버린 감싸는 명령어 뒤(`sudo git`). 하나의 한계는 감추지 않고 단언했다:
  `git log|gr`은 명령 위치가 *아니다*. 팝업의 단어는 공백으로 가르므로 현재 단어는
  `log|gr`이기 때문이다 — 한 기능을 위해 "단어"를 다시 정의하면 팝업 전체가 움직이므로 이
  판정은 보수적으로 유지했다. 이 판정은 키 입력마다 돌기 때문에 `tests/test-perf.zsh`에 절이
  하나 늘었다(단어 두 개의 짧은 줄에서 회당 약 19 µs, 긴 줄에 대한 스케일링 상한도 함께) —
  행동 게이트와 나란히. 이 주장에는 화면 쪽 증거도 생겼다: `tests/e2e-tmux.sh` 의 10b+ 장은
  여섯 개의 명령 이름을 행 처음과 `|` 직후에서 각각 입력하고, 두 경우 모두 여섯 줄이며 한 줄에
  두 개 이상의 후보가 없음을 요구한다. 정말 실패할 수 있는 단언이다 — 예전 판정으로 되돌리면 뒤의
  입력은 후보를 하나도 생성하지 못해 화면에 목록이 없거나 그리드가 되기 때문이다. 기본값은
  여전히 `false` 이다.
## [v2.3.0] - 2026-09-24

### 수정
- **히스토리가 크면 인덱스를 다시 만드는 동안 셸이 멈췄다.** 두 곳에서 결과를 한 조각씩 쌓고 있었고, 쌓을 때마다 지금까지 모은 것을 통째로 복사했다: 저장용 목록 문자열（명령마다 `joined+=$'\n'$v`）과 첫 글자 기준 버킷 표——후자는 요소 단위로 쌓으면 추가할 때도, 첨자로 다시 읽을 때도 zsh 배열의 연결 리스트를 타고 가야 한다. 8000 명령 히스토리에서 인덱스 재구축에 **693 ms**, 크기를 4배로 하면 시간이 **14배**였다. 두 곳 모두 C 레벨 순회 한 번으로 이어 붙이는 방식으로 바꿨다（문자열은 `${(F)…}`, 버킷마다 `${(M@)array:#${key}*}` 접두사 투영）. 같은 8000 명령 재구축은 이제 **30 ms**, 4배 크기는 4배 시간이다. 범위 위쪽인 20000 명령에서는 "전혀 못 느끼는 재구축"과 "프롬프트가 몇 초 멈추는 재구축"의 차이였다. 이 투영은 키를 리터럴로 취급해야 한다: `*`로 시작하는 명령의 버킷에는 그것만 들어 있어야 하며, glob 특수 문자마다 테스트로 검증했다.
- **Enter 를 누를 때마다 인덱스 전체를 두 번 훑었다.** 지금까지 '최근 사용'은 *순위*로 저장됐다——최근 순으로 정렬된 배열에서 각 명령의 위치——그래서 하나를 최신으로 올리면 나머지의 도장을 모두 다시 찍어야 하고, 소속 판정도 먼저 배열에서 찾아야 했다. 둘 다 Enter 마다 O(인덱스 크기)였다: 8000 인덱스에서 같은 명령을 50 번 실행하는 데 **9.8 초**, 주기 재구축을 끄면*새* 명령마다 상한을 지키려고 배열 전체를 복사했다（100 개에 **19.3 초**）. 이제 `history.recency`는 단조 증가하는 **마지막 사용 tick**을 담고 엔진은 나이로 비교한다（`history.tick` 에서 저장된 tick 을 뺀 값）. 그래서 Enter 는 슬롯 하나만 찍고 다른 것은 건드리지 않는다. `_SMART_CMDS`는 추가 전용 소속 집합이 되고, 최근 순서는 버킷에 두며, 제거는 `shift` 한 번이다. 같은 50 번 Enter 는 이제 **32 ms**, 상한에서 새 명령 100 개는 **12 ms**.
- **키 입력마다 메모리에 이미 있는 값을 읽으려고 하위 셸을 fork 했다.** 표시 계층과 이벤트 계층이 `$(_smart_state_get …)`, `$(_smart_menu_lister)`로 상태를 물었다——팝업과 다시 그리기 경로에 아홉 곳, 각각 이 기계에서 실측 약 0.41 ms 인 `fork()` 한 번이며, 그중 하나（`_smart_menu_word`）는 `$()` 안에서 ZLE 를 움직이지 못하게 한 원인이기도 했다. 뜨거운 경로는 이제 `_SMART_STATE`와 `_SMART_MENU_LISTER_RET`를 직접 첨자로 읽는다. 출력용 래퍼는 상태 출력, `smart-doctor`, 테스트를 위해 남겼고, 어느 쪽이 어느 쪽인지 양쪽에 주석으로 적었다.
- **Entware 설치에서 설정 블록이 아무 데나——아니면 아예——들어갔다.** `install-entware.sh`는 `ZSC_BLOCK_BEGIN` / `ZSC_BLOCK_END`를 정의하지 않았는데 `_upsert_options_block`은 그 이름을 기준으로 비교한다: 변수가 비면 `grep -qF "" "$file"`이 **모든** 파일과 일치하므로 "관리되는 로더 블록 위에 삽입" 분기가 항상 타서, 실제로는 "첫 빈 줄 앞에 삽입"으로 동작했다. 방금 만든 `.zshrc`에는 빈 줄이 없어서 설정은 조용히 빠졌고, PC 와 공유한 dotfile 에서는 플러그인 로드 앞（바인드 키 설정 중에 읽히는 항목이 있다）이 아닌 임의 위치에 들어갔다. 두 마커는 `install.sh`가 쓰는 것과 같은 값으로 정의했고, 두 설치 스크립트의 마커 상수 네 개를 검사하며, `_upsert_options_block`의 모든 분기를 픽스처로 돌렸다（마커도 빈 줄도 없는 파일 포함）.

### 추가
- **`tests/test-state.zsh`, `tests/test-display.zsh`, `tests/test-native.zsh`.** 세 모듈에 직접 테스트가 없었다: 상태 컨테이너（다른 모든 모듈이 여기를 통해 쓰고, 그 버킷 미러를 바로 방금 다시 썼다）, 표시 계층（`region_highlight` 항목 하나가 틀리면 고스트는 색을 잃고, 하나가 새면 구문 강조기는 자기 항목을 잃는다——모두 제안 단위 테스트로는 안 보인다）, 그리고 네이티브 Tab 브리지（계약 전체가 "Tab 은 플러그인 로드 전과 같은 일을 한다"이며 `compinit`를 한 번도 실행하지 않은 사용자도 포함된다）. 새 스위트는 일반 `zsh` 스크립트에서 돈다: ``$bindkey`, 키맵, `$widgets` 가 다 동작하고, 터미널이 필요한 `zle` 만 넘겨받은 위젯 이름을 기록하는 mock 함수로 대체한다.
- **`tests/test-perf.zsh` —— 계산 복잡도 트리프와이어.** 위에서 고친 버그는 전부 동작 테스트에 보이지 않았다: 출력은 완벽히 정확하고 증상은 초 수뿐이었다. 입력 중에 도는 세 연산을 실측하고（실제 히스토리와 닮은 합성 인덱스, 동사 13 종이라 버킷 처리와 배열 전체 처리를 구별할 수 있다）각각에 벽시계 상한과 4배 크기 스케일링 비를 단언하고, 측정 값을 출력해서 실패 전에 드리프트가 보이게 한다. 상한은 실측의 5~8 배라 CI 잡음에 걸리지 않는다. 이 수정 이전 트리에서는 실패한다: `FAIL l_set history.cmds at 8000 commands 692.6 ms (cap 500 ms)`, `FAIL 50 upserts over a 8000 command index 9772.7 ms (cap 500 ms)`.
- **CI 를 세 잡으로 나눴다（각각 무엇을 증명할 수 있는지로 구분）.** `test` 가 게이트（macOS + Linux）. `zsh-versions` 는 같은 스위트를 `ubuntu:focal` / `ubuntu:jammy` / `debian:bookworm`, 즉 패키지 zsh 5.7.1 / 5.8.1 / 5.9 에서 돌린다——이 저장소에는 "zsh 5.9 에서 실측"으로 적힌 주장이 많은데 OS 당 빌드 하나로는 검증되지 않았기 때문이다. `e2e-advisory` 는 tmux 종단 간 스위트를 **비차단**으로 돌린다: 렌더링된 화면을 볼 수 있는 유일한 스위트이면서, 실제 터미널을 실제 대기 시간으로 구동하는 유일한 스위트이기도 하니, 좋은 변경을 실패로 만들 자격을 얻기 전에 안정적인지 여부를 먼저 측정해야 한다. release 워크플로에는 `release`가 의존하는 `gate` 잡이 생겼다——스위트 그린, `VERSION`과 태그 일치, CHANGELOG 에 해당 섹션 존재. 사람들이 실제로 설치하는 태그에 대한 발행은 그전까지 무조건이었다.

### 변경
- **`./tests/run-all.sh` 가 하나만 외우면 되는 명령이 됐다.** `tests/test-*.zsh`（zsh）와 `tests/test-*.sh`（bash）를 자동 발견하고, 부분 문자열 필터와 `-v` 를 받으며, 각 스위트의 단언 수를 더한다. CI 잡은 열두 개 파일을 손으로 나열하고 있었고, 바로 옆에 "아무도 추가하지 않은 새 파일은 조용히 한 번도 안 돌는다"는 주석이 있었다——위 두 모듈이 무시험이 된 이유가 그거다.
- **README 에서 손으로 셌던 테스트 요약을 없앴다.** "12 개 테스트 파일, 804 개 단언" 은 스위트를 추가한 첫 커밋에서 이미 틀렸다. 이제 그 줄은 실측 합계를 출력하는 러너를 가리킨다. 다섯 언어 모두 갱신했다.
- **중복된 코드와 주석을 정리했다** —— 같은 위젯을 두 번 등록하던 두 번째 `zle -N _smart_native_complete`, reverse-Tab 위젯에 그대로 옮겨 적혔던 "목록 표시 이후의 `region_highlight`" 이유 설명, 세 곳에 복사됐으면서 자기 설명은 두 개였던 lister 표기 메모 포함. 허용 표기는 이제 한 번만 선언하고, 모든 사본이 맞다는 것은 `smart-lister`를 구동하는 테스트가 확인한다.
- **`smart-doctor`가 셸에 함수를 남기지 않는다.** 접두사 탐색용 함수를 그 명령의 최상위에 정의해서, 일반 `smart-doctor` 한 번 돌리고 나면 함수가 세션에 남았다. 이제 플러그인 자체 접두사로 이름 짓고 명령이 끝날 때 `unfunction` 한다.

## [v2.2.11] - 2026-09-23

### 추가
- **설치기가 플러그인 옆에 로컬 설정 스크립트（`zsc-settings`）를 두어, `~/.zshrc` 를 건드리지 않고도 모든 설정을 조정할 수 있게 됨.** 설치기는 `${XDG_CONFIG_HOME:-$HOME/.config}/zsh-smart-complete/settings.zsh` 를 만들고 `bin/zsc-settings` 를 `~/.local/bin/zsc-settings` 에 심볼릭 링크한다. 플러그인은 내장 기본값보다 **먼저** 이 파일을 source 하므로, 그곳에 적은 `KEY='VALUE'` 행은 기본값을 덮어쓴다. `zsc-settings` 는 `wizard` / `list` / `get KEY` / `set KEY VALUE` / `edit` / `reset [KEY]` / `path` / `init` 를 제공하며, `set` 은 설정 형（bool / int / enum / path）에 맞춰 값을 검증하고 잘못된 입력을 거부한다. 변경은 zsh 재시작 후 적용된다. 다른 파일을 쓰려면 `SMART_USER_CONFIG` 로 그 파일을 가리키면 된다.

### 변경
- **라이브 완성 팝업을 autocomplete 수준으로: 경로와 단일 매치도 표시.** 경로(예 `/u`, `~/l`, `cd /usr/`)는 **마지막 `/` 이후 첫 글자**부터 후보를 표시하고, 맨 `/`(또는 끝이 `/`)는 해당 디렉터리를 즉시 나열합니다 — v2.2.9 규칙은 세그먼트 2글자를 기다려 경로 완성이 "죽은 것"처럼 보였습니다. 단일 매치도 1줄 팝업을 그리며(인라인 회색 글자도 계속 표시)이며 회색 글자만 되지 않습니다. 경로 외 단어는 2글자 문턱을 유지(`git s`는 `git st`까지 표시 안 함). 예전 동작으로 돌아가려면 설정 파일에 `SMART_MENU_MIN_MATCHES=2`를 쓰면 됩니다.


## [v2.2.10] - 2026-09-22

### 수정
- **한 글자만 입력해도 줄이 제출된 것처럼 보였습니다. 키 입력마다 터미널이 한 줄씩 스크롤되었기 때문입니다.** 표시 계층의 마지막은 `zle -R "" ""`("새 region_highlight를 강제로 다시 그린다")였고, 이 형식은 zsh가 프롬프트 영역을 처음부터 다시 만들게 합니다. pty에 실제로 기록된 바이트로 측정하면, 2줄 프롬프트에서 키 입력 한 번은 **96 바이트이며 그 안에 `\r\r\n` 이 포함됩니다** — 실제 개행 뒤에 커서 위로 이동과 줄 전체 지우기가 따라옵니다 — 반면**이것을 빼면 33 바이트**입니다. 터미널에서 커서는 대부분의 시간 동안 마지막 행에 있습니다(어떤 명령 출력이든 커서를 그곳으로 밀어냅니다). 마지막 행에서의 개행은 화면 전체를 한 줄 스크롤시키고, 뒤따르는 커서 위로 이동은 이미 밀린 내용 위에 내려앉기 때문에 입력 중인 줄의 글자가 섞입니다(`lls-la /etc/`). 그래서 모든 키가 이미 제출된 것처럼, 그리고 그 아래에 새 프롬프트가 찍힌 것처럼 보였습니다. 고스트**와** 팝업을 모두 끈 상태에서도 같은 키 입력이 32 바이트를 썼고, 순정 zsh는 1 바이트입니다. 이 호출은 존재하던 두 곳 — `_smart_display_update` 와 `→` 수락 경로 — 에서 제거했습니다. `region_highlight` 배열만 쓰면 충분합니다: widget에서 돌아올 때 ZLE가 한 번 다시 그리고, 그 재그리기가 고스트와 그 색을 함께 실어 나릅니다(검증됨: 여전히 `ESC[38;5;110m`).
- **정말로 필요한 단 한 번의 재그리기는 이제 키 입력마다가 아니라 필요할 때만 나갑니다.** 이 일괄 재그리기를 제거하자 그것이 부수적으로 하던 일이 드러났습니다. 더 긴 접두사를 위해 그린 후보 행을 거두는 일입니다. 목록을 계속 그리는 동안에는 그 영역을 zsh가 스스로 유지합니다 — `git st` 를 `git sta` 로 좁히면 실제로 3행이 2행이 됩니다 — 하지만 **마지막** 전이, 즉 접두사가 후보 하나로 좁혀져 목록이 억제되는 tick은 zsh의 목록 코드에 도달하지 않으므로, 옛 행이 새 고스트 아래에 그대로 남아 있었습니다. 이제 플러그인은 "자기가 그린 행이 아직 화면에 있는지"를 추적하고, **한 번의 좁힘당 정확히 한 번**만 지우기용 재그리기를 요청합니다(신규 `_smart_menu_forget_rows`). 의도적으로 아무것도 그리지 않는 tick(게이트가 닫힌 경우와 스로틀로 건너뛴 경우)도 포함합니다. 실측: 이 전이에서는 깨끗하게 지워지고(`git stat` 아래 잔여 행 0, 화면은 수정 전과 동일), 일반 키 입력은 여전히 33 바이트, 두 기능을 모두 끄면 1 바이트 — 순정 zsh와 정확히 동일합니다.

### 추가
- **`tests/test-repaint.zsh` — 키 입력당 zsh가 기록하는 바이트 수에 대한 회귀 테스트.** tmux 엔드투엔드는 이런 종류의 버그를 **구조적으로 전혀 볼 수 없습니다**. tmux는 "개행 + 커서 위로" 쌍을 되돌리기 때문에, 키 입력마다 재그리기를 하든 하지 않든 화면**과** 스크롤백이 **모두** 동일합니다. 이 테스트는 `zsh/zpty` 로 실제 `zsh -i` 를 띄우고 `zsh/mapfile` 로 원시 바이트 스트림을 읽으며(명령 치환을 쓰면 검증 대상인 바로 그 끝 개행이 제거됩니다), 2줄 프롬프트에서 하나의 불변식을 검증합니다: **키 입력 한 번은 한 줄에 머문다** — 개행 없음, 수직 커서 이동 없음, 화면 지우기 없음 — 그리고 "두 채널을 모두 끄면 키 입력 한 번은 자기 에코만 쓴다". 수정 전 코드에서는 실패하고 여기서는 통과하므로, 이 재그리기를 되살리는 향후 변경은 사용자가 아니라 CI가 먼저 잡습니다. `smart-menu status` 는 이제 플러그인이 그린 후보 행이 아직 화면에 있는지도 보고합니다 — 플러그인이 보유한 유일한 화면 상태입니다.

## [v2.2.9] - 2026-09-22

### 수정
- **17개의 제어 키가 죽어 있었습니다(Ctrl-A, Ctrl-E, Ctrl-K, Ctrl-L, Ctrl-R, Ctrl-U, Ctrl-W 등).** 인쇄 가능한 문자는 두 개의 범위 바인드(`bindkey -R "^@"-"^_"`, `-R " "-"~"`)로 덮는데, 앞의 범위가 **제어 키까지 함께** 가져갔습니다. zsh 5.9에서 측정하면 실행 직후 `bindkey -M emacs '^A'` 가 `self-insert` 를 보고하여, Ctrl-A 는 줄 맨 앞으로 이동하는 대신 문자 그대로의 제어 문자를 삽입했습니다. 살아남은 것은 플러그인이 의도적으로 감싸는 키(`^M`, `^Y`, `^_`, `^G`, `^I`)뿐이었고, 이들은 나중에 다시 바인드되기 때문입니다. 이제 플러그인의 어떤 바인드보다 **먼저** keymap 별로 모든 제어 키의 원래 바인드를 스냅샷하고, 범위 바인드 직후에 복원하므로 Ctrl-A/E/F/K/L/N/P/R/T/U/W/V/X(그리고 `^A`–`^_` 의 나머지)가 사용자 설정대로 동작합니다. 감싸야 하는 키는 이후의 래퍼 바인드가 다시 가져갑니다.
- **`SMART_SUGGEST_STRATEGY=history,completion` 의 completion 쪽은 한 번도 후보를 만들어 내지 못했습니다 — 그리고 그것이 이제 기본값입니다.** 그 뒤의 프로브는 `$( _smart_menu_completion_suffix )` 로 호출되었습니다. 즉 명령 치환 내부이며, `$()` 는 서브셸을 fork 하고 그곳에서는 `zle` 빌트인을 실행할 수 없으므로 프로브는 조용히 빈 값을 반환하고 매 키 입력마다 `after == before` 가 되어, 출시된 날부터 모든 사용자에게 completion 전략은 죽어 있었습니다. 기본값을 `history,completion` 으로 바꾼 이유는 history 만으로는 **사용자가 가장 힌트를 기대하는 지점에 '힌트 공백'이 생기기** 때문입니다. `cd /u` 는 파일시스템 후보가 하나뿐이라 목록이 `SMART_MENU_MIN_MATCHES=2` 로 억제되고, `cd /usr/...` 를 한 번도 실행한 적이 없으면 히스토리에도 일치하지 않아 화면에 아무것도 나오지 않습니다. 그래서 widget 컨텍스트용 `_smart_menu_probe_suffix` 가 stdout 대신 전역 변수로 결과를 반환하고 직접 호출됩니다. 출력용 래퍼는 테스트와 수동 디버깅용으로 남기고, `$()` 안에서 절대 쓰면 안 되는 이유를 주석에 적었습니다. `SMART_SUGGEST_STRATEGY=history` 로 예전 히스토리 전용 동작으로 되돌릴 수 있습니다.
- **인라인 회색 제안이 실제로 입력한 글자와 구분하기 어려워, 한 글자만 입력해도 명령 전체가 이미 입력된 것처럼 보였습니다.** 제안은 `region_highlight` 로 `SMART_SUGGEST_COLOR` 색을 칠하는데 기본값이 `fg=8`(밝은 검정)이었습니다. 많은 테마와 Ubuntu / WSL 기본 팔레트에서 이 색은 일반 전경색과 거의 같은 농도이므로, `l` 뒤에 흐릿한 `s -la /usr/` 가 붙으면 줄 전체가 이미 `ls -la /usr/` 인 것처럼 읽힙니다. 버퍼에는 아무것도 삽입되지 않습니다. `l` 상태에서 Enter 를 누르면 실행되는 것은 `l` 입니다. 기본값이 이제 `auto` 입니다. 256색 터미널에서는 `fg=110`(확실히 눈에 띄는 푸른 회색)으로 해석됩니다. `terminfo[colors]` 로 감지하고, 못 찾으면 `TERM` 으로 판단합니다(`*256color*`, `*truecolor*`, 그리고 kitty / Alacritty / WezTerm / iTerm2 / GNOME / Konsole / foot 등 알려진 256색 터미널). 진짜 8/16색 터미널에는 110번 색이 없으므로 `fg=8` 을 유지합니다. 명시적 지정(`SMART_SUGGEST_COLOR="fg=245,bold"`)은 여전히 그대로 적용됩니다.
- **후보 목록이 첫 키 입력에서 열렸고, 최소 길이가 입력 중인 부분이 아니라 경로 전체를 세었습니다.** `SMART_MENU_MIN_PREFIX`(그리고 명령어용 `SMART_MENU_MIN_PREFIX_CMD`)의 기본값이 `1` 이었으므로 `l` 만 눌러도 일치하는 명령과 히스토리의 전체 목록이 그려졌습니다. 아직 의미 있는 입력을 하지 않았는데도 말입니다. `/etc/l` 에서는 셸 단어 전체를 측정해 6글자로 판정되어 `/` 를 친 직후에도 목록이 떴습니다. 두 키의 기본값이 이제 `2` 이며 **마지막 `/` 뒤의 부분만** 셉니다. `/etc/l` 은 한 글자, `/etc/lo` 에서 목록이 열립니다. 상한인 `SMART_MENU_MAX_PREFIX` 는 여전히 단어 전체를 측정합니다. 참고: 한 번 그려진 목록은 일반 터미널 출력이며 zsh 는 그것을 지우지 않습니다(순정 zsh 도 동일합니다). 따라서 "목록을 덜 그리기"만이 유일한 수단입니다. 그린 뒤 지우는 구현도 만들어 측정했으나 효과가 없어 제거했습니다.
- **Starship 이 권장 2줄 레이아웃이 아니라 자체 기본 프롬프트를 계속 그렸습니다.** `curl ... | bash` 로 실행하면 `templates/` 디렉터리가 없으므로 설정은 두 번째 인라인 사본에서 나오는데, 그 사본에 `format =` 줄이 없었습니다. `format` 이 없으면 starship 은 파일의 나머지를 조용히 무시하고 자체 기본값(`hostname in ~ via 🐍 ... ❯`)을 출력합니다. 이는 v2.2.8 을 새로 설치했을 때 실제로 보인 증상입니다. 폴백 사본은 이제 `templates/starship.toml.example` 과 바이트 단위로 동일합니다(단일 정규 기록 함수, 설치기 테스트 21절로 고정). 기존 `starship.toml` 은 분류됩니다: **권장**(`success_symbol = "[:> ](bold green)"` 표식과 `format` 키를 모두 가짐)은 그대로 두고, **레거시**(이전 버전이 쓴 것으로 `format` 키가 없음)는 `~/.config/starship.toml.bak.<타임스탬프>` 로 백업한 뒤 확인 없이 자동 복구하며, **사용자 정의**는 여전히 먼저 묻습니다.
- **설치기 출력에 영어 하드코딩이 남아 있었습니다.** 남은 블록——패키지 관리자 메시지, zsh / Oh-My-Zsh / atuin / fzf / starship / Entware 각 섹션, 정리, 백업, powerlevel10k, `.zshrc`, 조합 스니펫——이 5개 언어 모두에서 i18n 표를 통과합니다. `install.sh` 는 234키, `install-entware.sh` 는 150키, 하드코딩된 출력 문자열 0개, 빈 문자열로 해석되는 키 0개이며, 참조되는 모든 키가 정의되어 있습니다. 이전에는 5개 키가 정의되지 않아 키 이름이 그대로 출력되었습니다(메인 설치기의 `i.fzf_not_in_feed`, Entware 설치기의 `w.omz_failed`, `w.zsh_theme_write_failed`, `s.zsh_theme_set`, `s.zsh_theme_appended`). 설치기 테스트 22절.

## [v2.2.8] - 2026-09-21

### 수정
- **설치기의 언어 선택 메뉴가 모든 항목을 영어로 표시하여 영어를 읽지 못하는 사용자는 자신의 언어를 선택할 수 없었습니다.** `select_language` 는 기존에 `msg lang.option_*` 을 통해 5개 항목을 그렸는데, 이 표는 `LANG_CODE` 를 따르고 기본 언어(`en`)에서 영어로 폴백하므로 메뉴 전체가 영어가 되었습니다. 이제 메뉴는 각 언어를 해당 언어의 표기(endonym)로 항상 표시합니다: `English / 简体中文 / 繁體中文 / 日本語 / 한국어`. 그래서 영어를 몰라도 각 사용자가 자신의 언어를 알아볼 수 있습니다. 불필요한 `lang.option_*` 키를 삭제하여 `install.sh` 와 `install-entware.sh` 는 동일한 i18n 표와 메뉴를 가지며, 이 일치성은 새 설치기 테스트(第 18 節)로 고정됩니다。

## [v2.2.7] - 2026-09-19

### 수정
- **설치기를 모두 기본값으로 사용해도 화면에 동적 힌트가 두 개 표시: 플러그인 팝업 외에 atuin 자체의 플로팅 검색 TUI.** 기존 권장 설정은 `atuin` 바이너리가 있으면 무조건 `eval "$(atuin init zsh --disable-up-arrow)"` 을 실행했습니다. 이 플래그는 위쪽 화살표만 해제합니다: atuin 은 여전히 **Ctrl-R** 을, 최신 버전에서는 **`?`**(Atuin AI) 까지 바인딩하며, 그 TUI 는 두 번째 전체 화면 UI 입니다(일치하는 기록을 중복 포함해 플로팅 목록으로 표시, Enter 실행). 플러그인은 회색 제안을 위해 atuin 의 SQLite 기록 DB 를 직접 읽으므로 이 바인딩은 아무 이득이 없습니다. 이제 생성되는 설정은 `ATUIN_NOBIND="true" eval "$(atuin init zsh)"` 입니다: 기록과 플러그인의 atuin 백엔드는 그대로, ↑ / Ctrl-R / ? 는 기본 동작을 유지하고 화면에는 UI 가 하나만 있습니다. atuin TUI 바인딩 여부는 설치기의 명시적 질문이 되었습니다(기본값 아니오, 5 개 언어 모두 지원). 또한 atuin init 행을 콤보 스니펫에서 통합 블록으로 옮겨 모든 콤보에서 atuin 기록이 동작합니다(이전에는 Oh-My-Zsh / p10k 콤보에서 조용히 누락). 동봉된 `templates/zshrc.example` 도 같은 규칙을 따릅니다. 플러그인 관리 블록 **밖**의 `atuin init` 행(이전 수동 설정의 잔재 등)을 보고하는 읽기 전용 `_scan_foreign_atuin` 을 추가했습니다 — 없애지 않으면 두 번째 UI 가 조용히 되살아납니다.

## [v2.2.6] - 2026-09-19

### 수정
- **인라인 회색 제안이 색을 잃고, 키 입력마다 좀비 하이라이트 항목이 쌓였습니다.** 플러그인의 `region_highlight` 처리에 있는 두 가지 결함으로, 모두 색을 인식하는 tmux 프로브로 입증했습니다. 1) 항목을 식별하던 마커가 `#주석` 토큰이었는데 zsh는 다시 그릴 때마다 `region_highlight`에서 주석 텍스트를 버립니다 — 이전 항목을 거르는 필터가 더 이상 일치하지 않아 입력마다 불필요한 항목이 쌓였습니다(실측: 5키 -> 8개, fast-syntax-highlighting 로드 시 -> 171개). 이제 zsh가 그대로 보존하는 `memo=` 토큰을 사용합니다. 2) 후보 목록을 그리면 zsh가 행을 다시 렌더링하면서 POSTDISPLAY에 걸친 하이라이트 항목을 BUFFER 끝으로 잘라냅니다(길이 0 = 무색). 플러그인은 이제 목록을 그린 직후 그 항목을 다시 씁니다 — 추가 재렌더링 없이 목록은 그대로 유지됩니다. zsh-syntax-highlighting 과 fast-syntax-highlighting 모두에서 검증했습니다: 올바르게 공존하며 호환 훅이 필요 없습니다 — 이전의 "F-Sy-H 는 외부 항목을 지운다"는 결론은 틀렸습니다.
- **`bash -c "$(curl -fsSL .../install.sh)"` 이 `argument list too long: bash` 로 실패.** install.sh 가 Linux 의 인수당 128 KiB 한도(`MAX_ARG_STRLEN`)를 초과했습니다. 모든 문서는 파이프 형식 `curl -fsSL .../install.sh | bash`(미러 형식: `curl -fsSL .../install.sh | SMART_INSTALL_GH_MIRROR=... bash`)로 통일되어 스크립트를 argv 로 전달하지 않습니다.
- **파이프로 넣으면 설치기 프롬프트가 입력을 기다리지 않음**(`curl ... | bash`): stdin 이 곧 스크립트이므로 일반 `read` 는 즉시 빈 줄을 반환하고 언어 / 프록시 / 미러 메뉴가 말없이 기본값을 택했습니다. 모든 대화형 읽기는 `/dev/tty` 를 다시 여는 `_tty_read` 로 통일했습니다. `BASH_SOURCE[0]` 도 가드했습니다 — stdin 으로 들어오면 설정되지 않습니다(`set -u` 에서는 즉시 중단, 가드가 없으면 `SCRIPT_DIR` 이 조용히 호출자의 현재 디렉터리가 됩니다).
- **Starship 파싱 오류 `Error parsing "format": --> 1:7`(`[$user] › $directory`).** 두 오류가 겹쳤습니다: starship 최상위 `[텍스트]` 그룹에는 `(스타일)` 접미사가 필수이고, 최상위 변수명은 `$username` 입니다(`$user` 는 `[username]` 모듈 내부에서만 유효). 템플릿은 `$username › $directory` 로 바뀌었고, 두 복사본(예제 템플릿과 Entware 설치기의 인라인 복사본) 모두 실제 `starship` 바이너리로 렌더링하는 회귀 테스트로 고정했습니다.

## [v2.2.5] - 2026-09-19

### 추가
- **다른 시작 파일의 충돌 로더 탐지(읽기 전용 경고).** 설치기의 충돌 정리는 `~/.zshrc` 만 편집합니다 — 의도된 설계로, 사용자가 다른 곳에서 관리하는 설정을 건드리지 않기 위함입니다. 그러나 `zsh-autocomplete` / `zsh-autosuggestions` 로더 행이 `.zprofile`, `.zshenv`, `conf.d/*.zsh`, `.zshrc.d/*`, `/etc/zsh/zshrc` 에 들어 있는 경우가 있습니다. 이 행이 남으면 매 `exec zsh` 마다 해당 플러그인이 로드되어 중복 제안 / Tab 충돌이 재발합니다. 정리 후 설치기는 이제 이 파일들을 **검사**하고 정확한 `파일:행번호` 로 **경고**하여 수동 정리를 안내합니다 — 이 파일은 절대 편집하지 않습니다.

- **IP 소속 감지는 이제 '표시할 후보'를 결정합니다 — 중국 본토 외에서는 프리셋을 숨기지만 direct 는 여전히 실측합니다.** 외부 IP 소속 감지 결과는 **중국 본토** / **중국 본토 외** / **감지 실패** 의 3가지입니다. 중국 본토 / 감지 실패: 모든 후보를 표시하고 모든 후보(**direct 포함**)의 속도를 측정하며, 항상 두 가지 수동 입력을 제공합니다. 소속을 알 수 없을 때도 아무것도 숨기지 않고 선택은 사용자에게 맡깁니다. **중국 본토 외: 모든 프리셋 미러를 숨깁니다**(ghproxy 계열과 gitclone.com 은 중국 본토 전용 경로이며, 이 지역에서는 오히려 오판을 부르거나 더 느립니다) — direct 만 남지만 direct 는 **여전히 속도 측정**하고, 두 가지 수동 입력(미러 소스·전체 프록시)도 그대로 유지됩니다. 메뉴 번호는 표시되는 후보에 맞춰 압축되므로 골라도 아무 반응이 없는 빈 번호가 생기지 않습니다. 함께 **`kgithub.com` 을 제거**했습니다(도메인 교체형 미러는 장기적으로 안정적 서비스가 불가능합니다).
- **수동 입력은 두 종류: 미러 소스와 전체 프록시.** 메커니즘이 본질적으로 다르므로 둘 다 제공합니다. **미러 소스**는 GitHub URL 을 재작성합니다(접두사 또는 도메인 교체). **전체 프록시**(시스템 프록시로 설정할 수 있는 종류. 예: `http://127.0.0.1:7890`, `socks5://127.0.0.1:1080`)는 `HTTP_PROXY`/`HTTPS_PROXY` 로 내보내져 curl/git/wget 의 **모든** 요청이 그것을 통과합니다(URL 은 재작성하지 않습니다). 전체 프록시는 채택 전에 도달 가능성을 검사하며, 비대화형에서는 `SMART_INSTALL_PROXY` 로 지정할 수 있습니다.
- **사전 정의 미러에 「중국 본토용」표기.** ghproxy.net / ghproxy.com / mirror.ghproxy.com / gitclone.com 은 중국 본토 네트워크용이므로 라벨에 그 사실을 적었습니다 — 중국 본토 외 사용자가 자신에게는 direct 보다 느린 경로를 선택하는 것을 막기 위함입니다.

### 수정
- **충돌 탐지가 오래된 백업을 잔재로 다시 보고하지 않습니다.** zinit 디렉터리 검색이 부분 문자열 일치이므로, 이전 실행이 남긴 `zsh-autocomplete.bak.<타임스탬프>` 가 실제 플러그인이 이미 없는데도 "충돌 플러그인 디렉터리"로 다시 보고되었고, 삭제를 수락하면 그 백업까지 삭제되었습니다. 이제 백업 디렉터리는(동작 중인 플러그인이 아니므로) 건너뛰며 실제 플러그인 디렉터리만 보고합니다.
- **읽기 전용 검사가 `fzf-tab` 를 충돌로 보고하지 않습니다.** `fzf-tab` 은 지원되는 대안 목록 렌더러(`SMART_MENU_LISTER=fzf-tab`)이므로 충돌로 경고하는 것은 공개된 통합 방식과 모순되었습니다. 이제는 zsh-smart-complete 와 기능이 중복되는 `zsh-autocomplete` / `zsh-autosuggestions` 만 보고합니다.

## [v2.2.4] - 2026-09-16

### 추가
- **`SMART_MENU_LISTER=builtin|fzf-tab` — 명시적인 2택1.** 두 완성 목록 표시기 모두
  그릴 권리가 있으므로, 화면에 팝업이 두 개 뜨는 것은 둘 중 어느 쪽도 고칠 수 없는
  문제입니다. 하나가 멈춰야 합니다. 그 선택이 이제 추론이 아니라 설정입니다:

  ```zsh
  smart-lister                   # 지금 누가 갖고 있는지
  smart-lister fzf-tab           # 저희는 그리지 않고 외부 피커에 넘김
  smart-lister builtin           # 되돌리기
  ```

  넘기는 것은 **목록뿐**입니다. 인라인 회색 제안은 계속 동작하고 `SMART_MENU` 도 그대로라
  `smart-lister builtin` 은 완전한 되돌리기입니다. `builtin`, `smart`, `internal`,
  `native`, `built-in`, `on`, `yes`, `true`, `1` 은 이 플러그인을, `fzf-tab`,
  `fzf_tab`, `fzf`, `ftb`, `external`, `none`, `off`, `no`, `false`, `0` 은 넘김을
  뜻합니다(`off` 는 "저희 목록 끔"이며 "목록 없음"이 아닙니다 — 그것은
  `SMART_MENU=false` 입니다). 알 수 없는 인자는 **명시적으로 보고되고 0이 아닌 값을
  반환합니다**. 이전에는 오타가 상태 출력으로 흘러 "전환에 성공한 것처럼" 보였습니다.
  이 설정은 fzf-tab 을 **설치하지 않습니다**. 설치는 설치 프로그램의 fzf-tab 질문이
  담당하며, 대응하는 `SMART_MENU_LISTER` 를 관리 블록에 기록해 재실행해도 답이 남습니다.
- `smart-doctor` 는 이제 목록의 주인을 보고하고, **로드되지 않은** 피커에 넘긴 경우
  경고합니다. 그리고 이 경고는 다른 모든 판정보다 우선합니다. "아무것도 그려지지 않음"이
  "목록 두 개"보다 나쁜 상태이기 때문입니다. 첫 판정은 "외부 완성 훅이 몇 개 로드되었는지"
  만 보았고, 바로 그 상황에서 그것이 0이라 목록이 나올 수 없는데도 건강한 shell 이라고
  보고했습니다.

### 변경

### 변경
- **단일 열은 이제 옵트인입니다(기본값 꺼짐).** v2.2.3 에서는 기본으로 켰는데, 그것이 잘못된
  기본값이었습니다. 세로 목록은 후보를 *생성*해야만 그릴 수 있고, 생성에는 실제 기능 비용이
  따릅니다.
  - `_main_complete` 를 **우회**하므로, 대상 맥락에서는 후보 **설명**, `list-colors` 색상,
    그룹화, `matcher-list` 가 사라집니다. 문서화된 퍼지 매칭은 생성된 후보에 **적용되지
    않습니다**.
  - 생성되는 것은 명령 / 함수 / 별칭 / 빌트인, 파일 경로, `cd` 최근 디렉터리뿐입니다. 그 밖의
    맥락(git 하위 명령, ssh 호스트, `--옵션`, `sudo …`)은 네이티브 그리드로 폴백하므로
    **입력하는 동안 팝업 모양이 바뀝니다** — 이는 "두 번째 목록이 나타났다"로 오해되기 쉽습니다.
  - 터미널 폭보다 긴 후보는 잘립니다(말줄임표 없음).
  삭제된 것은 없습니다. `SMART_MENU_SINGLE_COLUMN=true` 는 v2.2.3 이 출시한 세로 목록
  그대로입니다. 설치 프로그램은 이제 trade-off 를 질문에 명시하고 묻습니다(기본값 **아니오**).
- `tests/e2e-tmux.sh` 는 **41** 개 어서션으로 늘었습니다. 시나리오 10 은 단일 열 계약을
  순서대로 검증합니다 — 기본은 그리드 (10a), 옵트인하면 한 줄에 하나 (10b), 다시 끄면 그리드
  (10c). 10a 와 10c 가 있기에 10b 는 헛돌지 않고 실패할 수 있는 검사가 됩니다. 시나리오 11 은
  목록 표시기 전환을 BEFORE → OFF → BACK ON 으로 걷습니다. 이 구조에서만 중간 단계가 의미를
  갖습니다(되돌리는 단계가 없으면 "0행"은 그저 완성을 멈춘 shell 로도 충족됩니다). 인라인
  제안이 인계 후에도 살아 있는지도 검증합니다.
- v2.1.6 A/B 기준선은 **재측정**한 것이며 **비례 계산이 아닙니다**: **20/41**. v2.1.6 에는
  팝업도 이 전환도 없어 10a/10b/10c 는 실패하고 시나리오 11 도 대부분 실패합니다. 그 영역에서도
  3개는 통과하지만 **진짜는 1개뿐**입니다 — 인계 후에도 인라인 제안이 남는 것 (v2.1.6 에도
  `POSTDISPLAY` 가 있습니다). 나머지 2개 — 10b 의 "한 줄에 둘이 들어가지 않음" 과 11b 의
  "인계 후 목록을 그리지 않음" — 은 **헛돌이**입니다. 그 빌드는 목록을 아예 그리지 않기
  때문입니다. 이 셋을 구분할 수 있다는 것이 기준선을 과거 분수에서 비례해 계산할 수 없는 이유
  그 자체입니다.
- `tests/test-menu.zsh`: 144 → 183. 시나리오 5 는 이제 **`smart-lister` CLI 자체를 구동**하여
  받아들이는 모든 표기에 대해 이 목록을 인코딩하는 세 곳이 일치하는지 검증합니다. 그것들은
  따로 쓰여 있고(정규화 함수, "인식되는가" 검사, CLI) **이미 어긋나 있었습니다**: CLI 는
  `on` 을 받아들이는데 인식 검사는 거부하여 `smart-lister on` 이 성공하고 바로 다음
  `smart-lister` 가 그것과 모순되었습니다. 또 `no`/`false`/`0` 은 두 보조 함수에는 fzf-tab
  인데 CLI 에서는 상태 출력으로 흘렀습니다. 손으로 베낀 표기 목록으로는 이를 잡을 수
  없습니다 — 지난번에 바로 그렇게 놓쳤습니다. `smart-doctor` 의 새 인계 판정은 시나리오 13b
  에 고정되며, **오탐 쪽**(실제로 로드된 피커는 경고를 해제해야 함)도 포함합니다.
- `tests/test-config.zsh`: 39 → 43. 새 시나리오 5 는 이미 두 번 어긋난 값을 고정합니다:
  5 개 README 에 적힌 단일 열 기본값은 `lib/config.zsh` 의 실제 기본값과 같아야 합니다.
- 합계: **590개 어서션**(zsh 9개 스위트 532 + 설치 스위트 58), e2e **41/41**.

## [v2.2.3] - 2026-09-16

### 수정
- **`Delete` 키가 인라인 잔상을 남겼습니다.** Backspace 는 감쌌지만 `Delete`
  (`ESC [ 3 ~`) 는 그렇지 않아서, 히스토리 후보를 불러온 뒤 한 글자를 지우면 이전 후보가
  화면에 **그대로 얼어붙어** 있었습니다. 이제 `Delete` 도 Backspace 와 똑같이 감싸고
  (emacs / viins 두 키맵) 해제 시 원래대로 돌려놓습니다.
- **단일 열 팝업이 실제로는 그리드로 그려지고 있었습니다.** 채움 폭을
  `local cols=... pad="$cols"` 한 줄로 계산했는데, `local` 명령의 모든 확장은 두 대입보다
  **먼저** 일어나므로 `pad` 가 빈 문자열이 되고 `${(r...)…}` 는 폭 0 으로 채웠습니다.
  **코드는 맞아 보이는데** 목록은 다중 열 그대로였습니다. 이제 폭을 변수 이름으로 넘깁니다
  (`${(r.cols.. .)...}`).

### 추가
- **단일 열(세로) 실시간 팝업** — `SMART_MENU_SINGLE_COLUMN`, 기본값 `true`. 입력 중
  팝업을 zsh 기본 다중 열 그리드 대신 **한 줄에 후보 하나**로 그립니다. 후보를 직접
  생성하고(명령 / 함수 / 별칭, 파일 경로, `cd ` 최근 디렉터리) 모든 **표시 문자열**을 정확히
  `COLUMNS` 폭으로 **채우거나 잘라내므로**, 수학적으로 한 열만 들어갑니다. `false` 로 두면
  기본 그리드로 돌아갑니다.
  - 왜 "가로채기"가 아니라 "생성"인가: `compadd` 를 함수로 가리면 일부 zsh 에서 후보가
    **아예 추가되지 않습니다**(실측). 그러면 팝업이 조용히 비어 버립니다.
  - 입력한 단어는 glob 엔진에 넘기기 전에 이스케이프합니다: `[` 를 입력하면 패턴 `[*` 가
    만들어지고, 잘못된 패턴은 nomatch 가 아니라 생성기를 **중단**시켜 **키 입력마다**
    `bad pattern:` 을 출력했습니다. 다만 앞부분의 `~/` 는 의도적으로 이스케이프하지 않습니다
    (그러지 않으면 모든 `~/…` 후보가 사라지기 때문입니다).
  - 폴백은 유지됩니다: 생성기가 다루지 못하는 맥락(git 하위 명령, ssh 호스트, 옵션 문자열)
    에서는 실제 완성이 그대로 동작합니다.
- **`smart-doctor`** — 화면에 두 번째 후보 목록을 그릴 수 있는 모든 지문을 출력합니다.
  `_main_complete` / `compadd` / `_complete` 가 아직 zsh 순정 진입점인지,
  zsh-autocomplete / zsh-autosuggestions / fzf-tab / 구문 강조가 로드되었는지, 키맵별로
  `Tab` 을 누가 갖는지, 목록을 켤 수 있는 zstyle, 그리고 이 플러그인 자체의 상태 — 마지막에
  판정 한 줄. **읽기 전용**이라 망가진 shell 에서도 안전하게 실행됩니다.
- **설치 프로그램의 선택 항목이 대화형으로.** fzf-tab, Tab 메뉴, 단일 열 레이아웃, 최근
  디렉터리, ↑/↓ 기록 검색, zsh-vi-mode, 제안 출처를 설치 시 하나씩 묻고, 답변은 생성되는
  `~/.zshrc` 의 관리 블록에 기록됩니다. `install.sh` 와 `install-entware.sh` 모두 지원합니다.
  - 이 블록은 의도적으로 **플러그인 로드보다 앞**에 놓입니다. 일부 옵션(특히
    `SMART_MENU_HISTORY_KEYS`)은 플러그인이 키 바인딩을 설치하는 **시점**에 읽히므로,
    나중에 쓰면 조용히 무시됩니다.
  - fzf-tab 은 기본 **꺼짐**(명시적 옵트인)이며, 켜면 내장 선택 메뉴를 강제로 끕니다 —
    둘 다 켜는 것이 바로 두 목록 표시기가 같은 화면 영역을 다투는 원인입니다.
  - `NONINTERACTIVE=1` 에서는 문서화된 기본값을 사용합니다.

### 변경
- **`starship.toml` 템플릿 수정.** 최상위 `format` 이 `[$user]($style)` 였는데
  `($style)` 은 각 `[section]` 내부에서만 유효하므로 **사용자 이름이 삼켜졌습니다.** 이제
  1행은 `[$user] › $directory`, 2행은 `$character` 입니다.
- 설치 프로그램의 `.zshrc` 템플릿에 관리되는 옵션 슬롯을 추가하고, 끝부분의
  "Optional:" 주석 묶음을 질문 대상이 **아닌** 나머지 조정 항목으로 교체했습니다.

### 테스트
- `tests/test-menu.zsh`: 109 → 144. 새 시나리오 13b(`smart-doctor`)와 14(단일 열 후보 생성
  및 단일 열 불변식: 모든 표시 문자열이 정확히 `COLUMNS` 폭이고, 채움/잘라내기가 실제로
  삽입되는 텍스트를 건드리지 않음).
- `tests/test-zle.zsh`: 93 → 102. 두 키맵의 `Delete` 바인딩, 해제 시 반환, 그리고 키 입력마다
  stdout 이 조용할 것.
- `tests/test-config.zsh`: 37 → 39. `SMART_MENU_SINGLE_COLUMN` 기본값과 사용자 재정의.
- **`tests/test-installer-options.sh`(신규, 54개 어서션, bash)** — 설치 프로그램의 옵션
  기구를 추출해 임시 파일에 대해 구동합니다. 소스에서 보이지 않는 성질을 고정합니다: 관리
  블록은 **플러그인 로드보다 앞**에 놓여야 하고, 재실행은 멱등이어야 합니다. 또한 모든
  설치 기본값을 `lib/config.zsh` 와 교차 검증합니다 — 이 검증이 Tab 메뉴 기본값과 실제
  배포 값의 불일치를 잡아냈습니다.
- `tests/e2e-tmux.sh`: 29 → 33. **10** 은 후보 6개가 6행을 차지하고 모두 표시되며 한 행에
  둘이 들어가지 않음을 검증합니다. **10b** 는 `SMART_MENU_SINGLE_COLUMN=false` 로 그리드가
  돌아옴을 검증하여 10 이 실제로 실패할 수 있음을 보입니다.
- 합계: **538개 어서션**(zsh 9개 스위트 484 + 설치 스위트 54), e2e **33/33**.

## [v2.2.2] - 2026-09-16

### 수정
- **`Tab` 뒤의 `Enter`가 이제 줄을 실제로 실행**합니다(단순 다시 그리기가 아님).
  이전에는 완성 후 첫 `Enter`가 accept-line 위젯에 삼켜져(완성을 아직 "진행 중"으로
  간주하여 표시만 새로 고침) 완성된 `cd …`를 실행하려면 `Enter`를 **한 번 더** 눌러야
  했습니다. 이제 위젯은 자체 상태를 정리한 뒤 `zle .accept-line`을 직접 호출합니다.
  이는 **이전부터 존재하던 버그**로, v2.2.1에서도 재현됩니다.

### 추가
- **최근 디렉터리 후보**(`lib/engine/recent.zsh`, `SMART_RECENT_PATHS`, 기본 `true`).
  `cd` / `pushd` / `chdir` 인자를 완성할 때 **실제로 이동했던** 디렉터리가 후보로
  제시되며, `cd ` 뒤의 **빈 단어**에서는 즉시 목록으로 표시됩니다——빈 단어를 목록으로
  보여줄 가치가 있는 유일한 상황입니다. 완성기를 `zstyle ':completion:*' completer`
  앞에 삽입하는 방식이라 사용자의 완성 체인과 공존하며, `smart-recent off` /
  `smart-disable` 시 깔끔하게 제거됩니다.
- **읽기 전용.** 데이터는 zsh 자체의 최근 디렉터리 데이터베이스(`cdr`와 `~[1]`가 쓰는
  것과 동일)이며, 플러그인은 **절대 기록하지 않습니다**. `SMART_RECENT_PATHS_MAX`
  (기본 `20`)가 후보 수를 제한하고, `smart-recent status`가 현재 사용 가능한 항목 수를
  보고합니다.
- **`smart-recent on|off|toggle|status`** 런타임 명령.

### 변경
- **퍼지 매칭은 문서화만 하고 직접 구현하지 않습니다.** 라이브 팝업은 사용자 자신의
  완성 시스템을 실행하므로 `zstyle ':completion:*' matcher-list`가 이미 자동으로
  적용됩니다——이 플러그인에는 **퍼지 매칭 코드가 의도적으로 없습니다**. 직접 추가하면
  compsys와 충돌할 뿐입니다. README의 "선택 기능" 절에 설정할 한 줄을 제시합니다.

### 테스트
- `tests/test-recent.zsh`(38개 어설션): `cd` 인자 위치 판정, 데이터베이스 파싱
  (공백 / 따옴표 / XDG 위치 / 오래된 항목), 그리고 **회귀 테스트로 고정**한 사항——
  완성기가 `zstyle ':completion:*' completer`를 통해 연결되며 `$completer` 배열이
  **아님**(그 변수는 zsh에 존재하지 않으며, 이전 코드는 "연결된 것처럼 보였지만" 실제로는
  아무 일도 하지 않았습니다).
- `tests/e2e-tmux.sh`: **8b**(Tab 후 Enter가 실행)와 **9**(`cd `에서 최근 디렉터리 목록,
  Tab으로 전체 경로 완성) 추가. 총 29개 어설션, **v2.1.6에서는 17/29**.

## [v2.2.1] - 2026-09-16

### 수정
- **라이브 팝업이 더 이상 키 입력을 삼키지 않습니다.** 이전에는 `LISTMAX=-1`을 목록 호출
  앞뒤로 스코프 대입해 zsh의 "N개 전부 표시할까요(M줄)?" 프롬프트를 억제했는데, 이것이
  ZLE의 다음 입력 읽기를 손상시킵니다. `git status`를 입력해도 버퍼에는 `gitstatus`만
  들어가고 셸은 **잘못된 명령**을 실행했습니다. 이제 `LISTMAX`는 전혀 건드리지 않고,
  너무 큰 후보 목록을 아예 그리지 않는 방식(`SMART_MENU_MAX_MATCHES`, 기본값을
  "무제한"에서 `100`으로 변경)으로 억제합니다. 짧은 후보 목록과 1200개 항목 디렉터리
  양쪽에서 모두 깨끗했던 유일한 설정입니다.
- `smart-menu status`와 tick 디버그 로그가 "최소값 미만"과 "상한 초과"를 구분합니다.
  이전에는 둘 다 `below min`으로 기록되어 디버깅 방향을 잘못 잡게 했습니다.

### 추가
- **`SMART_SUGGEST_STRATEGY`** (`history` | `history,completion`,
  zsh-autosuggestions와 동일한 이름). `completion`을 추가하면 히스토리에 없는 경로,
  옵션, 하위 명령도 회색 제안으로 표시할 수 있습니다(커서 위치 단어의 "모호하지 않은
  접두사"를 완성 시스템에 질의).
- **이름 있는 재지정 가능 위젯**: `smart-accept-suggestion`, `smart-accept-word`,
  `smart-execute-suggestion`, `smart-suggestion-toggle`.
- **`SMART_MENU_HISTORY_KEYS`** (기본 `false`): `true`이면 줄이 비어 있지 않을 때
  ↑/↓가 접두사 히스토리 검색(zsh-autocomplete의 대표 기능)이 되고, 빈 줄에서는 일반
  히스토리 이동으로 돌아갑니다.

### 테스트
- `tests/e2e-tmux.sh`에 실제 터미널에서의 **버퍼 무결성** 검증을 추가했습니다. 한 글자씩
  입력해 문자가 유실되지 않아야 하고, **실제로 실행된 명령**이 입력한 그대로여야 합니다.
  이전 스위트는 "목록이 그려졌는가"만 확인해서, 팝업이 모든 명령줄을 망가뜨리는 동안에도
  전부 통과했습니다.

## [v2.2.0] - 2026-09-15

이 플러그인이 존재하는 **전체 이유**를 드디어 제공한 릴리스: `zsh-autocomplete` +
`zsh-autosuggestions`의 두 부분을 단일 플러그인 안에서 네이티브로 구현합니다.

### Added
- **입력하면 팝업되는 후보 메뉴** (`lib/engine/menu.zsh`): 버퍼를 편집할 때마다 후보 목록을
  계산하고 그리므로, 완성이 입력 중에 나타납니다 — `Tab` 불필요. 사용자 자신의 compsys 위에
  프라이빗 완성 위젯(`zle -C ... list-choices`)으로 구현하며, 그 completer는 `compstate[nmatches]`를
  읽어 목록을 그릴지 결정하고 `compinit`은 절대 건드리지 않습니다. 마스터 스위치 `SMART_MENU`,
  실행 시 `smart-menu on|off|status`.
- **행 내부 회색 제안과 후보 목록이共存.** 목록 재도화와 `POSTDISPLAY`는 같은 화면 영역을
  다투므로 순서를 고정했습니다: 먼저 회색 글자를 그리고, 그 다음 목록을 실행하고, 이후 재도화하지
  않습니다. 단일 후보(`SMART_MENU_MIN_MATCHES`)는 목록을 버리고 회색 글자에 양보합니다.
- **적응형 스로틀** (`SMART_MENU_SLOW_MS` / `SMART_MENU_COOLDOWN_KEYS`, 기본 끄기): 임계값 이상
  걸리는 목록 가져오기는 쿨다운을 유발하며, **지속적으로** 비싼 완성을 위한 것. 기본 끄기인 이유는,
  실측에서 유일한 spike가 완성 하위 시스템 로드 시의 일회성 ~180ms이며, 이를 줄이면 세션의 첫
  `git <TAB>`에서 팝업을 잃기만 하고 절약은 그 한 번의 180ms뿐이기 때문입니다.
- **`SMART_MENU_DEBUG=/path/to/log`**: 매 tick 결정(게이트 거부 / 쿨다운에 의한 스킵 / 후보 수 /
  측정 밀리초)을 한 줄씩 덧붙입니다. "팝업 안 뜸"은 "후보 하나라 회색 글자에 양보함"과 구분할 수
  없으므로 이 로그가 도움이 됩니다.
- **`Alt+→`로 단어 하나 수락**: 행 내부 제안의 단어 하나를 수락하고(이후 나머지를 다시 제안) 아무
  수락할 것이 없으면 표준 `forward-word`로 폴백합니다.

### Fixed
- **심각 — 새 세션에서 우측 화살표가 안 먹힘**: `ESC [ C`(CSI)만 바인딩했고, `TERM=xterm-256color`의
  `kcuf1`은 `ESC O C`(*애플리케이션 커서 키* 형식)이며, ZLE은 터미널을 그 모드로 전환합니다. 그래서
  그 키는 zsh 기본 `forward-char`로 갔고 제안은 결코 수락되지 않은 채 회색 글자만 보였습니다 — 전형적인
  "회색 글자는 보이는데 화살표는 죽음" 보고. 이제 모든 화살표 인코딩을 바인딩하고, 시퀀스 목록은
  `terminfo`에 CSI/SS3 폴백을 더해 구성합니다.
- **심각 — 같은 터미널에서 `Alt+→`도 죽음**: `Alt`는 문자 그대로 "`ESC`를 보내고 그 다음 화살표"이므로
  다중 인코딩 문제를 물려받습니다. 구버전은 `ESC [ 1 ; 3 C`(xterm)와 `ESC ESC [ C`만 바인딩했고,
  애플리케이션 커서 키 모드 터미널이 보내는 `ESC ESC O C`는 바인딩되지 않아 리터럴 `^[`를 버퍼에
  떨어뜨렸습니다. `Alt` 형식은 이제 "각 화살표 인코딩에 `ESC`를 접두"하여 **파생**하므로 둘이 다시
  어긋나지 않습니다.
- **바인딩 캡처가 원시 바이트 시퀀스를 오파싱**: `_smart_evt_binding`(`lib/event/zle.zsh`)과
  `_smart_current_binding`(`lib/engine/native.zsh`)은 조회한 시퀀스를 텍스트 매칭으로 벗기려 했으나,
  `bindkey`는 키를 항상 `^X` 기호법으로 에코하므로 원시 바이트 시퀀스(예: terminfo `kcuf1`)에서는
  매칭이 실패하고 *키 텍스트*가 위젯 이름으로 저장되었습니다. 저장된 원본이 오염되어 언바인드 시 키가
  복원되지 않았습니다. 두 파서 모두 이제 `bindkey` 출력의 마지막 필드를 취합니다.
- **루프 내 `local`이 stdout으로 새나감**(`lib/event/zle.zsh`): zsh 5.9는 `local` 선언을 두 번째
  이후 실행할 때 `var='<옛값>'`을 출력하며, 이는 1회 초과 반복되는 루프 안에 쓴 `local`에서 발생합니다.
  그 출력은 ZLE 위젯 경로에서 그대로 명령행에 닿습니다. 모든 루프 변수를 함수 맨 위에서 한 번에 선언하도록
  했고, `tests/test-zle.zsh`가 bind/unbind 사이클의 무음을 어설트합니다.
- **`zmodload -F` 기능 접두사**: `EPOCHREALTIME`과 `terminfo`는 *파라미터*이므로 `p:EPOCHREALTIME` /
  `p:terminfo`로 요청해야 합니다. 거부된 `b:` 요청으로 둘 다 미정의인 채로 남아 스로틀과 terminfo
  유래 화살표 시퀀스가 조용히 비활성화되었습니다. 회귀 테스트 추가.
- **연관 배열 따옴표 첨자**: `assoc["km|seq"]=x`는 따옴표를 키 이름의 일부로 저장하므로 `assoc[km|seq]`로
  도달할 수 없습니다. 키는 이제 변수로 조립하고 인용 없는 첨자로 색인합니다(`lib/state.zsh`가 이미
  지키던 규칙과 동일).
- **설치기**: *왜* `zsh-autocomplete` / `zsh-autosuggestions`를 제거하는지(두 동작 모두 이제 네이티브)
  설명하며, 더 이상 조용히 삭제하지 않습니다.

## [v2.1.6] - 2026-09-15

### Fixed
- **심각 — 인쇄 가능 ASCII 입력이 삼킴**: `_smart_evt_binding`이 `bindkey -R "^@-^_"` 범위 쿼리에서
  의사 위젯 `undefined-key`를 캡처해 인쇄 가능 키를 거기에 디스패치했으므로, `zle undefined-key`(무효
  동작)가 모든 ASCII 키입력을 먹었습니다. CJK/UTF-8(바이트 >= 0x80, 재바인드 범위 밖)은 진짜
  `self-insert`로 계속 삽입되어 "중국어는 되고 영어는 안 됨". 캡처는 이제 `undefined-key`를 미바인드로
  정규화해 `self-insert`를 쓰고, `_smart_evt_dispatch`도 방어하며, `_smart_current_binding`(native.zsh)
  역시 강화. `tests/test-zle.zsh`에 회귀 테스트 추가.
- **키 캡처 강화**: self-insert 원본 위젯은 이제 범위 탐색이 아니라 하드코딩합니다(범위 쿼리는 우리 바인드
  전에는 `undefined-key`, 후에는 우리 wrapper를 반환하며 어느 쪽도 쓸 수 있는 원본이 아님). 캡처는 전용
  플래그 `_SMART_EVT_CAPTURED`로 방어하며, 단일 `ORIG_*` 변수의 내용이 아니라 하므로, 오래되거나 수동
  설정한 `_SMART_EVT_ORIG_SELF_*`가 전체 캡처를 건너뛰는 일(그러면 네이티브 Tab 바인드와 다른 모든 원본도
  조용히 사라짐)이 없어졌습니다. 캡처 탐색은 또한 어떤 `_smart_*` / `smart-*` 위젯 기록도 거부하여, 재캡처가
  우리 wrapper로 다시 디스패치되지 않습니다.
- **설치기 — 관리 블록 마커가 쓰여지지 않음**: `build_zsc_integration`이 bash 스크립트 안에서 `print -r --`
  (zsh 내장)을 써서 호출이 조용히 실패했고, `# >>> zsh-smart-complete integration (managed) >>>` / `# <<< ... <<<`
  마커 줄이 빠졌습니다. BEGIN 마커가 없으면 `_upsert_zsc_block`이 맞을 수 없어 재설치마다 중복 블록을
  덧붙였습니다. 이제 `printf '%s\n'` 사용.

### Added
- **선택적 `zsh-vi-mode`(opt-in, 기본 NO)**: vi 키바인드는 확실히 유용하나, 이 플러그인은 키맵 전체를
  소유하고 매 라인 초기화에서 ZLE을 재초기화하므로 다른 플러그인 바인드를 부수는 전형적 원인입니다 — 그래서
  암묵적 설치는 절대 없습니다. 사용자가 선택하면 설치기는 그것을 클론하고, 우리 위젯을 *이전*에 로드하며
  `zvm_after_init` / `zvm_after_lazy_keybindings`로 재적용하는 블록을 씁니다.
- **설치기가 흐름 중 fast-syntax-highlighting 설치**(`_ensure_zinit_plugin
  zdharma-continuum/fast-syntax-highlighting`), 풀 콤보와 플러그인 양 경로 모두, Zinit 첫 시작 자동
  클론에 더 이상 의존하지 않습니다.

### Changed
- **설치기 .zshrc 전략**: 완전 권장 `.zshrc` 템플릿은 이번에 풀 스택을 (재)설치한 경우(Phase 0/5 콤보)에만
  권장. 플러그인만 설치는 이제 마커 구분 `zsh-smart-complete` 블록만 관리(멱등 upsert, 파일 전체를 덮어쓰지
  않음).

## [v2.1.5] - 2026-09-15

### Fixed
- **설치기 — p10k/OMZ 제거기**: `_remove_p10k` / `_remove_omz`는 이제 Zinit 클론 플러그인 dir
  (`$ZINIT_PLUGINS_DIR` 아래 `romkatzen---powerlevel10k`, `OMZ::ohmyzsh---ohmyzsh`)도 삭제하므로,
  비 p10k/OMZ 콤보를 고르면 다음 시작에 다시 로드되는 낡은 잔재를 완전히 지웁니다. `.bak.*` 산물은
  계단식 백업을 피해 직접 삭제.
- **설치기 — `.zwc` 바이트코드**: 플러그인 갱신 경로(`git reset --hard`)는 이제 Zinit 컴파일 `*.zwc`
  캐시도 제거하므로 엔진 수정이 갱신 후 실제로 효력(이전엔 낡은 컴파일 코드가 로드됨).
- **엔진 — 전역 누수**: `lib/engine/suggest.zsh`의 `cmd_cwd` / `cmd_host` / `cmd_exit`는 이제 `local`
  선언(이전엔 매 키입력마다 전역으로 새어나감).
- **엔진 — 히스토리 상한**: `_SMART_CMDS`는 이제 `SMART_SUGGEST_HISTORY_LIMIT`(기본 20000)로 제한.
  `SMART_HISTORY_REBUILD_EVERY=0`으로 주기 재구성을 끄면 가장 오래된 항목을 버리고 bucket/assoc 슬롯을
  동기 유지.

## [v2.1.4] - 2026-09-12

### Fixed
- fzf 설치가 조용히 건너뛰어짐(비대화). 설치 진행이 두 번 표시(Phase 0/5 그리고 Phase 1-4). `RAN_COMBO`
  가드 추가하고 fzf 프롬프트를 대화식으로.

## [v2.1.3] - 2026-09-11

### Fixed
- 모든 y/N 프롬프트에서 `read: -: invalid option` 크래시 — `IFS=$'\n\t'`가 `read $_args`를 망가뜨림.
  `read "$@"`로 변경.

## [v2.1.2] - 2026-09-10

### Fixed
- 설치기 프롬프트는 이제 사용자가 각 단계를 확인할 때까지 블록. 충돌 플러그인 `.bak.*` 계단식 수정(주 dir은
  한 번만 백업). 낡은 플러그인은 이제 `git fetch --depth 1` + `git reset --hard`로 실제 갱신.

## [v2.1.1] - 2026-09-09

### Added
- **zsh 재설치 프롬프트**: zsh가 이미 설치돼 있으면 brew(macOS)나 apt(Debian/Ubuntu)로 재설치/업그레이드
  권유.
- **fast-syntax-highlighting**: `.zshrc` 템플릿의 `zinit light
  zdharma-continuum/fast-syntax-highlighting`으로 로드(Zinit는 시작 시 자동 클론). install.sh가 직접 관리
  않음.
- **i18n 메시지**: zh-CN, zh-TW, ja, ko, en에 `prompt.zsh_reinstall` 추가.

### Changed
- **Phase 0**: 풀 콤보 설치에 zsh 재설치 로직 포함. fast-syntax-highlighting은 Zinit 경유 `.zshrc` 템플릿
  로드.
- **Phase 1-3**: starship/atuin/zinit 프롬프트의 `SKIP_DEPS` 가드 복원.

### Fixed
- zsh 재설치 프롬프트는 올바른 brew/apt 폴백 로직 사용.

## [v2.1.0] - 2026-09-08

### Added
- **Phase 0/5**: 완전 권장 콤보 설치(zsh + fzf + starship + atuin + zinit + zsh-smart-complete).
- **대화식 백업 정리**: 충돌 플러그인 잔재(`.cache/p10k-*`, `.cache/zsh*`, `.local/state/zsh-autocomplete`
  등) 정리 권유.
- **fzf 자동 설치**: 패키지 관리자로 못 구하면 GitHub에서 클론.

### Changed
- `SKIP_DEPS!=1`이고 `NONINTERACTIVE!=1`일 때 설치기는 이제 먼저 Phase 0 실행. Phase 0 건너뛰면 Phase 1-3이
  폴백.

## [v2.0.6] - 2026-08-26

### Fixed
- 릴리스 워크플로: tar/zip 전에 파일 stage하여 'file changed' 경합 회피.

## [v2.0.5] - 2026-08-26

### Fixed
- `mirror.chosen` 메시지의 잘못된 변수 치환.
- 낡은 `.bak.*` 잔재 정리.

## [v2.0.3] - 2026-08-26

### Fixed
- i18n: 남은 모든 중국어 상태 메시지 번역.
- SSH 입력 문제 수정.

## [v2.0.2] - 2026-08-26

### Fixed
- i18n: 미러 선택 메뉴 완전 국제화.

## [v2.0.1] - 2026-08-26

### Fixed
- 설치기 문제 3건 해결: i18n 콤보 메뉴, OMZ/p10k 기본 yes, starship.toml 이스케이프.

## [v2.0.0] - 2026-08-25

### Added
- 엔진과 설치기 재작업.
- O(bucket) 접두사 인덱스.
- de-subShell 스코어링.
- 실시간 증분 인덱스.
- Zsh 감지.
- OMZ/p10k 콤보 셀렉터.
- Entware 설치기.
- ZLE 행 편집기를 어지럽히던 매 키입력 stdout 누수 중단.

## 이전 릴리스

```
v0.1.0  ZLE 프론트엔드, 히스토리 인덱스, 제안 엔진
   │
v0.1.3  결정적 순위(감쇠 + 빈도 + CWD 부스트)
   │
v0.2.0  Atuin SQLite 백엔드(host / exit / CWD 인식 순위)
   │
v1.0.0  GA — 안정 공개 API, CI/CD, 자동 릴리스
   │
v2.0.0  엔진과 설치기 재작업 — O(bucket) 접두사 인덱스, de-subShell
        스코어링, 실시간 증분 인덱스, Zsh 감지, OMZ/p10k 콤보
        셀렉터, Entware 설치기
   │
v2.1.0  Phase 0 풀 콤보 설치(zsh + fzf + starship + atuin + zinit +
        zsh-smart-complete), 대화식 백업 정리
   │
v2.1.6  인쇄 가능 ASCII 입력 수정(undefined-key), 캡처 강화,
        fast-syntax-highlighting, 콤보 인식 .zshrc, opt-in zsh-vi-mode
   │
v0.5.x  smart-shell-engine(Rust / Go, IPC 경유)(미래, opt-in)
   │
v2.0    Smart Shell — 완전한 독립 셸(미래)
```
